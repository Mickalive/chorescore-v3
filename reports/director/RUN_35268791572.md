# Director RUN_35268791572 — V3-08 Repair

**Cycle:** 35268791572
**Criterion:** V3-08 — Finition, accessibilité, coût et release mobile
**Decision:** **repair → continue**
**Next cycle:** V3-08 repair (same criterion, WIP baseline preserved)

## Summary

The audit decided repair on V3-08. Trusted verification failed with exit code 2 due to a single unescaped apostrophe in `__tests__/domain/cost-gates-v3-08.test.ts` line 205. The string literal `'V3-08 Gate 2: Tab switching doesn't reload same objects'` terminates prematurely at the apostrophe in `doesn't`, causing 10 cascading TypeScript parse errors (TS1005 `,` expected, TS1002 unterminated string literal, TS1128 declaration expected). This is a trivial mechanical defect — the entire V3-08 cost-gate test suite is blocked from running.

All V3-01 through V3-07 remain accepted (391 tests green from the previous cycle). No regression risk.

## Must-fix findings (current cycle)

### 1. Unescaped apostrophe in describe string literal

**Path:** `__tests__/domain/cost-gates-v3-08.test.ts` line 205

```
describe('V3-08 Gate 2: Tab switching doesn't reload same objects', () => {
```

The `'` in `doesn't` terminates the single-quoted string literal prematurely. TypeScript sees `doesn` as a complete string, then `t` as an unexpected identifier, producing 10 cascading parse errors across lines 205–260. Typecheck exits 2 and the full test suite never runs.

**Required fix:** Escape the apostrophe: `'V3-08 Gate 2: Tab switching doesn\'t reload same objects'` or switch the outer quotes to backtick: `` `V3-08 Gate 2: Tab switching doesn't reload same objects` ``. Then verify `tsc --noEmit` exits 0 and the full `npx jest` suite runs green (expected: 19+ suites, 391+ tests). The same string also appears in a comment on line 202 (no TS impact, but should be consistent).

## What was correct and is preserved

- V3-08 cost-gate test suite is comprehensive: 10 gates covering 50k reads, tab switching, sync deltas, bounded writes, classification cache, privacy gate, cost budgets, offline fallback, golden path E2E, and accessibility/design system.
- All V3-01 through V3-07 remain accepted.
- No chrono, no premium, no paywall, no Firebase types in delta.
- WIP baseline preserved — the next cycle should repair the string literal on the existing candidate rather than rebuild.

## Next task: V3-08 Repair — Fix Apostrophe, Confirm Typecheck and Full Test Suite

The next cycle must:

1. **Fix the string literal** in `cost-gates-v3-08.test.ts` line 205 (escape apostrophe or switch to backtick).
2. **Verify `tsc --noEmit` exits 0** — all 10 parse errors resolved.
3. **Verify full `npx jest` suite runs** — expected 19+ suites, 391+ tests, all green.
4. **V3-08 remaining acceptance** depends on passing tests plus visual audit, accessibility, offline, Android APK, iOS readiness — those are addressed by the trusted release finalizer after the code candidate is clean.

## Preserved

V3-01 through V3-07 must not regress. The two independent ledgers, explicit-only cross-ledger compensation, 100% free model, no chrono, adult premium visual direction, historical unit semantics, privacy plane separation, local-first/delta-only backend architecture, bounded network cost, incremental analytics/classification cache, and all already-completed criteria remain in force.
