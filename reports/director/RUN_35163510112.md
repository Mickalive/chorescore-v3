# Director Report — Cycle 35163510112

## Decision: continue

**V3-03 accepted.** All four must-fix findings from the previous repair cycle are resolved. The V3-03 criterion — Ajouter + historique — is now complete.

## Progress Evidence

- **V3-03 accepted** by auditor cycle 35163510112. All 4 must-fix findings resolved.
- **MF-1 — Composite cursor**: Implemented `(occurredAt, id)` composite cursor with `ORDER BY occurredAt DESC, id DESC` and predicate `(occurredAt < c.o OR (occurredAt = c.o AND id < c.i))` in both InMemory and SQLite repositories for contributions, expenses, and settlements. `paginateActivityLog` uses consistent composite cursor. Test seeds PAGE_SIZE+5 entries with one identical `occurredAt` plus older entries; asserts every seeded id is reachable exactly once. 50-trial fuzz confirms no missing/duplicate ids across both algorithms.
- **MF-2 — Concurrent load guard**: `loadHistory` guarded by an in-flight ref that skips concurrent load-more calls. Append step dedupes by entry id. Concurrent load-more test asserts no duplicate ids.
- **MF-3 — Per-entry unit**: Share message and history row display use entry's stored unit (`e.unit`) instead of current household unit. Historical unit labels preserved after Minutes↔Points switch (constitution §18).
- **MF-4 — Custom-split submit blocked**: Button disabled via memoized validation when `sum(customShares) != amountMinor`; `submitExpense` early-returns. Invalid custom-split expense cannot enter the ledger.
- **Trusted verification PASSED**: exit 0, typecheck green, 138 tests / 11 suites green, Expo export Android ok, prebuild ok, Android native debug build BUILD SUCCESSFUL.
- **No regression**: V3-01 ledger invariants, V3-02 three-tab design and local-first architecture all preserved. No chrono/premium/paywall reintroduced. Cost gates bounded reads, no full scan.

## Minor findings (accepted, not blocking)

1. Share adapter instantiated directly (`new LocalSystemShareAdapter()`) instead of via `services.share` from app context — DI bypass, port remains honest.
2. Alert date selector offers 4 buttons; Android supports ≤3, so 'Hier' is dropped on Android — `occurredAt` selection still works.
3. Merge limit of 10,000 is technically a cap; `loadHistory` fetches both repos regardless of active filter — no hard 150 cap, bounded per-page reads satisfied.

## Next Task: V3-04 — Balances + compensation

Evolve `ScoreScreen` into Balances with dual-ledger view. Per constitution §8-9 and roadmap V3-04:

1. **Contribution balance** — per-member balance using stored unit (minutes or points), perpetual, zero-sum invariant enforced.
2. **Financial balance** — per-member balance using stored currency, perpetual, zero-sum invariant enforced.
3. **Time period filters** — Semaine / Mois / Année / Depuis le début as views only; perpetual balance remains identifiable; no archive locked.
4. **Settlement suggestions** — reimbursement proposals derived from financial ledger.
5. **Cross-ledger compensation** — manual flow with group rate, Before/After preview, `CrossLedgerSettlement` write with immutable snapshot (rate, source value, target value, members, author, timestamp). Constitution §8: default disabled, group enables explicitly.
6. **Filtered history** — compact transaction history at bottom, filterable by type (contribution / expense / compensation).
7. **Visual direction** — forest green desaturated (positive), wine red desaturated (negative); two blocks visually distinct; names as primary identifiers; no ranking/podium/competition.

**Cost regression gate**: Balances must be materialized views derived from ledger entries — never recomputed by replaying full history at each screen render. No N+1 queries, no full scans. An action on the Balances screen must not have cost proportional to total history length.

Preserve all V3-01, V3-02, and V3-03 acceptance.
