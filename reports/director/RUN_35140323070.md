# Director Report — RUN 35140323070

- **Cycle**: 35140323070
- **Criterion**: V3-02 (REPAIR)
- **Audit decision**: `repair`
- **Director decision**: `continue`

## Summary

The repair correctly resolved 5 of the 6 previous must-fix findings:

1. **expo-sqlite** — pinned to `~57.0.3`; lockfile regenerated with resolved entry. FIXED.
2. **bare '>'** in `app/index.tsx` — replaced with `{'>'}`. FIXED.
3. **tabLabel** — added to Text `Variant` union. FIXED.
4. **assets/icon.png** — real 1024×1024 PNG created; prebuild/export resolve it. FIXED.
5. **openHousehold** — now calls `setCurrentHouseholdId(id)` before `router.push('/(tabs)')`. FIXED.
6. **SQLite wiring** — `RepositoryFactory` created with device/fallback selection; `AppContext` builds repos through `createRepositories()`. PARTIALLY FIXED — persistence layer wired but demo fixture broken and no tests added.

## Remaining must-fix findings

### MF-7: Demo fixture regression (`src/features/app/AppContext.tsx`)

`ensureDemoFixture` creates the demo household via `repos.households.create()` (line 146), which assigns a **generated** id. But every subsequent fixture record (memberships, members, tasks, entries, todos) and `signIn`/`loadHouseholds` still reference the constant `DEMO_HOUSEHOLD_ID` (`'h-core'`). `loadHouseholds` resolves `getById('h-core')` → `null`, so the root Groups screen shows an empty list after demo sign-in. Empirically confirmed via jest test: `loaded.length = 0` (expected 1).

Additionally:
- `reposReady` (line 73) is set but never consumed — sign-in before repo init permanently skips the fixture.
- Demo user created with generated id so `'demo-user-alex'` never exists; duplicate user rows inserted on every sign-in.

**Required fix**: Seed/create demo household and user under consistent canonical ids, or use returned ids consistently. Consume `reposReady` or retry fixture after repo init.

### MF-8: No SQLite-backed repository tests (`__tests__/infrastructure/repositories.test.ts`)

The required fix explicitly demands "repository tests cover the SQLite-backed repositories", but zero such tests were added. `SqliteRepositories` (781 lines) and `RepositoryFactory` have no test coverage.

**Required fix**: Add tests covering SQLite-backed repository CRUD round-trips and factory fallback selection.

## What was preserved

- **V3-01 domain untouched**: zero changes under `src/domain` or `__tests__/domain`.
- **70 tests / 6 suites green** (trusted verification).
- **expo export/prebuild/Android build all succeed**.
- **No chrono/premium/paywall/billing** in product code.
- **Three tabs exactly** (Ajouter, Balances, A faire).
- **V3 design system** unchanged (graphite/off-white/metallic).
- **Ports/adapters provider-agnostic**.

## Next task

Repair V3-02 must-fix findings:

1. Fix demo fixture regression — ensure `ensureDemoFixture` creates the demo household and user under consistent canonical ids (`DEMO_HOUSEHOLD_ID 'h-core'`, `'demo-user-alex'`), and consume `reposReady` so sign-in converges correctly.
2. Add SQLite-backed repository tests covering CRUD round-trips and the `RepositoryFactory` fallback selection logic.

Preserve V3-01 domain untouched and all previously accepted V3-02 work (expo-sqlite ~57.0.3, bare '>' fix, tabLabel, assets, openHousehold fix). Re-run `npm run check` and verify trusted verification passes.
