# Director RUN_35277862075 — V3-08 Repair of Trusted Finalizer Failure

**Cycle:** 35277862075
**Criterion:** V3-08 — Finition, accessibilité, coût et release mobile
**Decision:** **repair → continue**
**Next cycle:** V3-08 repair (same criterion, WIP baseline preserved)

## Summary

The audit decided repair on V3-08. Trusted verification **passed** (exit 0): typecheck clean, 21 suites / 426 tests pass, privacy:check PASS (static contract + 71 tests), cost:check PASS (21 cost-gate tests), expo install --check OK, expo export android OK, prebuild OK, native debug build BUILD SUCCESSFUL. The three npm scripts required by the trusted finalizer's gates step are now present and honest. iOS export and release APK verified independently. One must-fix finding remains: the E2E script's APK path guard contradicts the finalizer workflow's invocation pattern.

## Context: the trusted finalizer failure (run 35277775025)

The previous accepted candidate (ef9f1d3) had **none** of the three npm scripts (`privacy:check`, `cost:check`, `e2e:android`) in `package.json`. The trusted finalizer's gates step ran `jq -e '.scripts["privacy:check"]'` and failed immediately with `set -euo pipefail`. This reopened V3-08 for repair.

The current cycle (35277862075) added exactly 4 files — `package.json` (+3 script entries) and three new gate scripts. The gates step repair is correct and all checks pass.

## Must-fix finding (remains)

### F1 — `scripts/e2e-android.js` requires `CHORESCORE_APK_PATH` that the finalizer never sets

- **Path:** `scripts/e2e-android.js` line 205 (docstring line 14).
- **Problem:** The script's first try-block statement is:
  ```js
  if (!apkPath || !fs.existsSync(apkPath)) throw new Error(`Unreadable CHORESCORE_APK_PATH: ${apkPath}`);
  ```
  The trusted finalizer emulator step (`.github/workflows/chorescore-v3-finalize.yml` lines 84–94) runs:
  ```bash
  apk=$(find android/app/build/outputs/apk/release -type f -name '*.apk' | head -1)
  adb install -r "$apk"
  npm run e2e:android
  ```
  There is **no** `CHORESCORE_APK_PATH` env var anywhere in the workflow (grep confirms). The script's own docstring says the variable is "optional if already installed" — the implementation contradicts this.
- **Evidence:** Simulation with the env var deleted reproduces `throw Error('Unreadable CHORESCORE_APK_PATH: undefined')` and exit 1.
- **Consequence:** The finalizer would fail at the Android API 35 install/launch/golden-path step, reopen V3-08 again, and the repair objective would not be met.
- **Required fix:** Make the APK path optional when the package is already installed. Two approaches:
  1. Check `adb shell pm list packages <packageName>` first; if installed, skip the install step entirely and only require/use `apkPath` when an install is actually needed.
  2. Auto-locate the APK under `android/app/build/outputs/apk/release/` when `CHORESCORE_APK_PATH` is unset, falling back gracefully.
- **Verification:** In the finalizer environment (no `CHORESCORE_APK_PATH`, package pre-installed via `adb install -r`), `npm run e2e:android` must reach the golden-path steps instead of throwing at the first guard.

## What the candidate does correctly

- **Gates step repaired:** the three npm scripts the finalizer requires now exist and pass (`privacy:check`, `cost:check`, `e2e:android` jq checks + runs).
- **Honest gates:** `privacy-check.js` performs a real static contract scan + 71 focused privacy tests; `cost-check.js` runs the 21 cost-gate tests. Both exit 0.
- **No product regression:** zero product files changed; all 426 tests pass.
- **iOS export and release APK verified independently** (the two finalizer steps the trusted verification does not cover): both exit 0.
- **Native debug build** successful (395 tasks, 22m 18s).

## Candidate delta

Exactly 4 files vs pristine accepted tree (ef9f1d3):

- `package.json` (+3/−0) — adds `privacy:check`, `cost:check`, `e2e:android` npm scripts
- `scripts/privacy-check.js` (new, 153 lines) — static privacy release contract + focused privacy regression tests
- `scripts/cost-check.js` (new, 43 lines) — runs the V3-08 cost-gate tests
- `scripts/e2e-android.js` (new, 285 lines) — adb-based Android golden-path E2E

`src/`, `app/`, `__tests__/`, `docs/`, `governance/`, `directives/` are byte-identical to pristine (`diff -r` exits 0). No product code touched → no regression risk to V3-01..V3-07.

## V3-08 acceptance criteria status

| Acceptance | Status |
|---|---|
| Tests et privacy gate verts | ✅ Met — 426/426 tests; privacy:check PASS |
| Cost regression gate vert | ✅ Met — cost:check PASS (21 gates) |
| Android export/prebuild/release APK | ✅ Met — verified independently |
| iOS export readiness | ✅ Met — verified independently |
| Golden path E2E automatisé | ❌ **Blocker** — e2e-android.js throws without CHORESCORE_APK_PATH |
| Installation lancement sans Metro | ⏳ Blocked by the same E2E script guard |
| V3-01..V3-07 preserved | ✅ Met — no product delta; 426 tests pass |

## Next task: V3-08 Repair — Make APK Path Optional in E2E Script

The next cycle must:

1. **Fix `scripts/e2e-android.js`** — make `CHORESCORE_APK_PATH` optional when the package is already installed (check `adb shell pm list packages` or auto-locate APK).
2. **Match the documented contract** — the script's docstring says "optional if already installed", so the implementation must honor that.
3. **Verify the script runs without `CHORESCORE_APK_PATH`** — simulate the finalizer environment.
4. **Verify `tsc --noEmit` exits 0** — no new typecheck errors.
5. **Verify full `npx jest` suite runs** — expected 21 suites, 426 tests, all green.
6. **Preserve the WIP baseline** — repair the single script, do not rebuild the candidate.

## Preserved invariants

V3-01 through V3-07 remain accepted. All constitutional invariants are maintained: two independent ledgers, explicit-only cross-ledger compensation, 100% free model, no chrono, adult premium visual direction, historical unit semantics, privacy plane separation, local-first/delta-only backend architecture, bounded network cost, incremental analytics/classification cache.
