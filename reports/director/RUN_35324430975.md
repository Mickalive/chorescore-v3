# Director RUN 35324430975 — V3-08 repair: expo dependency gate red + workflow blocker persists

## Summary

**Decision: `continue`** — next factory cycle must repair the Builder-fixable mustFix (F1); the trusted shell owns the remaining workflow blocker (F2).

The audit decision for this cycle is `repair` with `hasDelta=true`. Trusted verification exited **1** (`npx expo install --check` reports outdated dependencies), which is a mandatory mustFix and forbids `accept`. The candidate delta adds only a wrapper contract test and does not address either blocker. The coherent accepted baseline is retained as WIP for the next repair cycle.

## What happened

### Trusted verification (exit 1)

`reports/audits/TRUSTED_35324430975.md` shows all product gates green before the failure:

- `npm run typecheck` — PASS
- `npm run test` — 22 suites / **431 tests PASS** (including the new `v3-08-finalizer-wrapper.test.ts`)
- `privacy:check` — PASS (71 tests)
- `cost:check` — PASS (21 tests, 10 gates)
- `npx expo install --check` — **FAIL**: `Found outdated dependencies` → `Trusted verification exit code: 1`

No `expo export`, `prebuild`, or `assembleRelease` was reached, so no APK can be produced while this gate is red.

### Candidate delta

- `__tests__/domain/v3-08-finalizer-wrapper.test.ts` — **added** (127 lines, staged). Guards the Builder-scoped half of the repair: `scripts/finalizer-e2e.sh` exists and parses as bash, contains `set -euo pipefail`, APK locate, empty guards, `adb install -r`, `npm run e2e:android`; `package.json` exposes `e2e:android`; `scripts/e2e-android.js` auto-locates the APK; fixture alignment for `Ajouter`/`Balances`/`A faire` and absence of chrono/premium.
- No change to `scripts/finalizer-e2e.sh` (identical to pristine, `bash -n` OK).
- No change to `.github/workflows/chorescore-v3-finalize.yml` (still `script: |`).
- No change to `package.json` dependencies.

The added test is harmless and correctly asserts the wrapper contract, but it does not fix either mustFix.

## Must-fix findings

### F1 (mustFix, Builder-fixable) — `expo install --check` outdated dependencies

- **Path:** `reports/audits/TRUSTED_35324430975.md` (trusted verification exit code 1)
- **Problem:** `npx expo install --check` exits 1 with `Found outdated dependencies`. The trusted finalizer requires this check to pass before `expo export`/`prebuild`/`assembleRelease`; no APK or emulator E2E can be produced while it fails.
- **Evidence:** Log tail lists 5 packages needing update: `expo@57.0.23` expected `~57.0.24`, `expo-constants@57.0.18` expected `~57.0.19`, `expo-router@57.0.21` expected `~57.0.22`, `expo-sharing@57.0.20` expected `~57.0.21`, `@expo/metro-runtime@57.0.15` expected `~57.0.16`. `package.json` pins `expo ~57.0.19`, `expo-constants ~57.0.16`, etc., unchanged vs pristine.
- **Required fix:** Builder: align `package.json`/`package-lock.json` to the expected versions (e.g., run `npx expo install --fix` and commit), then re-run trusted verification.
- **Verification:** Trusted verification exits 0: `npm run check`, `privacy:check`, `cost:check` PASS, `npx expo install --check` PASS (no outdated), `expo export` android/ios PASS, `prebuild` PASS, `assembleRelease` builds `app-release.apk`.

### F2 (mustFix, trusted-shell-only) — finalizer emulator step still broken

- **Path:** `.github/workflows/chorescore-v3-finalize.yml` (emulator step, lines 90-94)
- **Problem:** Multi-line `script: |` is line-split and executed via `sh -c` by `reactivecircus/android-emulator-runner@v2`; `set -euo pipefail` exits 2 under dash before APK install/golden path. Same root cause as finalizer runs 35293489693, 35302658175 and 35315519533. The candidate does not change the workflow, so the blocker persists; the next finalizer run will fail identically even after F1 is fixed.
- **Evidence:** HEAD workflow still `script: |` (lines 90-94); `scripts/finalizer-e2e.sh` exists but is never invoked; local reproduction `sh -c 'set -euo pipefail'` → exit 2 vs `bash -c` → exit 0; action source confirms line-split via `sh -c`.
- **Required fix:** Trusted shell on `main`: replace the emulator step's multi-line `script: |` block with single-line `script: bash scripts/finalizer-e2e.sh`, then re-run the trusted finalizer. No additional product code needed for this item.
- **Verification:** Trusted finalizer passes the Android API 35 emulator step (release APK install, launch without Metro, golden path `Ajouter`/`Balances`/`A faire` with the demo fixture) with exit 0, and `finalize-success` attests V3 RC complete with `android-release-apk-sha256-<sha>` and `android-api35-install-launch-e2e-green`.

## Independent checks

| Check | Result |
|---|---|
| Audit decision | repair (hasDelta=true) |
| Trusted verification | **exit 1** — typecheck PASS, 431 tests PASS, privacy PASS, cost PASS, `expo install --check` FAIL |
| Delta vs pristine | 1 staged file added (`v3-08-finalizer-wrapper.test.ts`); workflow, `finalizer-e2e.sh`, `package.json` unchanged |
| Workflow state | HEAD and `origin/main` still `script: |` at line 90; no workflow change |
| Wrapper present | `scripts/finalizer-e2e.sh` `bash -n` OK, complete golden-path logic; `e2e-android.js` `node --check` OK |
| Regression sweep V3-01..V3-07 | 431 tests green; ledgers, invariants, sync/auth, privacy, design system, no chrono/premium/warm-V2 |
| Cost gates | 10 gates PASS (50k ≤5 reads, tab-switch 0, 3-delta sync, bounded writes, 10k labels cache) |
| Prohibited patterns | None introduced |

## Next task (Builder scope)

Repair **F1 only** on the preserved WIP baseline:

1. Align `package.json`/`package-lock.json` so `npx expo install --check` passes (expected: `expo ~57.0.24`, `expo-constants ~57.0.19`, `expo-router ~57.0.22`, `expo-sharing ~57.0.21`, `@expo/metro-runtime ~57.0.16`; use `npx expo install --fix` and commit the lockfile).
2. Keep the preserved WIP delta (`v3-08-finalizer-wrapper.test.ts`) intact.
3. Re-run trusted verification and confirm exit 0 through `expo export` (android + ios), `prebuild`, and `assembleRelease` producing `app-release.apk`.

F2 (workflow `script: bash scripts/finalizer-e2e.sh` on `main`) remains trusted-shell-owned and is required before the finalizer can attest APK install/launch/golden-path. V3-08 final completion belongs to the trusted release finalizer after privacy/cost/APK/install/golden-path evidence.

## State

| Item | Status |
|---|---|
| V3-01..V3-07 | complete — no regression (431 tests, privacy, cost, ledgers, invariants all green) |
| V3-08 | in_progress — F1 Builder-fixable (expo versions); F2 trusted-shell-only (workflow `script:`) |
| Trusted verification | exit 1 (mustFix) |
| Candidate delta | repair baseline preserved (wrapper contract test added; no workflow/APK change) |