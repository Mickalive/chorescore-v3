# Director RUN 35325991233 — V3-08 repair accepted, expo alignment green, finalizer handoff

## Summary

**Decision: `stop`** — V3-08 audit decision is `accept` with `hasDelta=true`. The Builder-fixable mustFix (F1, expo dependency alignment) is resolved and trusted verification now exits **0** with every product, privacy, cost, export, prebuild and native build gate green. V3-08 is the final roadmap criterion; all Builder-scoped work is accepted. The only remaining blocker (F2, workflow emulator `script: |`) is trusted-shell-owned per `AGENTS.md` and cannot be edited by the Builder. The factory Builder loop stops here; V3-08 final completion belongs to the trusted release finalizer.

## What happened

### Audit decision: accept

`reports/audits/RUN_35325991233.json` / `.md` record the independent Auditor decision **accept** for the V3-08 repair candidate. The candidate delta vs the pristine accepted tree is minimal and correct:

- `package.json` — 5 version bumps exactly matching the expected SDK 57 patches:
  - `expo ~57.0.19` → `~57.0.24`
  - `expo-constants ~57.0.16` → `~57.0.19`
  - `expo-router ~57.0.18` → `~57.0.22`
  - `expo-sharing ~57.0.17` → `~57.0.21`
  - `@expo/metro-runtime ~57.0.14` → `~57.0.16` (devDependencies)
- `package-lock.json` — 74 lines updated correspondingly (resolved versions, integrity).
- `src/`, `scripts/`, `__tests__/`, `docs/` — byte-identical to pristine (auditor `diff -rq` confirmed; only ephemeral build output differs).
- WIP preserved: `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (wrapper contract), `scripts/finalizer-e2e.sh` (1155 bytes, `bash -n` OK), `scripts/e2e-android.js` (`node --check` OK).

### Trusted verification: exit 0

`reports/audits/TRUSTED_35325991233.md` shows the full green path (previously blocked at exit 1 in cycle 35324430975):

- `npm run typecheck` — PASS
- `npm run test` — 22 suites / **431 tests PASS** (all V3-01..V3-08 suites, including `v3-08-finalizer-wrapper.test.ts`)
- `privacy:check` — PASS (71 tests: taxonomy 3.0.0 deterministic, classification cache, pipeline, privacy gate, consent, query budget, DP, buyer contracts, audit log)
- `cost:check` — PASS (21 tests, 10 gates: 50k entries ≤5 reads, tab-switch 0 reads, 3-delta sync incremental, bounded writes, 10k labels → 1 classification, privacy gate, budgets, offline, golden path, WCAG AA)
- `npx expo install --check` — **PASS**: `Dependencies are up to date` (was `Found outdated dependencies` in 35324430975)
- `expo export` — PASS: Android bundled 18136 ms, 1318 modules, 3.2 MB hbc, exported to `/home/runner/work/_temp/v3-audit-export`
- `expo prebuild --platform android --no-install` — PASS
- Gradle — **BUILD SUCCESSFUL in 19m26s, 395 actionable tasks: 395 executed** (`:app:assembleDebug` in the audit's verbose run; the trusted finalizer's release job runs `:app:assembleRelease` — both prove the native build chain works)

Independent re-checks by the Auditor (`npx expo install --check`, `npm run typecheck`, `npm run test`) also exit 0.

## Must-fix findings

### F1 (resolved) — expo dependency alignment

- **Path:** `package.json` / `package-lock.json`
- **Problem:** Previously `npx expo install --check` exited 1 with outdated dependencies, blocking trusted verification before export/prebuild/build (no APK possible).
- **Evidence:** Diff pristine→candidate shows the 5 bumps to the exact expected versions; `npx expo install --check` now PASS; TRUSTED log reaches export/prebuild/BUILD SUCCESSFUL and ends `Trusted verification exit code: 0`.
- **Required fix:** None — already fixed via `npx expo install --fix` and committed.
- **Verification:** Trusted verification exit 0 with all gates; independent `expo install --check` exit 0.

### F2 (informational, trusted-shell-only) — finalizer emulator step still multi-line

- **Path:** `.github/workflows/chorescore-v3-finalize.yml` (emulator step, lines 90-94)
- **Problem:** Still `script: |` with `set -euo pipefail` etc. `reactivecircus/android-emulator-runner@v2` line-splits the block and executes each line via `sh -c`; dash rejects `pipefail` (exit 2) before APK install/golden path. Same root cause as finalizer runs 35293489693, 35302658175, 35315519533.
- **Evidence:** HEAD workflow confirmed still `script: |`; `scripts/finalizer-e2e.sh` exists (single bash process, complete golden-path logic) but is not yet invoked; `AGENTS.md`/`TASKS.json` explicitly assign this fix to the trusted shell.
- **Required fix:** Trusted shell on `main`: replace the emulator step's multi-line `script: |` block with `script: bash scripts/finalizer-e2e.sh`, then re-run the trusted finalizer. No product code change needed.
- **Verification:** Finalizer passes the Android API 35 emulator step (release APK install, launch without Metro, golden path `Ajouter`/`Balances`/`A faire` with the demo fixture) with exit 0 and attests `android-release-apk-sha256-<sha>` + `android-api35-install-launch-e2e-green`.

## Independent checks

| Check | Result |
|---|---|
| Audit decision | accept (hasDelta=true) |
| Trusted verification | **exit 0** — typecheck, 431 tests, privacy 71, cost 21, expo check up-to-date, export, prebuild, Gradle BUILD SUCCESSFUL |
| Candidate delta vs pristine | only `package.json` + `package-lock.json`; no src/scripts/tests drift |
| Package versions | 5/5 match expected `~57.0.24` / `~57.0.19` / `~57.0.22` / `~57.0.21` / `~57.0.16` |
| WIP preservation | wrapper test passes; `finalizer-e2e.sh` `bash -n` OK; `e2e-android.js` `node --check` OK |
| Regression sweep V3-01..V3-07 | 431 tests green — ledgers zero-sum per currency, integer minor units, immutable rate snapshot, Minutes/Points distinct, materialized balances, sync/auth/delta, privacy deterministic cache, no chrono/premium/warm-V2 |
| Cost gates V3-08 | 10 gates PASS (50k bounded, tab 0, delta sync, 1-write, 10k cache, privacy gate, offline, golden path, WCAG AA) |
| Prohibited patterns | none introduced |
| Workflow state | `script: \|` still present — correctly deferred to trusted shell per AGENTS.md |

## Next step (trusted shell, not Builder)

1. On `main`, replace the emulator step's multi-line `script: |` block with the single-line `script: bash scripts/finalizer-e2e.sh`.
2. Re-run the trusted finalizer.
3. The finalizer must attest: release APK built (`android-release-apk-sha256-<sha>`), installed and launched on the Android API 35 emulator without Metro, golden path `Ajouter`/`Balances`/`A faire` traversed (`android-api35-install-launch-e2e-green`), and iOS readiness verified.
4. Only then mark V3-08 `complete` in `docs/RELEASE_STATUS.json`.

No Builder rebuild is needed. Product-side readiness is complete: all eight roadmap criteria (V3-01..V3-08) are auditor-accepted and trusted product/privacy/cost/export/prebuild/build checks are green.

## State

| Item | Status |
|---|---|
| V3-01 .. V3-07 | complete — no regression |
| V3-08 (product/config) | accept — expo alignment fixed, trusted verification exit 0, delta minimal |
| Trusted verification | exit 0 |
| Candidate delta | accept — preserved WIP, only version bumps |
| Factory Builder loop | stop — remaining work is trusted-shell-owned (workflow one-liner + finalizer re-run) |
| V3-08 final completion | trusted release finalizer, after APK/install/golden-path/iOS-readiness evidence |