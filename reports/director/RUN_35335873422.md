# Director RUN 35335873422 — V3-08 repair: finalizer failure NOT repaired, lockfile-only delta, trusted shell handoff

## Summary

**Decision: `stop`** — V3-08 audit decision is `repair`. The candidate delta is `package-lock.json` only (6 transitive dependency bumps from `npm install`); no product, privacy, cost, APK, runtime, E2E or readiness code changed. The trusted finalizer failure from Actions run 35333103534 is **NOT repaired** by this candidate. The emulator step in `.github/workflows/chorescore-v3-finalize.yml` still uses a multi-line `script: |` block which `reactivecircus/android-emulator-runner@v2` line-splits into `sh -c` (dash); dash rejects `set -euo pipefail` (exit 2) before the APK install/golden path runs. The candidate is coherent (trusted verification exit 0, 431 tests, no regression) and retained as WIP baseline, but the mustFix remains. V3-08 is the final roadmap criterion; the Builder cannot edit workflows. The factory Builder loop stops here.

## What happened

### Audit decision: repair

`reports/audits/RUN_35335873422.json` / `.md` record the independent Auditor decision **repair** for the V3-08 repair candidate. The candidate delta vs the pristine accepted tree is minimal but does not address the failure:

- `package-lock.json` — 6 transitive dependency bumps from `npm install`:
  - `@types/node` 22.20.3 → 26.6.1 (major)
  - `undici-types` 6.21.0 → 8.9.0
  - `baseline-browser-mapping` 2.11.24 → 2.11.25
  - `electron-to-chromium` 1.5.430 → 1.5.431
  - `is-core-module` 2.16.2 → 2.17.0
  - `node-releases` 2.0.55 → 2.0.56
- `src/`, `scripts/`, `__tests__/`, `app/`, `package.json` — byte-identical to pristine (`diff -rq` confirmed).
- No change to `.github/workflows/` (protected control-plane; Builder cannot edit).
- WIP preserved: `scripts/finalizer-e2e.sh` (bash -n OK), `scripts/e2e-android.js` (node --check OK), `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (passes).

### Trusted verification: exit 0

`reports/audits/TRUSTED_35335873422.md` shows the full green path:

- `npm run typecheck` — PASS
- `npm run test` — 22 suites / **431 tests PASS**
- `privacy:check` — PASS (71 tests)
- `cost:check` — PASS (21 tests, 10 gates)
- `npx expo install --check` — **PASS**: `Dependencies are up to date`
- `expo export` — PASS: Android bundled 19495 ms, 1318 modules, 3.2 MB hbc
- `expo prebuild --platform android --no-install` — PASS
- Gradle — **BUILD SUCCESSFUL in 20m43s, 395 actionable tasks**

### Why this is repair, not accept

Per governance: the trusted finalizer failure from Actions run 35333103534 is NOT repaired. The candidate changes only `package-lock.json` and does not touch the workflow or the e2e scripts. The emulator step's root cause (documented in `reports/director/RUN_35325991233.md` F2) persists unchanged on `main` and `lab`.

## Must-fix findings

### F1 (mustFix: true) — Trusted finalizer failure NOT repaired by this candidate

- **Path:** `.github/workflows/chorescore-v3-finalize.yml` (emulator step, lines 90-94)
- **Problem:** Finalizer run 35333103534 failed at step 7 "Android API 35 install, launch and golden path". All product/privacy/cost gates, iOS export, release APK build and APK hashing succeeded (steps 1-6). The emulator step's multi-line `script: |` block is line-split by `android-emulator-runner@v2` into `sh -c` (dash) invocations that reject `set -euo pipefail` (exit 2) before the APK install/golden path runs. Same root cause as finalizer runs 35293489693, 35302658175, 35315519533. The workflow on `main` and `lab` still contains the multi-line block; the candidate does not touch it.
- **Evidence:** GitHub API run 35333103534 jobs (step 7 failure, steps 1-6 success); `git show origin/main:.github/workflows/chorescore-v3-finalize.yml` still `script: |`; candidate.patch contains only package-lock.json; `diff -rq` shows src/scripts/tests/app/package.json identical to pristine; `scripts/finalizer-e2e.sh` exists but is not invoked by the workflow.
- **Required fix:** Trusted shell (sole owner of `.github/workflows/` per AGENTS.md): on `main`, replace the emulator step's multi-line `script: |` block with `script: bash scripts/finalizer-e2e.sh`, then re-run the trusted finalizer.
- **Verification:** Finalizer passes the Android API 35 emulator step (release APK install, launch without Metro, golden path Ajouter/Balances/A faire via `scripts/e2e-android.js`) with exit 0 and attests `android-release-apk-sha256-<sha>` + `android-api35-install-launch-e2e-green`.

### F2 (informational) — Candidate delta is lockfile-only churn

- **Path:** `package-lock.json`
- **Problem:** The only product-tree change is 6 transitive dependency bumps from `npm install` (including a major `@types/node` 22→26 bump). These do not address the finalizer failure and add unnecessary churn. Harmless and verified green; retained as WIP baseline per repair semantics.
- **Required fix:** None for the candidate itself. The next step must focus on the actual blocker (trusted-shell workflow fix), not lockfile churn.

## Independent checks

| Check | Result |
|---|---|
| Audit decision | repair (hasDelta=true) |
| Trusted verification | **exit 0** — typecheck, 431 tests, privacy 71, cost 21, expo check up-to-date, export, prebuild, Gradle BUILD SUCCESSFUL |
| Failed finalizer run 35333103534 | steps 1-6 success; **step 7 (emulator) failure**; reopen-on-failure success |
| Workflow root cause | `script: \|` multi-line still present on main and lab — unchanged |
| Candidate delta vs pristine | only `package-lock.json`; src/scripts/tests/app/package.json identical |
| Lockfile consistency | `npm ci --dry-run` exit 0; trusted verification `npm ci` (862 packages) OK |
| WIP preservation | `finalizer-e2e.sh` bash -n OK; `e2e-android.js` node --check OK; wrapper test passes |
| Regression sweep V3-01..V3-07 | 431 tests PASS — zero-sum ledgers, integer minor units, immutable rate snapshot, Minutes/Points distinct, materialized balances, revision/tombstone, sync/auth/delta, privacy pipeline |
| Cost gates V3-08 | 21 tests / 10 gates PASS (50k bounded, tab 0, delta sync, 1-write, 10k cache, privacy gate, offline, golden path, WCAG AA) |
| Prohibited patterns | none introduced (lockfile-only delta) |
| Builder scope boundary | workflow fix is trusted-shell-owned; no Builder cycle can deliver it |

## Next step (trusted shell, not Builder)

1. On `main`, replace the emulator step's multi-line `script: |` block with `script: bash scripts/finalizer-e2e.sh`.
2. Re-run the trusted finalizer.
3. The finalizer must attest: release APK built (`android-release-apk-sha256-<sha>`), installed and launched on the Android API 35 emulator without Metro, golden path `Ajouter`/`Balances`/`A faire` traversed (`android-api35-install-launch-e2e-green`), and iOS readiness verified.
4. Only then mark V3-08 `complete` in `docs/RELEASE_STATUS.json`.

No Builder rebuild is needed. All Builder-scoped work is auditor-accepted. The release blocker is exclusively the trusted-shell-owned workflow emulator step one-liner change (`script: bash scripts/finalizer-e2e.sh`).

## State

| Item | Status |
|---|---|
| V3-01 .. V3-07 | complete — no regression |
| V3-08 (product/config) | repair — candidate coherent and green, but finalizer failure not repaired; mustFix remains |
| Trusted verification | exit 0 |
| Candidate delta | repair — lockfile-only, retained as WIP baseline |
| Factory Builder loop | stop — remaining work is trusted-shell-owned (workflow one-liner + finalizer re-run) |
| V3-08 final completion | trusted release finalizer, after APK/install/golden-path/iOS-readiness evidence |

## Roadmap position

All eight roadmap criteria (V3-01 through V3-08) have been accepted by the auditor. V3-08 is the final criterion. Once the trusted shell applies the workflow change and the finalizer confirms — product/privacy/cost gates pass, APK builds/installs/launches, golden path traverses successfully on device, and iOS readiness is verified — V3-08 will be marked `complete` and the V3 release candidate is ready for final promotion from `lab/chorescore-v3` to `main`.
