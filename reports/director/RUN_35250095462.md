# Director RUN_35250095462 — V3-06 Accept

**Cycle:** 35250095462
**Criterion:** V3-06 — Groupes, invitations, identité, sync et backend frugal
**Decision:** **accept → continue**
**Next criterion:** V3-07 — Data product frugal

## Summary

The audit decided `accept` on V3-06. The repair delta (3 files, +46/−18) resolved all three must-fix findings from the previous cycle. Trusted verification passed with exit code 0: `tsc --noEmit` exits 0, `npx jest` passes 18 suites / 320 tests, and the Android export + prebuild + native debug build succeeds (`BUILD SUCCESSFUL in 19m 28s`). Independent re-runs confirm the same. All V3-01 through V3-05 regression suites pass (16 suites, 236 tests). No must-fix finding remains.

## What was repaired and accepted

### Finding 1: Missing import — RESOLVED

`ScopedContributionRepository` and `ScopedExpenseRepository` are re-imported at lines 22–23 of `v3-06-repair-sync-authorization.test.ts`. All 7 instantiations in the "authorization enforced through repository path" describe block resolve. `tsc` exits 0 and the 7 previously-failing authorization tests pass.

### Finding 2: Failure-injection prototype chain — RESOLVED

The failing repo is built with `Object.create(originalContributions)` preserving the prototype chain (`getById`, `getByHousehold`, …). Only `seed`/`create` are overridden to throw `Simulated materialization failure`. The production `MaterializingSyncState.applyDeltas` path now reaches the intended error. The business-table snapshot was moved after the valid pull for correctness. Assertions kept: error message contains intended text, cursor ≤ 1, all 7 business tables unchanged.

### Finding 3: SQLite factory-wiring test — RESOLVED

`MaterializingSyncState` is exported from `RepositoryFactory.ts` (export keyword only). The test wraps `baseSyncState` exactly as the factory does and passes the wrapped state to `pullDeltas`/`pushDeltas`. The remote delta now materializes into the business table (`entry.label === 'Remote via factory'`, `entry.value === 30`). Local write → `getDirtyRecords` returns the payload; `pushDeltas` sends it.

## V3-06 acceptance criteria — all verified present

- `applyDeltas` materializes pulled records into all 8 business collections inside `withTransaction`
- `storeLocalRecords` has callers; `getDirtyRecords` returns real payloads; `pushDeltas` sends actual local writes
- Group-options UI: Minutes|Points toggle with provenance warning, compensation toggle + rate
- Invitation UI: share sheet, `/join/:token` deep-link route, atomic membership + member acceptance
- Authorization rules enforced: tenant isolation, deterministic conflict resolution replacing blind LWW
- Hostile tests: cross-tenant blocked, balance falsification rejected, concurrent same-id resolved deterministically
- End-to-end sync tests: remote delta materializes; local write appears in getDirtyRecords and is pushed
- No N+1 or listener massif; cost instrumentation budgets sync-delta and tab-switch
- No regression V3-01 through V3-05

## Non-blocking observations (carried forward unchanged)

- `rawRepos` on AppContext exposed to every screen; only join flow consumes it
- `InMemoryInvitationRepository` not snapshotted in `withTransaction`
- `ScopedInvitationRepository.getPendingByEmail` unscoped (no app caller today)

## Next task: V3-07 — Data product frugal

The roadmap defines V3-07 as:

> Migrer et étendre `src/analytics` V2 : consent, taxonomy/classifier downstream versionné, privacy pipeline, query budget, DP, release gate, buyer contracts, audit log. Étendre aux contributions, dépenses, settlements et usage sans exporter texte libre ni identifiants opérationnels. Pipeline asynchrone, incrémentale/checkpointée et batchée ; cache de classification ; règles/classifieur léger avant IA ; aucun LLM synchrone ; aucune double collecte inutile ; Research Facts minimisés ; rétention/reprocessing contrôlés.

V3-07 must deliver the frugal data pipeline obligations from the roadmap rather than treating them as optional polish. Key objectives:

1. **Migrate V2 analytics foundation** — consent policy, taxonomy, pipeline, differential privacy, query budget, release gate, buyer contracts, audit log
2. **Extend event schema** to cover contributions, expenses, settlements, and product usage
3. **Classification pipeline** — downstream semantic classification versionned, taxonomyVersion/classifierVersion tracked, confidence scored
4. **Privacy transform** — separate from classification; strip text, operational IDs, names, emails, device IDs before Research Analytics Store
5. **Incremental async pipeline** — checkpoint/revision-based extraction, batch processing, no full reprocessing without explicit trigger
6. **Classification cache** — normalize + lang + taxonomyVersion + classifierVersion key; never reclassify identical labels
7. **No synchronous LLM** on user path; lightweight classifier first; unknown/ambiguous queued for batch
8. **Research Facts minimized** — no operational IDs, no free text, no re-identifiable history
9. **Cost regression gate** — no action should trigger proportional-to-history classification or scanning

## Preserved

V3-01 through V3-06 must not regress. The two independent ledgers, explicit-only cross-ledger compensation, 100% free model, no chrono, adult premium visual direction, historical unit semantics, privacy plane separation, local-first/delta-only architecture, bounded network cost, incremental analytics/classification cache, and all already-completed criteria remain in force.
