# Director Report — Cycle 35460432762

**Criterion:** V3-08 — Finition, coût et release (final criterion)
**Audit Decision:** accept
**Director Decision:** stop

## Summary

V3-08 audit is accepted. The candidate is a non-empty delta (changedFiles=2, hasDelta=true, verificationOnly=false, agentExitCode=0) repairing the trusted finalizer step-7 blocker of run 35457974999 within Builder scope: `scripts/e2e-android.js` and `scripts/finalizer-e2e.sh` only.

## Root Cause

The adb SERVER PROCESS was degraded after React Native Hermes cold start. The previous repair (cycle 35453691894) applied a fail-fast dump strategy (45s timeout, 1 attempt) and raised the Demarrer window to 300s, but with a degraded server each failed dumpUi cycle costs ~110s, so ~2.7 cycles exhaust the 300s window. The previous `adb reconnect` only resets the transport layer and cannot recover a degraded server process.

## Repair

Replace `adb reconnect` with `adb kill-server` + `adb start-server` (full server process restart, which automatically rediscovers the running emulator via its broadcast channel) plus a post-restart `adb get-state` health check, in the JS `adbReconnect()` and in both shell restart points (pre-E2E and post-install). All new waits bounded (kill 10s, start 10s, wait-for-device 30s, get-state 5s in JS; shell matches pristine pattern with `|| true` guards). No E2E assertion or timeout changed. No product code changed. No protected control-plane file touched.

## Verification

- Trusted verification passed exit 0: typecheck, 432 tests (22 suites), privacy 71 tests, cost 21 tests (10 gates), `expo install --check` up to date, `expo export android`, prebuild, `assembleDebug BUILD SUCCESSFUL`
- Regression spot check391 tests pass (19 suites)
- All V3-01 through V3-07 remain accepted with zero regression
- No chrono/premium/paywall/warm-V2 reintroduction
- No cost regression (CI-only changes)

## Next Step

V3-08 is the final criterion. The next trusted finalizer run will empirically confirm the fix: if step 7 passes, V3-08 is complete and the release is ready for final promotion by the trusted release finalizer.

No further Builder work is required after the final criterion is accepted. The trusted finalizer owns the final V3-08 completion.
