# Director RUN 35310569665 — V3-08 Builder repair accepted, trusted shell workflow change pending

## Summary

**Decision: `continue`** (V3-08 Builder-scoped repair accepted; trusted shell must apply workflow change and re-run finalizer)

The auditor accepted the Builder-scoped half of the two-part fix mandated by the open findings. The Builder created `scripts/finalizer-e2e.sh` — a 35-line bash script that runs the complete golden-path logic (set -euo pipefail, APK auto-location, adb install, npm run e2e:android) in a **single bash process**. This eliminates the root cause of the trusted finalizer failure: the `reactivecircus/android-emulator-runner@v2` action splits multi-line `script: |` blocks into separate `sh -c` invocations, where dash rejects `pipefail` (exit 2) and shell-local variables are lost between lines.

The remaining blocker is the trusted-shell-owned workflow change. The Builder is forbidden from modifying workflows (AGENTS.md). The trusted shell must update `.github/workflows/chorescore-v3-finalize.yml` and re-run the finalizer.

## What happened

### Previous cycles
- Cycle 35295951364: Auditor accepted E2E hardening (transient dump failure resilience). Trusted finalizer ran and failed at step 7 (exit code 2).
- Cycle 35304642494: Root cause diagnosed — workflow's multi-line `script: |` block is split line-by-line by the action; `set -euo pipefail` fails in dash with exit 2. E2E hardening preserved as WIP baseline.

### This cycle (35310569665)
- Builder created `scripts/finalizer-e2e.sh` (1 new file, 35 lines).
- Content verified line-by-line against the required logic — all four mandatory statements present in a single bash process.
- Bash syntax check passes. Root-cause reproduction confirms `sh -c 'set -euo pipefail'` → exit 2 (dash), `bash -c 'set -euo pipefail'` → exit 0.
- E2E compatibility verified: `npm run e2e:android` invokes the hardened `scripts/e2e-android.js` which auto-locates the APK and skips install when the package is present (idempotent wrapper).
- Trusted verification passed: typecheck clean, 426 tests, privacy:check PASS, cost:check PASS, export OK, prebuild OK, assembleDebug BUILD SUCCESSFUL.
- No product code touched. V3-01..V3-07 regression impossible.
- Auditor decision: **accept**.

## What the trusted shell must do

1. **Update the workflow**: change the emulator step in `.github/workflows/chorescore-v3-finalize.yml` from the multi-line `script: |` block to:
   ```yaml
   script: bash scripts/finalizer-e2e.sh
   ```
2. **Re-run the trusted finalizer** (Actions run).
3. The finalizer step 7 must:
   - `bash scripts/finalizer-e2e.sh` executes in a single process.
   - `adb install -r "$apk"` installs the release APK on the API 35 emulator.
   - `npm run e2e:android` runs the hardened E2E script, traversing: login, groups, tabs, add, balances, todos, back-to-add.
   - All golden-path checkpoints pass. Exit code 0.
4. **Mark V3-08 as `complete`** in `docs/RELEASE_STATUS.json` only after the finalizer confirms all gates (product, privacy, cost, APK install, golden path).

## State

| Item | Status |
|---|---|
| V3-01 | complete |
| V3-02 | complete |
| V3-03 | complete |
| V3-04 | complete |
| V3-05 | complete |
| V3-06 | complete |
| V3-07 | complete |
| V3-08 | in_progress — Builder repair accepted; trusted shell applies workflow change and runs finalizer |

## Audit evidence

| Check | Result |
|---|---|
| Trusted verification | exit 0 — typecheck, 426 tests, privacy:check, cost:check, export, prebuild, assembleDebug |
| Candidate delta | 1 new file — `scripts/finalizer-e2e.sh` (35 lines) |
| Product code touched | None — `src/`, `app/`, `__tests__/` byte-identical to pristine |
| Bash syntax | `bash -n` exits 0 |
| Root-cause reproduction | `sh -c 'set -euo pipefail'` → exit 2 (dash); `bash -c 'set -euo pipefail'` → exit 0 |
| APK guard simulation | Empty dir → error exit 1; present APK → proceeds; 0-byte → guard triggers |
| E2E compatibility | `e2e:android` → `node scripts/e2e-android.js` (auto-locates APK, skips install when present) |
| Workflow scope | `.github/workflows/` untouched by the candidate (trusted-shell owned) |
| Prohibited patterns | None |

## Roadmap position

All eight roadmap criteria (V3-01 through V3-08) have been accepted by the auditor. V3-08 is the final criterion. Once the trusted shell applies the workflow change and the finalizer confirms — product/privacy/cost gates pass, APK builds/installs/launches, golden path traverses successfully on device, and iOS readiness is verified — V3-08 will be marked `complete` and the V3 release candidate is ready for final promotion from `lab/chorescore-v3` to `main`.
