# Director RUN 35487229476 — V3-08 accept, no next criterion

## Decision: `stop`

## 1. Summary

V3-08 audit decision is **accept**. The candidate is a non-empty delta (`changedFiles=2`, `hasDelta=true`) repairing the trusted finalizer step-7 blocker of Actions run 35485115705 within Builder scope: `scripts/e2e-android.js` and `__tests__/domain/v3-08-finalizer-wrapper.test.ts` only.

## 2. What this cycle addressed

The previous trusted finalizer run (35485115705) failed at step 7 (Android emulator install/launch/golden path). The failure chain was documented across prior cycles: uiautomator dumps take 30–60s each on API 35 x86_64, exhausting `waitFor` windows. The previous cycle's 2s-TTL dump cache was explicitly identified as **inert** (TTL < dump duration, so every `findNodes` call triggered a fresh 30–60s dump).

This candidate repairs exactly that mechanism:

1. **Dump cache TTL 2s → 30s**: same-screen rapid calls (assertAbsent batches, screenshot + findVisible, sequential waitFor) now share one dump instead of each triggering a fresh 30–60s dump.
2. **Cache invalidation on `tapNode()` and `swipeUp()`**: every golden-path screen transition invalidates, so post-transition calls always get fresh state.
3. **Dumpsys pre-check 12 → 3 iterations**: saves up to 45s of dead time before the Demarrer window.
4. **All golden-path `waitFor` windows ≥ 60s**: Demarrer 600s; all other transitions 120s; each window fits 1–2 full dump attempts.

Three new static contract tests guard the repair: TTL ≥ 15s with tap/swipe invalidation, all golden-path waitFor timeouts ≥ 60s, dumpsys pre-check bounded to ≤ 20s.

## 3. Regression of accepted criteria

All previously accepted criteria remain green with zero regression:

- **V3-01** domain/ledgers: PASS (zero-sum, integer rounding, rate snapshot, no silent conversion).
- **V3-02** structure/design: PASS (no chrono/premium/warm-V2, factual copy, WCAG AA).
- **V3-03** Ajouter/history: PASS.
- **V3-04** Balances: PASS.
- **V3-05** À faire: PASS.
- **V3-06** groups/sync: PASS.
- **V3-07** data product: PASS (71 tests).
- **V3-08** cost gates: PASS (all 10 gates, 21 tests); wrapper contract 9/9 PASS.

No chrono/premium/paywall/warm-V2 reintroduction. No cost regression (CI-script-only delta; no user-path reads/writes/listeners/network affected).

## 4. Why `stop`

V3-08 is the **final criterion on the roadmap**. There is no next roadmap step. The only remaining action is the **empirical confirmation by the trusted release finalizer**: if the next finalizer run passes step 7 (Android API 35 install, launch and golden path), V3-08 is complete and the release is ready for final promotion. The trusted shell alone can mark V3-08 `complete` after that confirmation.

## 5. Informational findings (all `mustFix=false`)

1. **`typeInto`/`back`/`swipeToTop` do not invalidate the dump cache**: defined but never called in the golden path. If a future golden-path step adds typing/back/swipeToTop, those functions must invalidate `_dumpCache`.
2. **Exact step-7 logs of run 35485115705 not directly accessible**: no GitHub token in this environment; diagnosis reconstructed from the documented failure chain and code-level analysis. The Builder's diagnosis is technically coherent.
3. **Empirical confirmation pending**: the repo alone cannot prove the next finalizer run will pass; the trusted finalizer is the empirical gate.

## 6. Next action (for trusted shell)

The trusted release finalizer must:

1. Run the finalizer workflow against the latest accepted baseline.
2. If step 7 passes: mark V3-08 `complete` in `RELEASE_STATUS.json`, close the open finding, and promote the release.
3. If step 7 fails again: the surviving diagnostic evidence (logcat-wait-*, dumpsys-wait-*, pidof, result.json echoed to step logs) will identify the concrete cause for the next repair cycle.
