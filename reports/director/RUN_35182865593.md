# Director RUN_35182865593 — V3-04 Accepted

**Cycle:** 35182865593
**Criterion:** V3-04 — Balances, périodes, vues matérialisées et compensation explicite
**Decision:** **accept → continue**
**Next criterion:** V3-05 — À faire

## Summary

V3-04 is accepted. Both prior must-fix findings are genuinely resolved:

1. **Currency in expense modification detection** — `app/(tabs)/balances.tsx` now compares `oldEntry.currency !== newEntry.currency` alongside all other ledger-affecting fields. Three new tests prove currency-only edit (CHF→EUR) triggers remove-old + apply-new, snapshot equals full replay, and zero-sum holds per currency. Mutation probe confirms the tests guard the detection logic.

2. **Claim #8 reworded** — Header changed from "Screen-level" to "Data-level: delta handler re-reads only the changed collection, proving bounded repo calls". Tests substantiate with repository read counts (contributions=3, expenses=1, settlements=1 after initial load + two contribution signals). No "Screen-level" wording remains.

**Trusted verification:** exit 0 — typecheck, 180 tests green, Expo export, prebuild, native debug build BUILD SUCCESSFUL.

**Delta scope:** Exactly 2 files vs pristine — no governance/directives/state/report files touched by the builder.

## Next task: V3-05 — À faire

Migrate TodoScreen free. Atomic completion toward exactly one ContributionEntry. Reminders/calendar via honest ports. No chrono. Completion must remain coherent in local/offline and integrate cleanly with the sync queue.

### Key requirements from roadmap and constitution:

- Migrate `TodoScreen.tsx` from V2 as direct evolution
- Preserve: title, assignee, beneficiaries, due date, reminder, note, PersistentTask, creation, deletion, completion via mini-formula
- When a task is completed: confirm `Fait par`, confirm value, confirm `Fait pour`, create atomically exactly one ContributionEntry, update ledger
- Group in Minutes → ask value in minutes; group in Points → ask value in points
- No chrono whatsoever
- Default value can come from PersistentTask
- Completion must be atomic and transactional in local-first architecture
- Integrate with data-change signal so Balances reflects completed tasks
- All todo functionality is free — no paywall, no restrictions
- Preserve existing test patterns and adapt TodoScreen tests

### Acceptance criteria (from TASKS.json to be updated):

- TodoScreen migrated with V3 navigation (within "À faire" tab)
- Completion creates exactly one ContributionEntry atomically
- Unit-aware: asks minutes or points based on group setting
- No chrono anywhere in the todo flow
- Completion integrates with data-change signal
- Existing todo tests preserved and adapted
- No regressions on V3-01, V3-02, V3-03, V3-04
- Cost profile: no action proportional to total history
