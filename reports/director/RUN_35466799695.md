# Director RUN 35466799695 — V3-08 accept, no next criterion

## Decision: `stop`

## 1. Summary

V3-08 audit decision is **accept**. The candidate is a non-empty delta (changedFiles=1, hasDelta=true, verificationOnly=false, agentExitCode=0) repairing the trusted finalizer step-7 blocker of run 35464672760 within Builder scope: `scripts/e2e-android.js` only.

## 2. What was repaired

The previous cycle's `adb kill-server` + `adb start-server` repair was in the baseline of run 35464672760 and the finalizer STILL failed. The remaining failure source is the **uiautomator server session not being initialized** after the adb server-process restart — the first real `dumpUi()` call blocks for the full 45s timeout while the server initializes, burning one of the limited retry cycles in the Demarrer `waitFor()` window.

Repair (two coherent facets targeting the same root cause):

- **(a) Uiautomator warm-up dump** — immediately after the adb server restart in `launch()`, a bounded `uiautomator dump` (45s timeout, 1 attempt) forces the server to initialize before the window begins. Non-fatal (WARN + 5s sleep fallback on failure); warm-up file cleaned up.
- **(b) Demarrer window headroom** — 300s → 360s (+60s), compensating the worst-case warm-up cost (+50s) with +10s net headroom. Timing verified: 360s window fits 4.8 failed-dump cycles (75s each) vs 4.0 with 300s.

## 3. Verification

- Trusted verification exit 0: typecheck, 432 tests (22 suites), privacy 71 tests, cost 21 tests (10 gates), `expo install --check`, `expo export android`, `prebuild`, `assembleDebug` BUILD SUCCESSFUL in 20m 25s.
- Candidate patch matches `git diff --cached` byte-for-byte.
- Finalizer wrapper + golden-path + cost-gates suites: 41/41 PASS.
- Regression spot-check: 391/391 PASS.
- Protected control-plane files byte-identical to pristine.
- No chrono/premium/paywall/warm-V2 reintroduction. No cost regression.

## 4. Regression of accepted criteria

No product code, domain, analytics, persistence or configuration was changed — only the CI E2E script. All previously accepted criteria remain green:

- **V3-01** domain/ledgers: PASS (zero-sum, integer rounding, rate snapshot, no silent conversion).
- **V3-02** structure/design: PASS (no chrono/premium/warm-V2, factual copy, WCAG AA).
- **V3-03** Ajouter/history: PASS.
- **V3-04** Balances: PASS.
- **V3-05** À faire: PASS.
- **V3-06** groups/sync: PASS.
- **V3-07** data product: PASS (71 tests).
- **V3-08** cost gates: PASS (all 10 gates).

## 5. Why `stop`

V3-08 is the **final criterion on the roadmap**. There is no next roadmap step. The only remaining action is the **empirical confirmation by the trusted release finalizer**: if the next finalizer run passes step 7 (Android API 35 install, launch and golden path), V3-08 is complete and the release is ready for final promotion. The trusted shell alone can mark V3-08 `complete` after that confirmation.

## 6. Next action (for trusted shell)

The trusted release finalizer must:

1. Run the finalizer workflow against the latest accepted baseline (HEAD `a46a20b`).
2. If step 7 passes: mark V3-08 `complete` in `RELEASE_STATUS.json`, close the open finding, and promote the release.
3. If step 7 fails again: the surviving diagnostic evidence (logcat-wait-*, dumpsys-wait-*, pidof, result.json echoed to step logs) will identify the concrete cause for the next repair cycle.
