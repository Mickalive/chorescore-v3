# Director RUN 35351917454 — V3-08 repair: no progress, trusted shell workflow fix still pending (6th consecutive cycle)

## Summary

**Decision: `stop`** — V3-08 audit decision is `repair`. The candidate delta is empty (no product code changes vs pristine); the only new files are the audit reports themselves. The single mustFix finding persists unchanged: `.github/workflows/chorescore-v3-finalize.yml` on `origin/main` still contains the broken multi-line `script: |` block at the emulator step (lines 90–94) instead of the single-line `script: bash scripts/finalizer-e2e.sh`. This is the same root cause documented across **6 consecutive finalizer failures** (runs 35293489693, 35302658175, 35315519533, 35333103534, and the current state). The Builder-scoped wrapper (`scripts/finalizer-e2e.sh`) exists, is valid bash, and is preserved. Trusted verification exits 0 with all product/privacy/cost/export/prebuild/build gates green and 431 tests passing. All V3-01 through V3-07 remain complete with zero regression. However, `.github/workflows/` is trusted-shell-owned per `AGENTS.md`; the Builder cannot edit it. No new Builder cycle can deliver the required one-line workflow fix. The factory Builder loop **must stop**. V3-08 final completion belongs exclusively to the trusted release finalizer.

## What happened

### Audit decision: repair

`reports/audits/RUN_35351917454.json` records the independent Auditor decision **repair** for V3-08. The candidate vs the pristine accepted tree is byte-identical in all product code:

- `src/`, `__tests__/`, `scripts/`, `app/`, `package.json`, `package-lock.json` — identical (auditor `diff -rq` confirmed).
- Only the new audit report files differ (ephemeral).
- WIP preserved: `scripts/finalizer-e2e.sh` (valid bash), `scripts/e2e-android.js` (valid node), `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (passes).

### Trusted verification: exit 0

The full green path confirmed:

- `npm run check` → typecheck + 22 suites / **431 tests PASS**
- `privacy:check` → 71 tests PASS
- `cost:check` → 21 tests / 10 gates PASS (50k bounded, tab 0, delta sync, 1-write, 10k cache, privacy gate, budgets, offline, golden path, WCAG AA)
- `npx expo install --check` → PASS (Dependencies are up to date)
- `expo export` → PASS (1318 modules, 3.2 MB hbc)
- `expo prebuild` → PASS
- Gradle → **BUILD SUCCESSFUL**

### Why this is repair, not accept

Per governance: the trusted finalizer failure persists because the workflow on `main` still contains the broken multi-line `script: |` block. The candidate changes nothing that addresses this finding. The `reactivecircus/android-emulator-runner@v2` action line-splits the multi-line block into `sh -c` (dash) invocations; dash rejects `set -euo pipefail` (exit 2) before the APK install/golden path runs. The documented one-line fix (`script: bash scripts/finalizer-e2e.sh`) was never applied by the trusted shell.

## Must-fix findings

### F1 (mustFix: true) — Workflow emulator step still broken, 6th consecutive failure

- **Path:** `.github/workflows/chorescore-v3-finalize.yml` (emulator step, lines 90–94)
- **Problem:** The trusted shell has not applied the documented one-line workflow fix on `origin/main`. The emulator step still contains a multi-line `script: |` block:
  ```yaml
  script: |
    set -euo pipefail
    apk=$(find android/app/build/outputs/apk/release -type f -name '*.apk' | head -1)
    adb install -r "$apk"
    npm run e2e:android
  ```
  `reactivecircus/android-emulator-runner@v2` line-splits this block and executes each line via `sh -c` (dash); dash rejects `set -euo pipefail` (exit 2) before the APK install/golden path runs. This is the same root cause as finalizer runs 35293489693, 35302658175, 35315519533, and 35333103534. The last commit touching this workflow on `main` is `8b245b2` (2026-09-16). No subsequent finalizer run exists.
- **Evidence:** `git show origin/main:.github/workflows/chorescore-v3-finalize.yml` still `script: |` with 4 indented lines; `scripts/finalizer-e2e.sh` exists but is not invoked; `diff -rq` confirms no product code changes in candidate; `RELEASE_STATUS.json` V3-08 still `in_progress` with same open finding; no `android-api35-install-launch-e2e-green` attestation.
- **Required fix:** Trusted shell (sole owner of `.github/workflows/` per `AGENTS.md`): on `main`, replace the emulator step's multi-line `script: |` block with `script: bash scripts/finalizer-e2e.sh`, commit to `main`, then re-run the trusted finalizer. No product code change is needed — the Builder-scoped wrapper is preserved.
- **Verification:** Trusted finalizer must pass the Android API 35 emulator step (release APK install, launch without Metro, golden path `Ajouter`/`Balances`/`A faire` via `scripts/e2e-android.js`) with exit 0, and `docs/RELEASE_STATUS.json` must contain `android-release-apk-sha256-<sha>` + `android-api35-install-launch-e2e-green` in V3-08 evidence.

## Independent checks

| Check | Result |
|---|---|
| Audit decision | repair (hasDelta=false) |
| Trusted verification | **exit 0** — typecheck, 431 tests, privacy 71, cost 21, expo check up-to-date, export, prebuild, Gradle BUILD SUCCESSFUL |
| Candidate delta vs pristine | empty — no product code changes; only new audit report files |
| WIP preservation | `finalizer-e2e.sh` valid bash; `e2e-android.js` valid node; wrapper test passes |
| Regression sweep V3-01..V3-07 | 431 tests PASS — zero-sum ledgers, integer minor units, immutable rate snapshot, Minutes/Points distinct, materialized balances, revision/tombstone, sync/auth/delta, privacy pipeline, classification cache |
| Cost gates V3-08 | 21 tests / 10 gates PASS |
| Prohibited patterns | none (empty delta) |
| Workflow root cause | `script: \|` still present on main — 6th consecutive failure from same root cause |
| Builder scope boundary | workflow fix is trusted-shell-owned; no Builder cycle can deliver it |

## Next step (trusted shell, not Builder)

1. On `main`, replace the emulator step's multi-line `script: |` block with the single-line `script: bash scripts/finalizer-e2e.sh`.
2. Commit to `main`.
3. Re-run the trusted finalizer.
4. The finalizer must attest: release APK built (`android-release-apk-sha256-<sha>`), installed and launched on the Android API 35 emulator without Metro, golden path `Ajouter`/`Balances`/`A faire` traversed (`android-api35-install-launch-e2e-green`), and iOS readiness verified.
5. Only then mark V3-08 `complete` in `docs/RELEASE_STATUS.json`.

No Builder rebuild is needed. All Builder-scoped work is auditor-accepted. The release blocker is exclusively the trusted-shell-owned workflow emulator step one-liner change (`script: bash scripts/finalizer-e2e.sh`).

## State

| Item | Status |
|---|---|
| V3-01 .. V3-07 | complete — no regression |
| V3-08 (product/config) | repair — candidate coherent and green, but finalizer failure not repaired; mustFix persists (6th cycle) |
| Trusted verification | exit 0 |
| Candidate delta | repair — empty, retained as WIP baseline |
| Factory Builder loop | **stop** — remaining work is trusted-shell-owned (workflow one-liner + finalizer re-run) |
| V3-08 final completion | trusted release finalizer, after APK/install/golden-path/iOS-readiness evidence |

## Roadmap position

All eight roadmap criteria (V3-01 through V3-08) have been accepted by the auditor. V3-08 is the final criterion. Once the trusted shell applies the workflow change and the finalizer confirms — product/privacy/cost gates pass, APK builds/installs/launches, golden path traverses successfully on device, and iOS readiness is verified — V3-08 will be marked `complete` and the V3 release candidate is ready for final promotion from `lab/chorescore-v3` to `main`.
