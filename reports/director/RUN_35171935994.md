# Director RUN_35171935994 — V3-04 Repair (cycle 2 of 2)

**Cycle:** 35171935994  
**Criterion:** V3-04 — Balances, periods, materialized views, compensation  
**Audit decision:** repair  
**Trusted verification:** exit 0 — typecheck, 162 tests, Expo export Android, native debug build BUILD SUCCESSFUL

## Progress since last cycle

The candidate progressed from 5 must-fix findings to 2. The following were resolved:

1. **Compenser modal implemented** — creditor selection, amount presets, `quoteCrossLedgerMoneyAmount`, Avant/Aprés preview for both ledgers, `validateSettlementPreconditions`, immutable `CrossLedgerSettlement` with `rateSnapshot` via `repos.settlements.create`, disabled when compensation is off or no rate.
2. **Filtered history section** — Tout / Contributions / Depenses / Compensations filters, `paginateActivityLog` with settlements included, compact rows, UI pagination.
3. **Settlements period-filtered** — both screen and materialized service filter settlements by `occurredAt`; tests prove out-of-period settlement excluded from period views, included in all-time.
4. **Cost-gate tests rewritten** — wall-clock assertions replaced by repository call counts; bounded reads for 10k entries.
5. **No regression** — V3-01/V3-02/V3-03 preserved, no chrono/premium/paywall reintroduced.

## Remaining must-fix findings

### 1. Screen refresh contract still full re-read + full replay
`useFocusEffect` → `loadData()` re-reads all five collections and rebuilds the snapshot via `createBalanceSnapshot` (full ledger replay) on every focus. The delta machinery is applied only to settlements created inside the screen. AppContext exposes no data-change signal. The required test that renders the Balances screen against a fake repo and proves delta-based refresh is missing.

**Fix:** Add a data-change signal to AppContext. Balances subscribes and applies deltas (deltaUpdateContribution, deltaUpdateContributionFromSettlement) to the materialized snapshot. UseFocusEffect re-fetches only when the signal indicates relevant changes. Add the required screen-level test with repository call counts.

### 2. Money ledger stale after settlement in all-time view
`handleCompenserConfirm` applies delta only to `snapshotRef.current.contribution`; `moneyByCurrency` is unchanged. The all-time Argent block shows stale balances after a settlement. Fake ids (`pending-<ts>`, `sett-<ts>`) never match the persisted entity.

**Fix:** Add `deltaUpdateMoneyFromSettlement` (creditor +money, counterparty -money). Apply it in `handleCompenserConfirm`. Use the real settlement entity from `repos.settlements.create`. Test that both contribution and money balances equal full replay after settlement.

## Verdict

**continue** — V3-04 is not yet accepted. Two focused must-fix findings remain. The candidate is a coherent WIP baseline preserved for the next repair cycle. No V3-01/V3-02/V3-03 regression.
