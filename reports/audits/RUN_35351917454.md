# Audit RUN 35351917454 — V3-08 (REPAIR)

## Decision: `repair`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — trusted shell (sole owner of `.github/workflows/` per AGENTS.md) must, on `main`, replace the emulator step's multi-line `script: |` block with the single-line `script: bash scripts/finalizer-e2e.sh`, then re-run the trusted finalizer. The Builder-scoped single-shell wrapper already exists and is preserved; no product code change is needed.
- Candidate checkout: current HEAD `34e25d6` (lab/chorescore-v3) — detached at `34e25d60a99cb43adf5fc0646399ea794c7b662b`.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Trusted verification: `reports/audits/TRUSTED_35351917454.md` — **exit code 0** — `BUILD SUCCESSFUL in 16m 1s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / 431 tests PASS (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests: 50k entries bounded reads, tab-switch 0 reloads, delta sync, bounded writes, classification cache, privacy gate, budgets, offline, golden path, accessibility).
- `expo export` Android + iOS, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL (395 tasks).
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `reports`, `.expo`, `node_modules`, `android/build`, `.opencode`) shows **no product code changes**:

- `src/`, `__tests__/`, `scripts/`, `app/`, `package.json`, `package-lock.json` — identical.
- Only build artifacts differ (`.expo/`, `android/build`, `.opencode/`, new TRUSTED report).

This matches the objective: no product code change is needed. Builder correctly did not edit `.github/workflows/` (trusted-shell owned per AGENTS.md).

## 4. Builder-scoped wrapper (preserved and valid)

- `scripts/finalizer-e2e.sh` exists; `bash -n` passes; contains `#!/usr/bin/env bash`, `set -euo pipefail`, APK locate under `android/app/build/outputs/apk/release`, empty-APK guards, `adb install -r "$apk"`, `npm run e2e:android` — all in one bash process.
- `scripts/e2e-android.js` exists; `node --check` passes; auto-locates release APK, installs only when absent, asserts three V3 tabs and demo fixture labels, asserts absence of chrono/premium/warm-V2 UI.
- `package.json` exposes `e2e:android = node scripts/e2e-android.js`.
- Guarded by `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (PASS in trusted run).

## 5. Regression of previously accepted V3 invariants

All previously accepted criteria remain green in trusted run; no new violations detected:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding (minor units), Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS — optimistic local writes, paginated history, no full-scan.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS — dual ledgers distinct, periods as views, materialized balances == ledger rebuild.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS — atomic exactly-one ContributionEntry, no chrono.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS — delta-only sync, no N+1, no blind last-write-wins, tenant isolation.
- **V3-07** data product: `v3-07-analytics-privacy` PASS — Operational Store / Research Analytics Plane separated, no operational IDs or free text in outputs, classification cache deterministic + versioned, no synchronous LLM, checkpointed incremental pipeline, consent/retention/query budget/DP/buyer contracts.
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates): 50k entries bounded ≤5 reads, tab-switch 0 reads, delta sync bounded, writes bounded (1 per contribution/expense), 10k identical labels → 1 classification, privacy gate clean, budgets enforced, offline fallback, golden path domain-level, accessibility.

Independent spot checks: no `Chrono`/`chrono` in `src/`/`app/`, no Premium/paywall/subscription/entitlement UX, no drift to warm V2 design.

## 6. MUST-FIX finding — NOT resolved (trusted-shell owned)

The prior cycle's must-fix finding (documented in `docs/RELEASE_STATUS.json` openFindings and `directives/TASKS.json`) remains open:

- `git show origin/main:.github/workflows/chorescore-v3-finalize.yml` lines 90-94 **still contain** the multi-line block:

  ```yaml
  script: |
    set -euo pipefail
    apk=$(find android/app/build/outputs/apk/release -type f -name '*.apk' | head -1)
    adb install -r "$apk"
    npm run e2e:android
  ```

- The last commit touching this workflow on `main` is `8b245b2` (2026-09-16) — the documented one-line fix was never applied.
- GitHub API: finalizer run `35333103534` → `conclusion=failure`, step 7 "Android API 35 install, launch and golden path" `conclusion=failure`; `reopen-on-failure` ran; **no subsequent finalizer run exists**.
- `docs/RELEASE_STATUS.json` still shows V3-08 `in_progress` with the same mustFix open finding; no `android-api35-install-launch-e2e-green` attestation.

Root cause is well-documented: `reactivecircus/android-emulator-runner@v2` line-splits multi-line `script:` and executes via `sh -c` (dash); dash rejects `set -euo pipefail` (exit 2) before APK install/golden path runs. The Builder-scoped fix (`bash scripts/finalizer-e2e.sh` as single bash process) exists and is preserved, but the trusted-shell commit on `main` to switch to `script: bash scripts/finalizer-e2e.sh` has not happened.

This is a trusted-shell action (`.github/workflows/` is trusted-shell owned per AGENTS.md); the Builder cannot and did not touch it. The Builder-scoped half is complete.

## 7. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- No mustFix remains: **not satisfied** — the workflow on `main` is still broken and the trusted finalizer has not been re-run to attestation.

Per the acceptance rule ("Accept iff trusted verification passed AND no mustFix remains"), the decision is **`repair`**: the candidate is a coherent, clean baseline (no product delta, all gates green) and must be retained as WIP; the next cycle must ensure the trusted shell applies the one-line workflow fix on `main` and re-runs the trusted finalizer.

## 8. Required next step (verbatim)

> Trusted shell (sole owner of .github/workflows/ per AGENTS.md): on main, replace the emulator step's multi-line 'script: |' block with the single-line 'script: bash scripts/finalizer-e2e.sh', commit to main, then re-run the trusted finalizer. No product code change is needed — the Builder-scoped wrapper is preserved.

Verification: Trusted finalizer must pass the Android API 35 emulator step (release APK install, launch without Metro, golden path Ajouter/Balances/A faire via scripts/e2e-android.js) with exit 0, and `docs/RELEASE_STATUS.json` must contain `android-release-apk-sha256-<sha>` and `android-api35-install-launch-e2e-green` in V3-08 evidence.
