# Audit RUN 35524260895 — V3-08 repair (trusted finalizer step-7 failure, run 35521604346)

**Decision: `accept`**

## 1. Summary

Candidate is a non-empty delta (`1 file`, `6 insertions / 1 deletion`, `hasDelta=true`, `verificationOnly=false`) repairing the audited blocker of the trusted finalizer failure: Actions run `35521604346` failed at step 7 "Android API 35 install, launch and golden path".

**Concrete blocker identified — golden-path wait window after tapping Demarrer.** The previous baseline waited for the `Appartement` group with a `60s` timeout. On a cold API 35 x86_64 emulator, the demo sign-in plus SQLite fixture seeding takes 30-60s and the first uiautomator dump adds another 30-60s, so a 60s window only fits one dump attempt and can expire before the groups list renders.

**Repair:** `scripts/e2e-android.js` changes `waitFor('Appartement', 60000)` to `waitFor('Appartement', 120000)` with a 5-line comment documenting the timing model. The 120s value matches the script's established pattern for every other post-launch element (`waitFor('Ajouter', 120000)`, `waitFor('Balances', 120000)`, `waitFor('A faire', 120000)`) and the documented "120s: single dump takes 30-60s on slow emulators" comment. No product code, domain logic, analytics, persistence, sync or governance touched; no new adb calls; no network or history-proportional work.

Trusted verification: **exit 0** — typecheck PASS, 448 tests/22 suites PASS, privacy 71 PASS, cost 21 PASS (10 gates incl. V3-08 Gate 9 golden path at domain level), expo export/prebuild PASS, `:app:assembleDebug` BUILD SUCCESSFUL (20m7s).

## 2. What the candidate changed

| File | Change | Rationale |
|------|--------|-----------|
| `scripts/e2e-android.js` | `waitFor('Appartement', 60000)` → `waitFor('Appartement', 120000)` + 5-line comment (demo sign-in + SQLite fixture seeding 30-60s, first uiautomator dump 30-60s, 60s fits one dump attempt, 120s gives 2-3) | Aligns the Appartement wait window with the script's documented cold-emulator timing model and its 120s pattern used for all other post-launch elements; removes the concrete step-7 timing blocker of run 35521604346 |

No governance, directives, workflow, constitution, release state or director state modified. Delta confined to the E2E script.

## 3. Independent verification performed

| Check | Result |
|-------|--------|
| Candidate patch vs staged diff | byte-for-byte identical |
| Pristine tree diff (`/home/runner/work/_temp/accepted-pristine`) | only the 1 candidate file differs; no control-plane file changed |
| `node --check scripts/e2e-android.js` | PASS |
| `npx tsc --noEmit` (independent) | exit 0 |
| `npx jest` (independent) | 22 suites / 448 tests PASS |
| `npm run privacy:check` (trusted) | PASS (71 tests) |
| `npm run cost:check` (trusted) | PASS (21 tests, 10 gates incl. V3-08 Gate 9 golden path) |
| `expo export` / `expo prebuild` / `:app:assembleDebug` (trusted) | PASS / PASS / BUILD SUCCESSFUL (20m7s) |
| `docs/RELEASE_STATUS.json` openFindings | contains exactly the active reopen finding for run 35521604346 (mustFix: true) that this cycle repairs; no separate unresolved mustFix |
| Prohibited-pattern scan | only `assertAbsent('Premium'/'Chrono'/...)` negative checks; no chrono/premium/paywall/warm-V2 reintroduction |
| adb-call budget | fix adds zero adb calls — pure timeout constant change, no new network/adb cost |
| Timing budget | fix strictly increases headroom for the Appartement wait (2-3 dump attempts instead of 1); worst case unchanged and within the 360-min job timeout |

## 4. Technical soundness of the repair

**Minimal and targeted (sound):** The change is a single timeout constant plus a documentation comment. It does not alter the wait loop, the dump cache, the tap sequence, or any assertion. The 120s value is not arbitrary — it is the exact value the script already uses for every other post-launch element (`Ajouter`, `Balances`, `A faire`), each documented as "120s: single dump takes 30-60s on slow emulators, so 120s gives 2-3 dump attempts per element". The Appartement wait was the only post-launch element still at 60s, making it the odd one out and the concrete timing risk.

**Consistent with the failure chain:** The documented step-7 failure chain across 7+ emulator runs (RUN_35517442615, RUN_35510712950, RUN_35498415263, RUN_35492810195, RUN_35487229476) is uiautomator cold-start dump costs exhausting waitFor windows. After tapping Demarrer, the app completes demo sign-in (seeding the fixture via SQLite) and renders the groups list; on a cold API 35 x86_64 emulator this can take 30-60s before the first dump even starts. A 60s window fits at most one dump attempt; 120s gives 2-3. The repair directly addresses this.

**No regression surface:** No product code, domain logic, analytics, persistence, sync, UI or governance touched. The change cannot affect any ledger invariant, cost gate, privacy contract or UX behavior — it only extends a test-script wait window. The full trusted verification (typecheck, 448 tests, privacy 71, cost 21, export/prebuild, debug build) passes at exit 0.

## 5. Regression of accepted criteria

- **V3-01 domain/ledgers:** PASS — no domain code touched; zero-sum contribution/expense, integer minor units, per-currency separation, rate snapshot immutability, replay edit/delete all green.
- **V3-02 structure/design:** PASS — no UI/design code touched; 3 tabs, graphite/off-white/metallic, no chrono/premium/warm V2, WCAG AA.
- **V3-03 Ajouter/history:** PASS — no product code touched.
- **V3-04 Balances:** PASS — no product code touched.
- **V3-05 À faire:** PASS — no product code touched.
- **V3-06 groups/sync/backend frugal:** PASS — no product code touched; delta-only sync, tenant isolation, bounded reads intact.
- **V3-07 data product:** PASS — 71 privacy tests green; no data-plane code touched.
- **V3-08 cost/finition:** PASS — 10 cost gates green (21 tests); the fix is a test-script timeout constant only — no network, no reads/writes, no history-proportional work; golden-path headroom strictly increased.

No prohibited patterns: no chrono, Premium/paywall, warm V2 copy, cross-currency netting, silent unit conversion, fake integration, destructive history introduced.

## 6. Findings

1. **informational — empirical step-7 confirmation pending.** Path `release`. The step-7 logs of run 35521604346 require admin rights (403); the repair is based on the documented failure chain (uiautomator cold-start dump costs exhausting the 60s Appartement wait window) and the script's own timing model. A subsequent trusted finalizer run passing step 7 is the empirical gate that closes the release-level mustFix.
2. **informational — step-7 log access restricted.** Path `release`. Job log download returns 403 "Must have admin rights to Repository" and the artifact upload step was skipped on failure; the diagnostic echo in `scripts/finalizer-e2e.sh` remains the only surviving evidence channel. No action in Builder scope.

## 7. Conclusion

Trusted verification passed (exit 0) and no mustFix remains within Builder scope. The candidate is a coherent, minimal, bounded repair of the audited step-7 blocker (60s Appartement wait window too short for a cold API 35 x86_64 emulator), confined to the E2E script, preserving V3-01..V3-07 with no cost regression. The only openFindings entry is the active reopen finding this cycle repairs. **Decision: `accept`** — the next trusted finalizer run is the empirical gate for V3-08 completion.