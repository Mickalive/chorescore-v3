# Audit RUN 35261053897 — V3-07 Data product, privacy plane et pipeline frugale (repair)

**Decision: `accept`**

Trusted verification **passed with exit code 0** (typecheck clean, 19 suites / 391 tests pass, Expo export + Android prebuild + native debug build successful). All five must-fix findings from cycle 35257638071 are resolved and independently verified. No new must-fix finding remains. No regression on previously accepted V3 invariants.

---

## 1. Candidate delta

Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine` at `5f74f84` (repair baseline of cycle 35257638071). Candidate delta is exactly **2 modified files**, no other product file touched:

- `src/analytics/pipeline.ts` (+20/−6) — input validation rework
- `__tests__/domain/v3-07-analytics-privacy.test.ts` (+97/−22) — test fixes

## 2. Trusted verification (authoritative)

`reports/audits/TRUSTED_35261053897.md` — **exit code 0**:

- `npm run typecheck` clean
- `npm run test` — 19 suites passed / 19 total, 391 tests passed / 391 total
- Expo export OK, Android prebuild OK, native debug build `BUILD SUCCESSFUL` (395 tasks)

## 3. Independent verification results

| Check | Result |
|---|---|
| `npx tsc --noEmit` | exit 0 — no TypeScript errors |
| `npx jest __tests__/domain/v3-07-analytics-privacy.test.ts` | 71 passed / 71 total |
| `npx jest` (full) | 19 suites / 391 tests pass — no regression |
| `npm run check` (typecheck + test) | exit 0 |
| Consent-expiry test × 5 runs | 5/5 pass (deterministic) |
| Gate executed on new test data | clean product → approved=true, 0 violations; audit data → approved=true, 0 violations; 5-record unique dataset still rejected (protections intact) |
| Pipeline executed on labeled fact | success=true; emitted event has no label/title/notes; taxonomyCategoryId derived; notes and userId still rejected on input |

## 4. Previous must-fix findings — all resolved

### F1 — Test-file type errors — resolved
- Line ~463: label-in-output record cast `as unknown as ResearchDataProduct['data']`
- Lines ~528–541: clean-product test now uses a full 18-record valid dataset with cast
- Line ~789: `'commercial-use'` → `'synthetic-data-generation'` (valid `DataProcessingPurpose`)
- Line ~929: `as unknown as Record<string, unknown>` double cast
- `npx tsc --noEmit` exits 0.

### F2 — Pipeline label contradiction — resolved
`validateNoOperationalIds` now forbids **operational IDs only** (label/title/notes removed). `validateNoFreeText` still rejects `title`/`notes` because no transform consumes them; `label` is consumed by `classificationCache.classify` and never emitted. The new test `allows label as input (consumed for classification, never emitted)` proves a labeled `contribution_created` fact transforms with `success=true` AND the emitted `AnonymousContributionEvent` contains no `label`/`title`/`notes` field while carrying the derived `taxonomyCategoryId`. The `PrivacyReleaseGate` `FORBIDDEN_TEXT_FIELDS` (gate.ts) still includes `label`/`title`/`notes`, so free text remains rejected at the release boundary — the no-free-text-in-outputs guarantee is preserved and now enforced at the correct layer (output), not on consumed input.

### F3 — Gate tests unpassable — resolved
- Clean-product test: 18 records, 3 repeated (category, month, beneficiaryCount) combos, 6 per category, householdCount 10 → `approved=true`, 0 violations (combinations 3 ≤ 18×0.8; facts 18 ≥ minCohortSize×2; each category ≥ 5).
- Audit-log test: 12 non-empty records, 2 combos, 6 per category → passes the gate; test asserts `log.approved === result.approved` and `log.violationCount === result.violations.length` (the log faithfully records the gate decision).
- Gate protections **not weakened**: independent execution confirms a 5-record unique dataset is still rejected (`rare_cell` ×4, `reconstruction_risk`, `re_identification_risk`).

### F4 — Query budget mismatch — resolved
Test asserts `remainingQueriesThisMinute=2` with the documented semantic "queries remaining AFTER this one" (`limit − queriesBeforeThisQuery − 1 = 3 − 0 − 1 = 2`), matching `queryBudget.ts` line 92. Rejection branches return `limit − queriesThisMinute`, consistent because a rejected query is not recorded.

### F5 — Consent expiry race — resolved
`retentionDays: -1` puts the cutoff strictly in the future of any just-created record (`cutoff = now + 1 day`), making the test deterministic. Passed 5/5 repeated runs. Default policies keep positive retention (365×3 / 365×2) and the "retention periods are finite" test still asserts `retentionDays > 0` for all default policies.

### F6 — Trusted gate — resolved
Full trusted V3 verification returns **exit code 0**.

## 5. Non-blocking observations (unchanged, outside repair scope)

- `PipelineCheckpoint` remains a passive data structure — no checkpoint store, cursor driver, or async batch scheduler; the pipeline is synchronous.
- No wiring from the `ResearchAnalyticsGateway` port / `LocalResearchAnalyticsAdapter` into the pipeline (adapter `emitFact` still a no-op).
- Gate `requiresDifferentialPrivacy` is computed but not enforced in `validate` approval.
- `types.ts` header invariant overstatement (`ConsentRecord.userId`, `PipelineCheckpoint.householdId` are operational-side records).
- `PipelineCheckpoint.id` uses `Date.now()` + `Math.random()`.

These were explicitly non-blocking in the previous audit and are not part of this repair's objective.

## 6. Acceptance criteria status (V3-07)

| Acceptance | Status |
|---|---|
| Operational Store / Research Analytics Plane séparés | Met — output event types contain no IDs/text; gate rejects IDs and free text in output |
| analytics désactivable sans casser le produit | Met (adapter disabled by default; module standalone) |
| taxonomie/classification versionnée | Met (3.0.0, deterministic, rule-based) |
| aucun texte libre/ID opérationnel dans sorties | Met — label consumed as input, never emitted; gate still rejects label/title/notes in output |
| privacy release gate et anti-reconstruction | Met — gate strict; tests now exercise real approval and rejection paths |
| événements V3 contributions/dépenses/settlements/usage | Met — contribution/entry transforms reachable and tested |
| checkpoint incrémental sans scan historique | Weak (passive checkpoint, no driver) — non-blocking |
| classification cache évite appels identiques | Met (keyed normalizedLabel+taxonomyVersion+classifierVersion) |
| aucun LLM synchrone | Met (rule-based only) |
| sans nouvel événement, pas de rescan | Met (stateless pipeline) |
| rétention et reprocessing contrôlés | Met — deterministic expiry test |

## 7. Conclusion

`accept`. The repair resolves every must-fix finding from cycle 35257638071 without weakening any privacy protection: label is now a legitimate pipeline input consumed for classification and never emitted, the release gate still rejects free text and operational IDs in outputs, the gate's anti-reconstruction rules are unchanged and verified, the query-budget and retention semantics are documented and deterministic, and the full trusted gate passes (exit 0) with no regression on V3-01..V3-06.