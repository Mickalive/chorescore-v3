# Audit RUN 35540196125 — V3-08 repair (cache-miss guard for stuck-app detector)

- **Cycle:** 35540196125
- **Role:** builder (independent audit)
- **Active criterion:** V3-08 (Finition, coût et release mobile)
- **Objective:** REPAIR V3-08 — guard `consecutiveEmptyMatchCount` increment on real dumps (cache miss) only; strengthen the contract test so reverting the guard fails.
- **Decision:** `repair`
- **Trusted verification exit code:** 1 (mandatory mustFix — accept forbidden)

---

## 1. Candidate delta (vs pristine accepted tree)

Exactly two files differ from `/home/runner/work/_temp/accepted-pristine`:

### `scripts/e2e-android.js` (+29/−11)
- `dumpUi()` now returns `{ xml, nodes, fromCache: true }` on 30 s dump-cache hits and `{ xml, nodes, fromCache: false }` on real dumps and on all failure paths.
- `findNodes()` calls `dumpUi()`, reads `dump.fromCache`, and attaches `nodes._fromCache = dump.fromCache`.
- `waitFor()` guards the increment: `if (!nodes._fromCache && dumpFailures <= previousDumpFailures)` — cache hits neither increment nor reset `consecutiveEmptyMatchCount`.
- Force-stop + relaunch recovery preserved; counter resets preserved on crash recovery (both pidof branches), dump-failure branch, and stuck recovery.

### `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (+31)
- New test `dumpUi() returns fromCache flag and findNodes() exposes it` — asserts `fromCache: true`/`fromCache: false` returns and `nodes._fromCache = dump.fromCache` propagation.
- New test `consecutiveEmptyMatchCount increment is guarded on real dump (cache miss)` — asserts the guarded increment regex and a comment explaining the cache-hit exclusion.

No control-plane, governance, directives, release-state or product code touched.

---

## 2. Independent verification performed

| Check | Result |
|---|---|
| Trusted log `TRUSTED_35540196125.md` | exit 1 — typecheck PASS, 450/451 tests PASS, single failure at `v3-08-finalizer-wrapper.test.ts:289` |
| `npm run typecheck` (tsc --noEmit) | PASS |
| `npx jest` full suite | 21 suites PASS, 1 suite FAIL (1 test), 450 PASS / 1 FAIL — identical scope to trusted log |
| `node --check scripts/e2e-android.js` | PASS |
| Loop simulation (virtual time, exact waitFor semantics) | 120 s cold start with cache hits → **0 force-stops**; cache hits neither increment nor reset; genuinely stuck app still triggers force-stop + relaunch; realistic 30 s-dump timing: recovery triggers within the 600 s window |
| Guard-revert experiment | Replacing the guard with the buggy `if (dumpFailures <= previousDumpFailures)` makes the new test FAIL at line 287 — reverting the guard fails the test, as required |
| Counter-reset / recovery preservation scan | `consecutiveEmptyMatchCount = 0` on crash recovery, dump failure, stuck recovery; force-stop + monkey relaunch intact |
| Prohibited-pattern scan | No chrono/premium/paywall/warm-V2/cross-currency/silent-conversion reintroduction; V3-01..V3-07 invariants untouched |
| Working-tree integrity after audit | Candidate restored byte-for-byte; guard present at `scripts/e2e-android.js:426` |

---

## 3. Findings

### Finding 1 — mustFix: brittle string assertion fails on comment line-wrap

- **Path:** `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (line 289)
- **Problem:** The new test asserts `expect(e2e).toContain('Cache hits must NOT increment the counter')`, but the script comment wraps the phrase across two lines:
  `// that returned no matching node.  Cache hits must NOT increment the` + newline + `// counter — the dump cache returns the same result without touching`.
  The exact substring is not contiguous, so `toContain` fails. This is a string-vs-comment line-wrap mismatch, **not** a logic defect: the guard itself is correct and simulation-verified. Trusted verification exit code 1 is a mandatory mustFix and forbids `accept`.
- **Evidence:** Trusted log shows `Expected substring: "Cache hits must NOT increment the counter"` at line 289; `scripts/e2e-android.js` lines 419–421 contain the phrase split across two comment lines; local `npx jest` reproduces the identical single failure (450 PASS / 1 FAIL); typecheck PASS.
- **mustFix:** true
- **Required fix:** Make the asserted phrase contiguous — reword the comment in `scripts/e2e-android.js` so the exact string `Cache hits must NOT increment the counter` appears on one line, or relax the test to a line-break-tolerant regex (e.g. `/Cache hits must NOT increment the\s*\n?\s*\/\/\s*counter/`). Keep the guard, the `fromCache` plumbing, the recovery and the counter resets unchanged.
- **Verification:** Full trusted verification exits 0 (typecheck + all jest suites, including the two new cache-miss guard tests); guard-revert experiment still fails the test; loop simulation still shows 0 force-stops on a 120 s cold start and recovery on a genuinely stuck app.

---

## 4. Conclusion

The candidate correctly implements the audited repair: the `consecutiveEmptyMatchCount` increment is now conditioned on a real dump (cache miss) having run, cache hits neither increment nor reset the counter, the force-stop + relaunch recovery and all counter resets are preserved, and the strengthened contract test fails when the guard is reverted. The only remaining defect is a brittle exact-substring assertion that fails because the script comment wraps the phrase across two lines — a one-line fix.

Trusted verification exit code is 1, so the decision **must not be accept**. The delta is coherent and correct in its product logic; it is preserved as the WIP baseline. **Decision: `repair`.**