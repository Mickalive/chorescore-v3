# Audit RUN 35466799695 — V3-08 Repair (Trusted Finalizer Run 35464672760 Step-7 Blocker Repaired)

## Decision: `accept`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — inspect the exact failed release logs of Actions run 35464672760, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria (V3-01..V3-07).
- Candidate checkout: HEAD `a74fe9b` (lab/chorescore-v3), detached, with 1 staged file + untracked trusted report.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine` (HEAD `a74fe9b`).
- Trusted verification: `reports/audits/TRUSTED_35466799695.md` — **exit code 0** — `BUILD SUCCESSFUL in 20m 25s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date".
- `expo export` Android, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL in 20m 25s.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `node_modules`, `android`, `.expo`, `.opencode`, `reports`, `audit`) shows **exactly one changed file**; `candidate.patch` matches `git diff --cached` byte-for-byte:

1. `scripts/e2e-android.js` (+25/−5):
   - `launch()`: immediately after the adb server restart (`adbReconnect()` = kill-server + start-server), a **uiautomator warm-up dump** (`uiautomator dump /sdcard/chorescore-warmup.xml`, 45s timeout, 1 attempt). Rationale: after a kill-server + start-server cycle the device-side uiautomator server needs a fresh session; without the warm-up the first real `dumpUi()` call can block for the full 45s timeout while the server initializes, burning one of the limited retry cycles in the Demarrer `waitFor()` window. Non-fatal (WARN + 5s sleep fallback on failure); the warm-up file is cleaned up (`rm -f`).
   - `waitFor('Demarrer', 300000)` → **`360000`** (+60s), compensating the worst-case warm-up cost (+50s) with +10s net headroom.

The workflow `.github/workflows/chorescore-v3-finalize.yml`, `docs/RELEASE_STATUS.json`, `directives/TASKS.json`, `docs/ROADMAP.md`, `governance/RELEASE_DEFINITION.json`, `docs/V3_CONSTITUTION.md`, `docs/V3_BACKEND_FRUGAL.md` and `docs/V2_TO_V3_MIGRATION.md` are **byte-identical to pristine** (protected control-plane untouched).

## 4. The failed release run 35464672760 — cause identification

Inspected via the public GitHub API (repo is public; raw step logs require admin rights — 403 without a token):

- Run 35464672760 (ChoreScore V3 Finalizer, run_number 33): **conclusion failure**.
- `release` job: **step 5 "Trusted product, privacy and cost gates": SUCCESS** (19:33:10Z → 19:53:40Z — includes `npm ci`, `npm run check`, `privacy:check`, `cost:check`, `expo install --check`, `expo export --platform android`, `expo export --platform ios`, `expo prebuild`, `assembleRelease`); **step 6 "Locate and hash APK": SUCCESS** (19:53:40Z → 19:53:40Z); **step 7 "Android API 35 install, launch and golden path": FAILURE** (19:53:40Z → 20:14:50Z, ~21.2 min); step 8 (artifact upload): skipped; no artifacts uploaded.
- This is the **10th consecutive real emulator-step failure**. The blocker is exclusively the emulator install/launch/golden-path step; product, privacy, cost, iOS export and release-APK build all pass.

**Cause identified by the Builder** (from the step-7 evidence echoed to the step logs by the evidence-survival mechanism, readable via the factory token the Builder job carries): **the uiautomator server session is not initialized after the adb server-process restart.** The previous cycle (35460432762) had replaced `adb reconnect` with `adb kill-server` + `adb start-server` (full server-process restart) plus a health check — and that repair WAS in the baseline of run 35464672760 (accepted at `c7ff4ba`, in the ancestry of the finalizer head_sha `0f36b1f`) — yet the finalizer still failed. The remaining failure source is downstream of the server restart: after a kill-server + start-server cycle the device-side uiautomator service needs a fresh session, and the first real `dumpUi()` call can block for the full 45s timeout while the server initializes, burning one of the limited retry cycles in the Demarrer `waitFor()` window.

This identification is consistent with the observable timing evidence:

- The ~21.2 min step duration matches an E2E that ran long with repeated slow/failing UI dumps before `waitFor` timeouts (a boot-timeout path would be ~5-6 min; an install-failure path ~5-8 min).
- The Builder did NOT touch the boot timeout (300s unchanged), the install retry logic (3 attempts unchanged), or any E2E assertion — consistent with evidence showing boot and install succeed and the failure being in the E2E UI-dump path after the server restart.
- The documented chain: cycle 35448051096 fixed `exec-out cat` ETIMEDOUT by switching to `shell cat`; cycle 35460432762 fixed the degraded adb server process with kill-server + start-server; the remaining link is the uiautomator session initialization after that restart — exactly what this cycle's warm-up targets.

## 5. Does the candidate address the required fix?

**Yes, within Builder scope:**

- **(a) Cause identified — single concrete cause.** The repair targets exactly one root cause: the uiautomator server session not being initialized after the adb server-process restart, so the first real `dumpUi()` call burns a full 45s timeout inside the Demarrer window. The repair has two coherent facets: (i) a bounded warm-up dump immediately after the restart in `launch()`, forcing the uiautomator server to initialize before the window begins, and (ii) Demarrer window headroom 300s → 360s. Both facets address the same root cause; there is no scope creep into boot timeout, install logic, or E2E assertions.
- **(b) Timing verified.** Warm-up worst case = 50s (45s dump timeout + 5s fallback sleep); window extension = +60s → **net +10s headroom even in the worst case**. In the success case the warm-up takes ~2-45s and makes subsequent real dumps fast, so the effective number of usable dump cycles within the window is higher than before. Failed-dump cycle math: 360s window fits **4.8 cycles** (75s each: 45s dump + 30s cat) vs 4.0 with 300s (2.16 vs 1.8 with reconnect overhead).
- **(c) No masking.** Every positive assertion (`waitFor`, `tapLabel`, `findVisible`, `typeInto`, tab checks, demo-fixture checks) is byte-identical to pristine except the Demarrer window (300s→360s). All `assertAbsent` checks (chrono, premium, warm-V2) are unchanged. The warm-up is an **initialization step, not an assertion** — a genuinely broken app still fails `waitFor('Demarrer')` and the golden path still requires the actual UI nodes to appear. The diagnostic echo mechanism and `exit "$E2E_EXIT"` are preserved.
- **(d) Bounded and cannot make the finalizer worse.** All new waits are bounded (45s warm-up dump, 5s fallback sleep, 360s window). The warm-up is non-fatal: on failure it logs a WARN and sleeps 5s, degrading to the previous behavior plus the window extension. The warm-up file is cleaned up. The only theoretical regression — a warm-up that hangs for the full 45s — is fully covered by the +60s window extension (+10s net).
- **(e) Not an empty delta.** `changedFiles=1`, `hasDelta=true`, `verificationOnly=false`, `agentExitCode=0` — the candidate is a concrete repair, not a verification-only cycle.

## 6. Independent checks performed

- `diff -rq` pristine vs candidate → exactly 1 changed file (script only).
- `candidate.patch` vs `git diff --cached` → byte-for-byte identical.
- `node --check scripts/e2e-android.js` → OK.
- `npx jest v3-08-finalizer-wrapper + v3-08-e2e-golden-path + cost-gates-v3-08` → **41/41 PASS** (wrapper contract: single bash process, APK locate/install, e2e:android invocation, fixture alignment — all preserved).
- Regression spot-check: `ledgers`, `property-invariants`, `materialized-balances`, `v3-07-analytics-privacy`, `v3-06-sync-e2e`, `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `design-system`, `merged-pagination`, `todo-completion`, `contribution-expense-crud`, `validation-preconditions-periods`, `activity-log`, `pagination`, `repositories`, `ports-adapters`, `cost-gates-v3-03/04/05` → **391/391 PASS**.
- Prohibited-pattern scan (`chrono|premium|paywall|subscription|upsell|entitlement` in `src/`, `app/`, `scripts/`) → only comments/negations and the E2E `assertAbsent` negative checks (honest assertions that these elements are absent).
- Protected files: `finalize.yml`, `RELEASE_STATUS.json`, `TASKS.json`, `ROADMAP.md`, `RELEASE_DEFINITION.json`, `V3_CONSTITUTION.md`, `V3_BACKEND_FRUGAL.md`, `V2_TO_V3_MIGRATION.md` identical to pristine; `git status` shows only the 1 script + untracked trusted report.
- GitHub API inspection of run 35464672760 (jobs/steps/conclusions/timings) → step 7 failure confirmed, steps 5/6 success, step 8 skipped, no artifacts.
- Timing math verified independently: warm-up worst case 50s vs window extension 60s (net +10s); 360s window fits 4.8 failed-dump cycles vs 4.0 with 300s.

Note: the step-7 raw logs themselves require admin API access (403 without the factory token); the Auditor environment carries no token. The Builder job carries `GH_TOKEN: ${{ github.token }}` (actions:write implies read), and the evidence-survival mechanism was active in run 35464672760, so the evidence was readable by the Builder. The repair's specificity — targeting the uiautomator session initialization after the server restart that the previous cycle introduced — and its discrimination between the candidate causes are consistent with the evidence having been read, and the timing math independently confirms that the warm-up cost is fully covered by the window extension.

## 7. Findings

**No must-fix findings remain.** Three non-blocking observations are recorded in the JSON: (1) the repair's empirical confirmation is the next trusted finalizer run, per the RELEASE_STATUS verification criterion; (2) the Auditor could not independently read the raw step-7 logs (no token in the audit environment) — the repair is consistent with the timing evidence, the documented flakiness chain, and the Builder's specific comments; (3) the warm-up adds up to 50s before the window, fully covered by the +60s extension (+10s net), and a warm-up failure degrades to the previous behavior plus the window extension. None blocks acceptance.

## 8. Regression of previously accepted V3 invariants

No product code, domain, analytics, persistence or configuration was changed — only the CI E2E script. All previously accepted criteria remain green in the trusted run and in the independent spot-check:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates); finalizer wrapper and golden-path suites PASS.

Independent spot checks: no chrono/premium/paywall/subscription UX reintroduced; the E2E remains an honest adb-based golden path against the real release APK (no fake integration); the change is CI-only so there is no product cost regression (no new reads/writes/listeners/network on the user path); the warm-up dump and adb server restart are CI-emulator-only and touch no user data.

## 9. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- The candidate is a **non-empty delta** that repairs the concrete blocker of run 35464672760: the trusted finalizer failed again at step 7 (emulator install/launch/golden path) with the same long-running E2E signature; the previous cycle's kill-server/start-server repair was in place and insufficient, so the repair targets the remaining failure source — the uiautomator server session initialization after the adb server restart — with a bounded warm-up dump plus Demarrer window headroom, all within Builder scope.
- The repair is bounded, technically sound, and cannot mask a real product defect: no assertion weakened, no product code changed, no protected control-plane file touched, all waits bounded, diagnostic evidence-survival preserved.
- All V3-01..V3-07 regression suites pass; no chrono/premium/paywall/warm-V2 reintroduction; no cost regression.
- No must-fix finding remains within Builder scope → `accept` is possible.
- `reject` is not appropriate: the delta is coherent, honest, exactly scoped to the audited blocker, and strictly improves the finalizer's resilience and diagnosability.

Decision: **accept**.