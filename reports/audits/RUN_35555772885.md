# Audit RUN 35555772885 — V3-08 repair (trusted finalizer failure, run 35552961193)

- **Cycle:** 35555772885
- **Role:** builder (independent audit)
- **Active criterion:** V3-08 (Finition, coût et release mobile)
- **Objective:** REPAIR V3-08 trusted finalizer failure from Actions run 35552961193. Inspect the exact failed release logs, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria.
- **Decision:** `repair`
- **Trusted verification exit code:** 0

---

## 1. What the cycle was supposed to repair

Run 35552961193 (workflow_dispatch of `chorescore-v3-finalize.yml`) failed. Via the public GitHub API:

- `release` job, step **"Trusted product, privacy and cost gates"**: **success** (02:05:19 → 02:28:18 UTC) — this step includes `assembleRelease` and the iOS export.
- Step **"Locate and hash APK"**: success.
- Step **"Android API 35 install, launch and golden path"**: **failure** (02:28:18 → 02:55:34 UTC, 27m16s).

The exact step diagnostics are not readable anonymously (logs endpoint returns 403 without admin rights; the auditor environment has no GH_TOKEN).

## 2. Candidate delta (vs pristine accepted tree)

**There is no delta.**

- `/home/runner/work/_temp/candidate-builder/metadata.json`: `changedFiles=0`, `hasDelta=false`, `verificationOnly=true`, `agentExitCode=0`.
- `candidate.patch`: 0 bytes.
- `git diff` vs `/home/runner/work/_temp/accepted-pristine`: no product change; only untracked `.expo/` and `.opencode/` tooling directories.

The cycle objective — repair the trusted finalizer failure — is **unmet**.

## 3. Trusted verification scope gap

`reports/audits/TRUSTED_35555772885.md` reports exit code 0:

- typecheck PASS; 451/451 tests PASS (22 suites); privacy 71/71 PASS; cost gate 21/21 PASS;
- expo export **android** PASS; prebuild PASS; `:app:assembleDebug` BUILD SUCCESSFUL (19m20s).

But `.github/scripts/verify-product.sh` does **not** exercise the failing step: it never runs the emulator E2E golden path, `assembleRelease`, or the iOS export. The trusted verification passing therefore says nothing about whether the finalizer failure is repaired.

## 4. The failure is persistent

The emulator E2E step has failed **15 consecutive dispatched finalizer runs** at the same step:

`35552961193, 35544223219, 35530222887, 35521604346, 35515804944, 35505122996, 35496233889, 35490543018, 35485115705, 35476794686, 35470723768, 35464672760, 35457974999, 35451816114, 35445913329, 35437961965`

Scheduled finalizer runs are no-ops (release skipped). The gates step passes every time; the emulator step fails every time. This is a persistent blocker, not transient flakiness.

## 5. Independent verification performed

| Check | Result |
|---|---|
| Trusted log `TRUSTED_35555772885.md` | **exit 0** — typecheck PASS, 451/451 tests PASS (22 suites), privacy 71/71 PASS, cost 21/21 PASS, expo export android PASS, prebuild PASS, assembleDebug BUILD SUCCESSFUL in 19m20s |
| Trusted verification scope | `verify-product.sh` covers typecheck/tests/privacy/cost/expo-export-android/prebuild/assembleDebug only — **not** the failing emulator E2E step, not assembleRelease, not iOS export |
| Candidate delta | **none** — `changedFiles=0`, `hasDelta=false`, `candidate.patch` empty, diff vs accepted-pristine clean |
| Finalizer failure (run 35552961193) | gates step success, APK located+hashed, emulator E2E step failure after 27m16s (02:28:18 → 02:55:34 UTC) |
| Failure pattern | 15 consecutive dispatched finalizer runs failed at the same emulator E2E step; scheduled runs are no-ops |
| Golden-path UI elements | all present in app code: `Demarrer (demo)` (app/index.tsx), tabs `Ajouter`/`Balances`/`A faire` (app/(tabs)/_layout.tsx), fixture `Appartement`/`Vaisselle du soir`/`Sortir les poubelles` (src/features/app/demoFixture.ts), member names `Alex`/`Sam` via `memberName` and section titles `Contribution`/`Argent` (app/(tabs)/balances.tsx) |
| App loading path | robust: in-memory repository fallback, `reposReady` gating of `isLoading`, sign-in awaits `ensureReposReady` — no unbounded hang blocking `Demarrer (demo)` |
| E2E script hardening review | `scripts/e2e-android.js` `waitFor()` force-stops and relaunches the app after `STUCK_APP_THRESHOLD=4` consecutive real dumps with no label match; on a slow API 35 x86_64 emulator (documented cold start up to 240s, dump latency 30-60s each) 4 real dumps = 2-4 min, so the detector can kill a healthy app mid-cold-start before `Demarrer (demo)` renders and reset the cold start each cycle — a plausible systematic blocker that remains in the tree |
| Previous audit review | hardening history (waitFor bumps, adb reconnect, dump cache/fromCache guard, warm-up, stuck-app detector, cache-miss guard) is consistent with a persistent emulator-step blocker that script hardening has not eliminated |
| Regression scan | no product delta ⇒ no regression possible; no chrono/premium/paywall/warm-V2/cross-currency/silent-conversion/analytics change introduced |

## 6. Decision rationale

The acceptance rule is: **accept iff trusted verification passed AND no mustFix remains.**

- Trusted verification passed (exit 0) — necessary but not sufficient.
- A mustFix **remains**: the release-level blocker "Trusted V3 finalizer failed" is unaddressed. The candidate makes no change that can affect the failing step, the failing step is outside the trusted verification coverage, and the step has failed 15 consecutive dispatched runs. Accepting would dispatch the finalizer again with no change and fail identically — an infinite loop with no progress.

**Decision: `repair`.** The empty candidate is retained as the WIP baseline. The next cycle must inspect the step-7 evidence of run 35552961193 (logcat-wait-*.txt, logcat-failure.txt, dump-*.xml, step logs via the factory token) and repair the concrete runtime/E2E blocker — primary hypothesis: the stuck-app detector's 4-dump force-stop threshold killing a healthy-but-slow cold start on API 35 x86_64 (fix with time-based stuck detection, a higher threshold, or no force-stop while the app process is alive and dumps are healthy). If the evidence instead shows an infrastructure-only failure, document it explicitly so the trusted shell can decide on a workflow-level remedy.

## 7. Must-fix finding

- **path:** candidate delta (empty) — `scripts/e2e-android.js` (stuck-app detector) / finalizer step "Android API 35 install, launch and golden path"
- **problem:** objective unmet — no delta; failing step not covered by trusted verification; 15 consecutive identical finalizer failures; next run would fail identically
- **evidence:** metadata.json (`changedFiles=0`, `hasDelta=false`, `verificationOnly=true`); empty `candidate.patch`; clean diff vs accepted-pristine; API run 35552961193 failure at the emulator E2E step after gates passed; 15-run failure pattern; `TRUSTED_35555772885.md` exit 0 covering only `verify-product.sh` scope; `STUCK_APP_THRESHOLD=4` force-stop logic in `scripts/e2e-android.js`
- **mustFix:** true
- **requiredFix:** read the step-7 evidence of run 35552961193 and repair the concrete runtime/E2E blocker (verify the stuck-app detector false-positive hypothesis first; otherwise document the infrastructure-only cause explicitly); preserve V3-01..V3-07
- **verification:** re-run the trusted finalizer (or at minimum the emulator E2E step) and confirm the golden path completes: Demarrer → Appartement → tabs Ajouter/Balances/A faire → Vaisselle du soir → Alex/Sam/Contribution/Argent → Sortir les poubelles → back to Ajouter; trusted verification exit 0; no regression in the 451-test suite, privacy 71, cost 21