# Audit RUN 35434905999 — V3-08 Repair (Trusted Finalizer Evidence Survival)

## Decision: `accept`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — obtain the actual step-7 evidence of run 35430259067 and fix the concrete blocker: (a) ensure the diagnostic evidence survives (audit/android-e2e/ uploaded as artifact on failure, or equivalent in-scope mechanism); (b) identify which of the three candidate blockers occurred (emulator boot timeout/flake, `adb install -r` failure, E2E golden-path failure) and repair only that cause within Builder scope, preserving V3-01..V3-07 and the accepted diagnostic/resilience baseline. Protected control-plane files must not be modified.
- Candidate checkout: HEAD `72064af` (lab/chorescore-v3), detached, with 2 staged files + untracked trusted report.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Trusted verification: `reports/audits/TRUSTED_35434905999.md` — **exit code 0** — `BUILD SUCCESSFUL in 19m 52s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date".
- `expo export` Android, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `node_modules`, `android`, `.expo`, `reports`, `.opencode`) shows **exactly two changed files**; `candidate.patch` (9623 bytes) matches `git diff --cached` byte-for-byte:

1. `scripts/e2e-android.js` (+30/−2):
   - `adb logcat -c` before launch → clean logcat for launch diagnostics.
   - Launch wait 12s → 20s, plus a 5s extended wait with a second `pidof` check and a post-launch logcat capture (last 50 lines) when the process is still not found.
   - `waitFor('Demarrer', 120000)` (was 90000) — API 35 x86_64 cold-start tolerance.
   - On failure, `result.json` is echoed to stderr so the structured diagnostic survives in the step logs even when `audit/android-e2e/` is not uploaded as an artifact.

2. `scripts/finalizer-e2e.sh` (+69/−10):
   - Emulator boot timeout 180s → 300s, with boot-state diagnostics on timeout (`sys.boot_completed`, `ro.build.version.sdk`, `init.svc.bootanim`).
   - Package-manager settle 30s → 20s (net total wait budget still +110s).
   - APK sha256 echoed; install attempts capture output and exit code, retry wait 10s → 15s.
   - On install failure: device diagnostics (`adb devices -l`, first 10 packages, `df /data`, `pm path app.chorescore.v3`).
   - On E2E failure: a bounded diagnostic echo block prints the `audit/android-e2e/` directory listing, `result.json`, `logcat-finalizer.txt` (last 200), `logcat-failure.txt` (last 200), `dumpsys-activities-failure.txt` (last 50) and `logcat-wait-*` dumps (last 100 each) to stdout — the evidence-survival mechanism.

The workflow `.github/workflows/chorescore-v3-finalize.yml`, `docs/RELEASE_STATUS.json` and `directives/TASKS.json` are **byte-identical to pristine** (protected control-plane untouched).

## 4. Does the candidate address the required fix?

Yes, within Builder scope:

- **(a) Evidence survival — fixed.** The root cause of 7 consecutive no-repair cycles was that the diagnostic files written to `audit/android-e2e/` were lost on failure (workflow uploads only the APK; step 8 skipped on failure; raw logs 403 without admin rights). The candidate implements the Director-recommended in-scope mechanism (RUN_35432141926): echo the diagnostic payload to stdout/stderr on failure. GitHub Actions preserves step output on failure; the factory token carries `actions: write` (implies read), so the next cycle can read the evidence from the step logs via API or UI. Artifact upload would require modifying the protected workflow YAML, which is out of Builder scope — the echo mechanism is the maximum in-scope repair.
- **(b) Blocker identification — bounded resilience for all three candidates.** The exact cause of run 35430259067 could not be identified because its evidence was lost (the very problem being fixed). The candidate hardens all three candidate failure modes with bounded, low-risk changes: boot timeout 180s→300s + diagnostics; install retry with captured output + device diagnostics; E2E cold-start tolerance (20s+5s launch, 120s Demarrer, logcat clear/capture, result.json echo). This matches the factory pattern endorsed in RUN_35419214459 and RUN_35425284987 ("the Builder-side action is exactly what this candidate does: Diagnostics — the next trusted finalizer run will now produce the actual step-7 logs").
- **No masking.** Every positive assertion (`waitFor`, `tapLabel`, `findVisible`, `typeInto`, tab checks, demo-fixture checks) is byte-identical to pristine except the `Demarrer` timeout (90s→120s) and the launch wait (12s→20s+5s). All `assertAbsent` checks (chrono, premium, warm-V2) are unchanged. The diagnostic echo cannot make the golden path pass spuriously and preserves `exit "$E2E_EXIT"`.
- **Cannot make the finalizer worse.** All waits remain bounded; the echo block is guarded (`|| echo missing`) and truncated (`tail -200/-100/-50`); the install retry already existed (3 attempts) and now only adds output capture and diagnostics.

## 5. Independent checks performed

- `bash -n scripts/finalizer-e2e.sh` → OK; `node --check scripts/e2e-android.js` → OK.
- `npx jest v3-08-finalizer-wrapper + v3-08-e2e-golden-path + cost-gates-v3-08` → **41/41 PASS** (wrapper contract, single-line-form regression guard, fixture alignment, cost gates).
- Regression spot-check: `ledgers`, `property-invariants`, `v3-07-analytics-privacy`, `materialized-balances` → **114/114 PASS**.
- E2E assertion diff vs pristine → only timeouts changed; no assertion removed or weakened.
- Fixture alignment: `Demarrer` (app/index.tsx), `Appartement`, `Vaisselle du soir`, `Sortir les poubelles`, `Alex`, `Sam` (demoFixture.ts), `Ajouter`/`Balances`/`A faire` (tabs layout), `Contribution` (balances screen) — all present.
- Protected files: `finalize.yml`, `RELEASE_STATUS.json`, `TASKS.json` identical to pristine.
- `git diff --cached --stat` → 2 files; no protected control-plane file modified.

## 6. Findings

**No must-fix findings remain.** Two non-blocking observations are recorded in the JSON (the unidentified exact cause of run 35430259067 — now made diagnosable by the evidence-survival mechanism — and the minor 30s→20s settle reduction, offset by the +120s boot wait and install retry). Neither blocks acceptance.

## 7. Regression of previously accepted V3 invariants

No product code, domain, analytics, persistence or configuration was changed — only CI E2E scripts. All previously accepted criteria remain green in the trusted run:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates).

Independent spot checks: no chrono/premium/paywall/subscription UX reintroduced; the E2E remains an honest adb-based golden path (no fake integration); the change is CI-only so there is no product cost regression (no new reads/writes/listeners/network on the user path); logcat capture is CI-emulator-only and touches no user data.

## 8. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- The candidate fixes the concrete evidence-loss blocker that caused 7 consecutive no-repair cycles: the diagnostic payload now survives in the step logs on failure, readable by the next cycle without admin rights.
- The candidate hardens all three candidate step-7 failure modes with bounded, low-risk resilience changes, consistent with the factory pattern endorsed in RUN_35419214459 and RUN_35425284987.
- No E2E assertion was weakened; no product code changed; no protected control-plane file touched; no regression of V3-01..V3-07; no must-fix remains within Builder scope → `accept` is possible.
- `reject` is not appropriate: the delta is coherent, honest, exactly scoped to the audited blocker, and strictly improves the finalizer's diagnosability and resilience.

Decision: **accept**.