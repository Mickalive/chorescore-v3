# Audit RUN 35421406333 — V3-08 (REPAIR)

## Decision: `accept`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — track install success explicitly with a flag: set `INSTALLED=1` immediately after the successful `adb install -r "$apk"` before `break`, and change the error branch to `if [ "${INSTALLED:-0}" -ne 1 ]; then echo "ERROR: APK install failed after ${MAX_ATTEMPTS} attempts" >&2; ...; exit 1; fi`. Keep the existing diagnostics (`adb devices -l`, `pm list packages`) in the error branch. Do not change the boot-wait, diagnostics or golden-path parts, which are correct.
- Candidate checkout: HEAD `cad0475` (lab/chorescore-v3), detached, with 1 staged file (`scripts/finalizer-e2e.sh`) + untracked trusted report.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Trusted verification: `reports/audits/TRUSTED_35421406333.md` — **exit code 0** — `BUILD SUCCESSFUL in 20m 10s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date".
- `expo export` Android, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -r` between pristine and candidate (excluding `.git`, `node_modules`, `android`, `.expo`, `reports`, `.opencode`) shows **exactly one changed file**: `scripts/finalizer-e2e.sh` (3 insertions, 1 deletion):

1. `INSTALLED=0` initialized before the install retry loop (line 68).
2. `INSTALLED=1` set immediately after the successful `adb install -r "$apk"` and before `break` (line 74).
3. Error branch changed from `if [ "$INSTALL_ATTEMPTS" -ge "$MAX_ATTEMPTS" ]; then` to `if [ "${INSTALLED:-0}" -ne 1 ]; then` (line 83).

The error branch keeps the existing diagnostics (`adb devices -l`, `adb shell pm list packages | head -5`) and `exit 1`. The boot-wait, package-manager settle, `adb get-state` connectivity check, APK locate/size checks and the golden-path invocation (`npm run e2e:android`) are byte-identical to pristine.

This is exactly the required fix from audit RUN_35419214459: the off-by-one where a success on the final (3rd) retry attempt still triggered the error branch and exited 1.

## 4. Does the candidate address the required fix?

Yes, precisely:

- **Flag semantics**: `INSTALLED` is initialized to 0 and set to 1 only after a successful `adb install -r "$apk"`. The error branch now keys on the flag (`${INSTALLED:-0} -ne 1`) instead of the attempt counter, so a success on any attempt (1st, 2nd or 3rd) skips the error branch.
- **Diagnostics preserved**: `adb devices -l` and `pm list packages` remain in the error branch, so the next trusted finalizer run still produces the step-7 diagnostics on genuine failure.
- **Untouched parts**: boot-wait, diagnostics and golden-path are unchanged, as required.

## 5. Independent checks performed

- `bash -n scripts/finalizer-e2e.sh` → OK (valid bash).
- **Install retry loop simulation** (exact candidate loop logic, adb exit codes as inputs):
  - `(0)` → golden path reached, exit 0 ✓
  - `(1,0)` → golden path reached, exit 0 ✓
  - `(1,1,0)` → golden path reached, exit 0 ✓ ← the previously failing case is now fixed
  - `(1,1,1)` → ERROR "APK install failed after 3 attempts", exit 1 ✓
  - `(1,1,1,0)` → ERROR, exit 1 (loop bounded at MAX_ATTEMPTS=3; 4th result never consumed) ✓
- `npx jest __tests__/domain/v3-08-finalizer-wrapper.test.ts` → **6/6 PASS** (wrapper contract, single-line-form regression guard, fixture alignment).
- `git diff HEAD --stat` → only `scripts/finalizer-e2e.sh` (1 file, 3 insertions, 1 deletion); no protected control-plane file modified.

## 6. Findings

**No must-fix findings remain.** The single must-fix from RUN_35419214459 (off-by-one in the install retry loop) is fully resolved by the candidate.

## 7. Regression of previously accepted V3 invariants

No product code, domain, analytics, persistence or configuration was changed — only the CI finalizer shell script. All previously accepted criteria remain green in the trusted run:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates).

Independent spot checks: no chrono/premium/paywall/subscription UX reintroduced; the script remains an honest adb-based install check (no fake integration); the change is CI-only so there is no product cost regression (no new reads/writes/listeners/network on the user path).

## 8. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- The candidate implements the required fix exactly: explicit `INSTALLED` flag set after successful install before `break`, error branch keyed on the flag, diagnostics preserved, boot-wait/diagnostics/golden-path untouched.
- Independent simulation confirms all permutations behave correctly, including the previously failing success-on-last-attempt case.
- No must-fix remains → `accept` is possible.
- `reject` is not appropriate: the delta is coherent, honest and exactly scoped to the audited must-fix.

Decision: **accept**.