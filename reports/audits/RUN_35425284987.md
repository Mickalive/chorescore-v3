# Audit RUN 35425284987 — V3-08 (REPAIR)

## Decision: `accept`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 trusted finalizer failure from Actions run 35424139784. Inspect the exact failed release logs, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria.
- Candidate checkout: HEAD `a6300c8` (lab/chorescore-v3), detached, with 3 staged files + untracked trusted report.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Trusted verification: `reports/audits/TRUSTED_35425284987.md` — **exit code 0** — `BUILD SUCCESSFUL in 16m 35s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date".
- `expo export` Android, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Exact failed release logs — run 35424139784

Inspected via the public GitHub API (repo is public; raw step logs require admin rights, 403):

- Job `prepare`: success.
- Job `release`: **failure**.
  - Step 5 "Trusted product, privacy and cost gates": **success** (05:31:03 → 05:46:18) — includes `npm ci`, `npm run check`, `privacy:check`, `cost:check`, `expo install --check`, `expo export` android+ios, `expo prebuild`, `assembleRelease`.
  - Step 6 "Locate and hash APK": **success** (1s) — the release APK was built and located.
  - Step 7 "Android API 35 install, launch and golden path": **failure** (05:46:19 → 05:56:39, ~10m20s) — the emulator step.
  - Step 8 "upload-artifact": skipped.
- Job `reopen-on-failure`: success — correctly reopened V3-08 with the open finding.

Conclusion: the concrete blocker is the **emulator install/launch/golden-path step**; the product, privacy, cost gates and the release APK build all passed. The exact failure inside step 7 (boot timeout, install failure, or E2E assertion timeout) is not visible without admin rights, so the Builder-side repair is diagnostics + resilience — the same pattern endorsed by audit RUN_35419214459 ("the Builder-side action is exactly what this candidate does: Diagnostics — the next trusted finalizer run will now produce the actual step-7 logs").

## 4. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `node_modules`, `android`, `.expo`, `reports`, `.opencode`) shows **exactly three changed files**:

1. `scripts/e2e-android.js` (+105/−26):
   - `adb()` wrapper now takes an options object with bounded timeouts: 60s default, 180s for `install`, 30s for `shell`, 10s for `exec-out cat`, 15s for `screencap` — a hung adb can no longer hang the whole step; retries log the failure and timeout-kill signal.
   - `dumpUi()` no longer throws on dump failure: it increments a `dumpFailures` counter and returns empty nodes (with bounded logging), so a transient uiautomator failure degrades to a poll-retry instead of an immediate crash.
   - `waitFor()` detects consecutive dump failures (WARN at 5) and captures logcat on timeout for post-mortem.
   - `launch()` waits 12s (was 8s) for React Native cold start and verifies the process via `pidof`.
   - Diagnostic screenshot immediately after launch; `waitFor('Demarrer', 90000)` (was 60000).
   - Failure path captures logcat + `dumpsys activity activities` + screenshot.
2. `scripts/finalizer-e2e.sh` (+24/−1):
   - Package-manager settle raised 10s → 30s (API 35 x86_64 slow settle).
   - Device-state logging: `adb devices -l`, `sys.boot_completed`.
   - E2E exit code captured with `set +e` / `set -e` and propagated via `exit "$E2E_EXIT"`; logcat captured regardless of outcome.
3. `__tests__/domain/v3-08-finalizer-wrapper.test.ts` — comment-only correction: the guard protects against multi-line-form regression; the E2E script itself carries the diagnostic instrumentation. Accurate.

The workflow `.github/workflows/chorescore-v3-finalize.yml` is **byte-identical to pristine** (single-line `script: bash scripts/finalizer-e2e.sh`; protected control-plane file untouched).

## 5. Does the candidate address the required fix?

Yes, within Builder scope:

- **Blocker located**: the emulator step (install/launch/golden path) is the failing step; gates and APK build passed.
- **Runtime/E2E resilience**: bounded adb timeouts prevent hangs; longer cold-start tolerances (12s launch, 90s Demarrer, 30s settle) address the most plausible failure cause on API 35 x86_64 (slow boot/cold start); dumpUi graceful fallback prevents transient dump failures from aborting the golden path.
- **Diagnostics**: the next trusted finalizer run will produce the exact step-7 evidence — boot state, install attempts, per-step checkpoints, screenshots, UI dumps, logcat on timeout/failure, dumpsys activities — so any remaining failure is fully diagnosable.
- **No masking**: the golden-path assertions are byte-identical to pristine except the `Demarrer` tolerance (60s → 90s). The dumpUi empty-node fallback cannot make the golden path pass spuriously: positive `waitFor`/`findVisible` assertions still fail on timeout if the app is not in the expected state.
- **Cannot make the finalizer worse**: every change is additive robustness or diagnostics; all waits remain bounded.

## 6. Independent checks performed

- `bash -n scripts/finalizer-e2e.sh` → OK.
- `node --check scripts/e2e-android.js` → OK.
- `npx jest __tests__/domain/v3-08-finalizer-wrapper.test.ts` → **6/6 PASS** (wrapper contract, single-line-form regression guard, fixture alignment).
- Workflow YAML diff vs pristine → identical; emulator step uses `script: bash scripts/finalizer-e2e.sh`, no `script: |`.
- E2E assertion diff vs pristine → only `waitFor('Demarrer', 60000)` → `waitFor('Demarrer', 90000)`; all `waitFor`/`tapLabel`/`assertAbsent`/`findVisible`/`typeInto` assertions otherwise identical.
- Fixture alignment: `Demarrer` (app/index.tsx), `Appartement`, `Vaisselle du soir`, `Sortir les poubelles`, `Alex`, `Sam` (demoFixture.ts) all present.
- Regression spot-check: `ledgers`, `property-invariants`, `v3-07-analytics-privacy`, `cost-gates-v3-08`, `v3-08-e2e-golden-path` → **131 tests PASS**.
- `git diff --cached --stat` → 3 files; no protected control-plane file modified.

## 7. Findings

**No must-fix findings remain.** The single open finding (trusted finalizer failure at the emulator step) is addressed by the candidate: bounded adb timeouts, increased cold-start tolerances, graceful dump handling, and comprehensive diagnostics that will surface the exact step-7 failure on the next trusted finalizer run. The candidate is coherent, honest, and cannot regress the finalizer.

## 8. Regression of previously accepted V3 invariants

No product code, domain, analytics, persistence or configuration was changed — only CI E2E scripts and a test comment. All previously accepted criteria remain green in the trusted run:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates).

Independent spot checks: no chrono/premium/paywall/subscription UX reintroduced; the E2E remains an honest adb-based golden path (no fake integration); the change is CI-only so there is no product cost regression (no new reads/writes/listeners/network on the user path); logcat capture is CI-emulator-only and touches no user data.

## 9. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- The exact failed release logs were inspected: the blocker is the emulator install/launch/golden-path step; gates and release APK build passed.
- The candidate repairs the runtime/E2E blocker within Builder scope: bounded adb timeouts, increased cold-start tolerances, graceful dump handling, and diagnostics that will produce the exact step-7 evidence on the next finalizer run — consistent with the factory pattern endorsed in RUN_35419214459.
- No product code changed → no regression of V3-01..V3-07; no masking of E2E assertions; no prohibited patterns; no must-fix remains → `accept` is possible.
- `reject` is not appropriate: the delta is coherent, honest and exactly scoped to the audited blocker.

Decision: **accept**.