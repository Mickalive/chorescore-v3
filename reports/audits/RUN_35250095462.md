# Audit RUN 35250095462 — V3-06 REPAIR (sync authorization, transactional rollback, factory wiring)

- **Cycle:** 35250095462
- **Active criterion:** V3-06 (REPAIR)
- **Decision:** `accept`
- **Trusted verification:** exit code **0** (tsc + 320 tests + Android export/prebuild/native debug build)

## Summary

The candidate is a narrow repair delta (3 files, +46/−18) that resolves all three must-fix findings from RUN_35248510646:

1. **Missing import restored** — `ScopedContributionRepository` and `ScopedExpenseRepository` are re-imported in `__tests__/domain/v3-06-repair-sync-authorization.test.ts` (lines 22–23), covering all 7 instantiations in the "authorization enforced through repository path" describe block. `tsc` exits 0 and the 7 previously-failing authorization tests pass.
2. **Failure injection fixed** — the failing repo is now built with `Object.create(originalContributions)`, preserving the prototype chain (`getById`, `getByHousehold`, …) while overriding only `seed`/`create` to throw `Simulated materialization failure`. The production `MaterializingSyncState.applyDeltas → materializeDeltas → upsertWithConflictResolution` path now reaches the intended error. The business-table snapshot was also moved to **after** the valid pull so the rollback baseline matches the state the failing transaction restores to. The test still asserts the cursor did not advance (≤ 1) and all 7 business tables are unchanged.
3. **MaterializingSyncState wired in the SQLite test** — the class is now exported from `RepositoryFactory.ts` (export keyword only, no behavior change) and the factory-wiring test wraps `baseSyncState` exactly as the factory does (`new MaterializingSyncState(baseSyncState, () => repos); repos.syncState = materializingSync`), passing the wrapped state to `pullDeltas` and `pushDeltas`. The remote delta now materializes into the business table (`entry.label === 'Remote via factory'`, `entry.value === 30`), the local write appears in `getDirtyRecords`, and `pushDeltas` sends it.

Independent re-runs confirm the trusted result: `npx tsc --noEmit` exits 0; `npx jest` passes 18 suites / 320 tests; the three previously-failing test groups all pass; all V3-01..V3-05 regression suites pass.

## Independent verification performed

| Check | Result |
|---|---|
| `npx tsc --noEmit` | **PASS** (exit 0) |
| `npx jest` (full suite) | **PASS** — 18 suites / 320 tests, 0 failed |
| `npx jest` — "authorization enforced through repository path" | 7/7 pass (previously 7 ReferenceError failures) |
| `npx jest` — "failing materialization rolls back" | 2/2 pass (previously wrong error message) |
| `npx jest` — "factory-wired SQLite repos" | 1/1 pass (previously `entry` null) |
| V3-01..V3-05 suites | all pass (16 suites, 236 tests) |
| Diff vs pristine accepted tree (ff64f55) | exactly 3 files, +46/−18 staged; no other product code touched |
| Trusted verification | exit 0; Android export + prebuild + native debug build `BUILD SUCCESSFUL in 19m 28s` |

## Must-fix findings

None. All three must-fix findings from RUN_35248510646 are resolved:

### 1. Missing import of `ScopedContributionRepository`/`ScopedExpenseRepository` — RESOLVED

- Import restored at lines 22–23; all 7 usages (lines 639, 654, 674, 689, 709, 726, 747) resolve; both classes remain exported from `ScopedRepositoryFacade.ts` (lines 168, 226).
- Verification: `tsc` exit 0; the 7 authorization tests pass.

### 2. Failure-injection test broken (spread lost prototype methods) — RESOLVED

- `Object.create(originalContributions)` preserves the prototype chain; only `seed`/`create` are overridden to throw `Simulated materialization failure`; the repo is restored after the pull.
- The snapshot was moved after the valid pull — a correctness improvement: the rollback restores to the state at the start of the failing transaction, which includes the valid pull's committed `c-ok`.
- Verification: test passes with the intended error message and still asserts cursor ≤ 1, failed record absent, all 7 business tables unchanged, expense count unchanged.

### 3. SQLite factory-wiring test passed raw sync state to `pullDeltas` — RESOLVED

- `MaterializingSyncState` exported from `RepositoryFactory.ts`; the test wraps `baseSyncState` exactly as the factory does and passes the wrapped state to `pullDeltas`/`pushDeltas`.
- Verification: remote delta → business record readable (`label === 'Remote via factory'`, `value === 30`); local write → `getDirtyRecords` returns the payload; `pushDeltas` sends the local payload.

## V3-06 acceptance criteria — verified present

- `applyDeltas` materializes pulled records into all 8 business collections inside `withTransaction` (`MaterializingSyncState.applyDeltas`, RepositoryFactory.ts lines 228–288).
- `storeLocalRecords` has callers; `getDirtyRecords` returns real payloads; `pushDeltas` sends actual local writes (tested in-memory and SQLite-wired).
- Group-options UI reachable from group settings: Minutes|Points toggle with provenance warning, compensation toggle + rate (`app/group-options.tsx`).
- Invitation UI: invite action with share sheet (`app/invite.tsx`), `/join/:token` deep-link route, atomic acceptance (membership + member + invitation status in one transaction, `app/join/[token].tsx` lines 130–142).
- Authorization rules enforced in the data-access path: tenant isolation on reads/writes (`authorizationRules.ts`, scoped repos), deterministic conflict resolution replacing blind LWW (`didRemoteWin`: revision → timestamp, `SyncMaterializer.ts`).
- Hostile tests: cross-tenant read blocked (`CROSS_TENANT`), balance falsification rejected (`assertLedgerWrite`), concurrent same-id edits resolved deterministically (revision → timestamp → id).
- End-to-end sync tests: remote delta materializes into business data; local write appears in `getDirtyRecords` and is pushed.
- No N+1 or listener massif: no-N+1 test (2 bulk reads for 50 members + 200 contributions), tab-switch budget 0 reads / 0 network.
- Remote reads/writes instrumentable and bounded: `costInstrumentation.ts` budgets `sync-delta` (≤ 8 reads / 8 writes / 2 network calls) and `open-household` (≤ 5 reads / 0 network).
- No regression V3-01 through V3-05: all 16 non-candidate suites pass.

## Non-blocking observations (carried over, unchanged by this delta)

- **`rawRepos` on AppContext** (lines 64–66, 254): the raw unscoped set is exposed to every screen; only the join flow consumes it. A dedicated join use-case would be tighter. Not blocking.
- **`InMemoryInvitationRepository` not snapshotted** in `withTransaction`: the join transaction would leave an invitation status change if a later step throws. Outside the objective's explicit repo list. Not blocking.
- **`ScopedInvitationRepository.getPendingByEmail` unscoped** (lines 615–617): latent leak of invitations (including `linkToken`) for any email; no app caller today. Not blocking.

## Conclusion

The candidate resolves all three must-fix findings from the previous audit with a minimal, surgical delta: the import is restored, the failure injection preserves the prototype chain and asserts cursor + all business tables unchanged, and the SQLite test now exercises the real `MaterializingSyncState` factory wiring with all three required assertions. Trusted verification passed (exit 0) including the full jest suite and the Android export/prebuild/native debug build. Independent re-runs confirm tsc exit 0 and 320/320 tests passing with no regression in V3-01..V3-05. No must-fix finding remains. Decision: **accept**.