# Audit RUN 35686566515 — V3-08 repair: Demarrer timeout 900s → 1080s (ACCEPT)

- **Cycle:** 35686566515
- **Role:** builder (independent audit)
- **Active criterion:** V3-08 (Finition, coût et release mobile)
- **Objective:** REPAIR V3-08 trusted finalizer failure from Actions run 35684869621. Inspect the exact failed release logs, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria.
- **Decision:** `accept`
- **Trusted verification exit code:** 0

---

## 1. Candidate delta (vs pristine accepted tree)

Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`. The candidate delta is **exactly two files** (staged), on top of HEAD `5a774c7` ("release 35684869621: reopen V3-08 repair"):

1. `scripts/e2e-android.js` — **+16 / −7**: the `waitFor('Demarrer', ...)` timeout is raised from `900000` to `1080000` (900s → 1080s), with comments updated to document the stuck-detector timing math (450s grace + 8 dumps × 30-60s = 690-930s fire point; 900s was insufficient; 1080s leaves 150s headroom).
2. `__tests__/domain/v3-08-finalizer-wrapper.test.ts` — **+4 / −1**: the contract test now expects `waitFor('Demarrer', 1080000, { graceMs: COLD_START_GRACE_MS })` with an explanatory comment.

Everything else (`app/`, `src/`, remaining `__tests__/`, remaining `scripts/`, governance, directives, docs, workflows) is byte-identical to the pristine tree. The only untracked file is the trusted verification log.

## 2. The blocker and the repair rationale: SATISFIED

The trusted finalizer run 35684869621 failed at the Android API 35 install/launch/golden-path step. The exact step logs and the diagnostics artifact are not retrievable without admin rights (consistent with prior cycles). The candidate repairs the concrete E2E blocker with a timing analysis that is fully verifiable against the accepted baseline code:

- `COLD_START_GRACE_MS = 450_000` (450s) — global cold-start grace from app launch.
- `STUCK_APP_THRESHOLD = 8` consecutive empty dumps before the stuck detector force-stops + relaunches.
- Each uiautomator dump takes **30-60s** on degraded API 35 x86_64 emulators (documented in the script).
- The stuck detector therefore fires at **450 + 8×30 = 690s** (best case) to **450 + 8×60 = 930s** (worst case).
- The previous Demarrer timeout was **900s < 930s**: in the worst case the stuck detector fired **after** the timeout had already expired, so the force-stop + relaunch recovery never ran inside the waitFor, and the waitFor always timed out → E2E failure → finalizer failure.
- The fix raises the timeout to **1080s > 930s**, leaving **150s** for the recovery (force-stop 2s + monkey relaunch 15s + relaunched-app cold start), and fits within the GitHub Actions default step timeout (360 min).

Independent timing simulation confirms:

| Scenario | Outcome |
|---|---|
| Old timeout 900s, worst-case stuck fire at 930s | **TIMEOUT before recovery** (root cause) |
| New timeout 1080s, worst-case stuck fire at 930s | **150s headroom** for recovery |
| New timeout 1080s vs GitHub Actions step timeout | 18 min ≪ 360 min default |

The waitFor still requires the Demarrer button to actually appear — the timeout increase does not skip any assertion and does not mask a genuinely stuck app (the stuck detector still fires and recovers).

## 3. Correctness and consistency

- The contract test and the script agree exactly: `waitFor('Demarrer', 1080000, { graceMs: COLD_START_GRACE_MS })`.
- All other golden-path waitFor timeouts (Appartement 600s, tabs 300s, etc.) are unchanged.
- All comments referencing the timeout were updated consistently (900s → 1080s) in the touched regions.
- `node --check scripts/e2e-android.js` passes.
- The change is confined to E2E test tooling; no product, ledger, sync, analytics, privacy, theme, chrono, premium/paywall, invitation or cost behavior is modified.

## 4. Trusted verification (authoritative)

`reports/audits/TRUSTED_35686566515.md` — **exit code 0**:

- `npm run check`: typecheck clean; **454 tests / 22 suites pass** (incl. `v3-08-finalizer-wrapper` and `v3-08-e2e-golden-path`).
- `npm run privacy:check`: **71 privacy tests pass**; static privacy release contract OK.
- `npm run cost:check`: **21 cost-gate tests pass**.
- `expo export` (android): OK. `expo prebuild` + `gradlew :app:assembleDebug`: **BUILD SUCCESSFUL** (20m 18s).

## 5. Independent verification performed

| Check | Result |
|---|---|
| Trusted log exit code | 0 (typecheck, 454 tests, privacy 71, cost 21, export, prebuild, assembleDebug) |
| Delta scope | `git diff --cached` = exactly `scripts/e2e-android.js` (+16/−7) and `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (+4/−1); `git status` shows only the two staged files + untracked trusted log |
| Root cause math | Stuck detector fires at 450s + 8×dump(30-60s) = 690-930s; old 900s timeout < 930s worst case → recovery fired after timeout |
| Fix math | 1080s > 930s → 150s headroom; fits within default 360-min step timeout |
| Syntax | `node --check scripts/e2e-android.js` OK |
| Contract tests | `npx jest __tests__/domain/v3-08-finalizer-wrapper.test.ts` — 24/24 pass |
| Product code untouched | `diff -rq` vs pristine: only `scripts/e2e-android.js` and its contract test differ |
| Regression scan | no V3-01..V3-07 invariant touched; all gates pass |

## 6. Findings

All findings are **non-mustFix**:

1. **Exact failure logs not retrievable** — the repair is based on the documented stuck-detector timing analysis plus step-timing evidence, not a proven reproduction from the exact logs of run 35684869621 (artifacts/step logs require admin rights, consistent with prior cycles). The next trusted finalizer dispatch will exercise the repaired E2E on a real API 35 emulator; the accepted mid-wait diagnostics will surface the exact failure point if the blocker persists.
2. **Stale comment in the contract test** (line 330: "Not more than the Demarrer timeout" referencing 900_000) — the assertion `graceMs <= 900_000` still passes (450s ≤ 900s ≤ 1080s); cosmetic only.
3. **Recovery budget after stuck detection is 133s** (1080 − 930 − 17) for the relaunched app's cold start, below the documented 240s first-cold-start worst case. A second cold start is typically faster (warm caches), and the previous 900s timeout guaranteed failure in the 60s-dump scenario, so this is a tuning judgment; the mid-wait diagnostics will pinpoint any remaining issue.

## 7. Conclusion

**ACCEPT.** The candidate repairs the concrete E2E blocker for the trusted finalizer failure: the Demarrer waitFor timeout (900s) expired before the stuck-app detector could fire (930s worst case), so the force-stop + relaunch recovery never ran inside the waitFor. Raising the timeout to 1080s gives the recovery 150s of headroom and fits within the GitHub Actions step timeout. The delta is confined to the E2E test script and its contract test; all product code and accepted criteria are preserved, and the trusted verification passed with exit code 0 (454 tests, privacy 71, cost 21, expo export, prebuild, BUILD SUCCESSFUL). No must-fix remains.