# Audit RUN_35182865593 — V3-04 Balances, currency-only edit detection and claim #8 (REPAIR)

**Cycle:** 35182865593
**Criterion:** V3-04 — Balances, périodes, vues matérialisées et compensation explicite (REPAIR)
**Decision:** **accept**
**Trusted verification:** exit 0 (TRUSTED_35182865593.md) — typecheck, 180 tests green, Expo export Android, prebuild, native debug build `BUILD SUCCESSFUL`
**Role:** builder (per directive schema)

## Candidate delta (vs pristine accepted tree `/home/runner/work/_temp/accepted-pristine`)

- `app/(tabs)/balances.tsx` (+1): `oldEntry.currency !== newEntry.currency` added to the expense modification detection (line 328).
- `__tests__/domain/cost-gates-v3-04.test.ts` (+209/−2): claim #8 reworded `Screen-level` → `Data-level`; three new tests in a `currency-only expense edit triggers remove-old + apply-new` describe block; the bounded-reads describe block renamed to `data-level delta refresh with bounded repo calls`.

Working tree otherwise clean (only the untracked trusted verification report). No governance, workflow, state, directive or report files modified by the builder. Independent re-run: `npm run check` clean — tsc typecheck passes, 13 suites / **180/180 tests green**.

## Must-fix finding 1 — currency in expense modification detection: RESOLVED

The prior audit found that the Balances delta handler compared `amountMinor`, `paidByMemberId`, `participantMemberIds`, `splitMode` and `customShares` but not `currency`, so a currency-only edit (CHF→EUR) was classified as unmodified: the old CHF effect was never removed and the new EUR effect never applied, leaving the all-time `moneyByCurrency` snapshot stale vs full replay.

**Code fix verified by inspection.** The condition at balances.tsx 326-333 now includes `oldEntry.currency !== newEntry.currency`. All ledger-affecting `ExpenseEntry` fields are compared: `amountMinor`, `currency`, `paidByMemberId`, `participantMemberIds`, `splitMode`, `customShares`. Non-ledger fields (`title`, `note`, `category`, `occurredAt`) are correctly excluded — they do not affect the all-time money snapshot (period views recompute from the persisted entries, which the delta handler does not need to touch). The scenario is reachable: the Add-tab edit form exposes an editable currency input (add.tsx 891-894) and persists it (add.tsx 532).

**Tests verified.** Three new tests (lines 959-1164):

1. *Currency-only edit (CHF→EUR) matches full replay* — builds the all-time snapshot with a CHF 2000 equal-split expense, applies remove-old + apply-new, and asserts: CHF zeroed (a=0/b=0), EUR created (a=+1000/b=−1000), delta EUR == `calculateFinancialBalancesByCurrency` full replay, `financialLedgerIsZeroSum(EUR)` true.
2. *Amount+currency edit matches full replay* — CHF 2000 → EUR 3000, delta == full replay (a=+1500/b=−1500), zero-sum holds.
3. *Currency detection in balances.tsx matches full replay* — simulates the exact balances.tsx detection condition (including the currency comparison) and asserts the delta snapshot equals full replay with CHF zeroed.

**Mutation probe (independent).** I removed the currency comparison from the simulated condition in test 3 and re-ran it: the test fails with `deltaEur` undefined (the EUR effect is never applied). The tests genuinely guard the currency-detection logic. Probe file removed afterward; working tree restored.

## Must-fix finding 2 — screen-level claim #8: RESOLVED (option b)

The prior audit required either a real screen-level test importing `app/(tabs)/balances.tsx` (option a) or rewording claim #8 to an explicitly data-level/handler claim the tests actually prove (option b). The candidate took option (b):

- Header claim #8 now reads: **"Data-level: delta handler re-reads only the changed collection, proving bounded repo calls (no full re-read on focus or signal)"**.
- The tests at lines 1166-1276 substantiate it with repository read counts: after a contribution data-change signal, only contributions are re-read (expenses/settlements untouched); totals bounded at contributions=3, expenses=1, settlements=1; snapshot equals full replay after each delta.
- No `Screen-level`/`screen-level` wording remains in the file (grep verified).
- The actual screen behavior matches the claim: `useFocusEffect` (balances.tsx 429-438) full-reloads only when the household changed (`lastHouseholdIdRef.current !== currentHouseholdId`); otherwise the data-change signal drives incremental delta updates.

## Regression and invariant checks (independent)

- **Trusted verification:** exit 0 — typecheck, 180 tests, Expo export, prebuild, native debug `BUILD SUCCESSFUL`.
- **Independent re-run:** `npm run check` — typecheck clean, 13 suites / 180 tests green.
- **V3-01/V3-02/V3-03 regression suites green:** ledgers, property-invariants, materialized-balances, cost-gates-v3-03, contribution-expense-crud, merged-pagination, activity-log, validation-preconditions-periods, repositories, ports-adapters, pagination, design-system.
- **Delta scope:** exactly 2 files vs pristine; no governance/directives/state/report files touched by the builder.
- **No forbidden patterns:** no chrono, premium, paywall, subscription, entitlement, upsell, streak, confetti, V2 warm/self-care copy, cross-currency netting, silent unit conversion or destructive reset in the changed files.
- **Accounting integrity:** zero-sum asserted per currency; integer minor units; Minutes/Points untouched; each currency kept separate in `moneyByCurrency` (no cross-currency netting).
- **Cost profile:** the fix adds one field comparison to the modification detection — no new reads/writes, no action cost proportional to history; tests use repository call counts, not wall-clock time.

## Non-blocking observations

1. **Settlement exactly-once test still not strengthened** (carried from RUN_35179481461, marked non-blocking, outside this cycle's task scope): the test applies the delta once directly and never simulates `emitDataChange('settlement')`/`handleDataChange`. The code path was independently verified in the prior cycle and remains correct. Recommended hardening: spy on `emitDataChange` or assert idempotence of a second application.
2. **Currency tests duplicate the detection condition** rather than importing the actual balances.tsx handler, so they would not directly fail if the real code regressed. Consistent with the file's data-level approach and the task's option (b); the code fix is verified by direct inspection and the mutation probe confirms the tests guard the detection logic as written. Optional hardening: extract the modification-detection predicate into a shared domain helper used by both the screen and the tests.

## Decision

**accept.** Trusted verification passed (exit 0) and no must-fix finding remains. Both prior must-fix findings are genuinely resolved: (1) currency is now part of the expense modification detection with tests proving the currency-only edit path (old currency zeroed, new currency created, snapshot == full replay, zero-sum per currency); (2) claim #8 is reworded to an explicitly data-level claim that the tests actually prove via bounded repository read counts. The candidate is a coherent, minimal repair delta and may be integrated as the accepted V3-04 baseline.