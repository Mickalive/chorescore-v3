# Audit RUN 35341822850 — V3-08 (REPAIR)

## Decision: `repair`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — trusted shell (sole owner of `.github/workflows/` per AGENTS.md) must, on `main`, replace the emulator step's multi-line `script: |` block with the single-line `script: bash scripts/finalizer-e2e.sh`, then re-run the trusted finalizer. The Builder-scoped single-shell wrapper already exists and is preserved; no product code change is needed.
- Candidate checkout: current HEAD `97186ef` (lab/chorescore-v3).
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Trusted verification: `reports/audits/TRUSTED_35341822850.md` — **exit code 0**.

## 2. Trusted verification (exit 0)

The trusted log shows, all green:

- `npm run check` → typecheck + 22 suites / 431 tests PASS (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests: 50k entries bounded reads, tab-switch 0 reloads, delta sync, bounded writes, classification cache, privacy gate, budgets, offline, golden path, accessibility).
- `expo export` Android + iOS, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL.
- Final line: `Trusted verification exit code: 0`.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate shows **no product code changes**:

- `src/`, `__tests__/`, `scripts/`, `app/`, `package.json`, `package-lock.json` — identical.
- Only build artifacts differ (`.expo/`, `android/build`, `.opencode/`, the new TRUSTED report).

This matches the objective: no product code change is needed.

## 4. Builder-scoped wrapper (preserved and valid)

- `scripts/finalizer-e2e.sh` exists; `bash -n` passes; contains `#!/usr/bin/env bash`, `set -euo pipefail`, APK locate under `android/app/build/outputs/apk/release`, empty-APK guards, `adb install -r "$apk"`, `npm run e2e:android` — all in one bash process.
- `scripts/e2e-android.js` exists; `node --check` passes; auto-locates the release APK, installs only when absent, asserts the three V3 tabs and demo fixture labels, asserts absence of chrono/premium/warm-V2 UI.
- `package.json` exposes `e2e:android = node scripts/e2e-android.js`.
- Guarded by `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (PASS in trusted run).

## 5. Regression of previously accepted V3 invariants

All previously accepted criteria remain green in the trusted run:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS — Operational Store / Research Analytics Plane separated, no operational IDs or free text in outputs, classification cache, no synchronous LLM, checkpointed incremental pipeline, consent/retention/query budget/DP/buyer contracts.
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates).

Independent spot checks confirmed: no `Chrono`/`Chronometre`/`stopwatch` in `src/` or `app/`; no Premium/paywall/subscription/entitlement UX.

## 6. MUST-FIX finding — NOT resolved (trusted-shell owned)

The prior cycle's must-fix finding remains open:

- `git show origin/main:.github/workflows/chorescore-v3-finalize.yml` lines 90-94 **still contain** the multi-line block:

  ```yaml
  script: |
    set -euo pipefail
    apk=$(find android/app/build/outputs/apk/release -type f -name '*.apk' | head -1)
    adb install -r "$apk"
    npm run e2e:android
  ```

- The last commit touching this workflow on `main` is `8b245b2` (2026-09-16) — the documented one-line fix was never applied.
- GitHub API: finalizer run `35333103534` (run 16) → `conclusion=failure`, step 7 "Android API 35 install, launch and golden path" `conclusion=failure`; `reopen-on-failure` ran; **no subsequent finalizer run exists**.
- `docs/RELEASE_STATUS.json` still shows V3-08 `in_progress` with the same mustFix open finding; no `android-api35-install-launch-e2e-green` attestation.

This is a trusted-shell action (`.github/workflows/` is trusted-shell owned per AGENTS.md); the Builder cannot and did not touch it. The Builder-scoped half (wrapper script + tests) is complete and preserved.

## 7. Decision rationale

- Trusted verification passed (exit 0): satisfied.
- No mustFix remains: **not satisfied** — the workflow on `main` is still broken and the trusted finalizer has not been re-run to attestation.

Per the acceptance rule ("Accept iff trusted verification passed AND no mustFix remains"), the decision is **`repair`**: the candidate is a coherent, clean baseline (no product delta, all gates green) and must be retained as WIP; the next cycle must ensure the trusted shell applies the one-line workflow fix on `main` and re-runs the trusted finalizer.