# Audit RUN 35414682827 — V3-08 (REPAIR)

## Decision: `repair`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 trusted finalizer failure from Actions run 35413347057 — inspect the exact failed release logs, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, preserve all accepted criteria.
- Candidate checkout: HEAD `a294922` (lab/chorescore-v3), detached.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Trusted verification: `reports/audits/TRUSTED_35414682827.md` — **exit code 0** — `BUILD SUCCESSFUL in 21m 57s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date" (F1 from the previous cycle resolved).
- `expo export` Android + iOS, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `reports`, `.expo`, `node_modules`, `android`, `.opencode`) shows **exactly one changed file**:

- `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (+24 lines): a new describe block "V3-08 finalize workflow YAML regression guard" that reads `.github/workflows/chorescore-v3-finalize.yml`, locates the `uses: reactivecircus/android-emulator-runner@v2` step, and asserts the emulator step contains `script: bash scripts/finalizer-e2e.sh` and does not match `/script:\s*\|/`.

The test passes (`npx jest __tests__/domain/v3-08-finalizer-wrapper.test.ts` → 6/6 PASS). The workflow YAML parses; the emulator step uses the single-line form. No product code, script, config or app change.

## 4. Independent investigation of run 35413347057 (the failure this cycle must repair)

I queried the GitHub API directly (repo is public):

- **Run 35413347057**: `head_sha = 0f36b1fdfb69d` — the commit "Fix V3 finalizer emulator shell". The workflow at that commit **already contains** `script: bash scripts/finalizer-e2e.sh` (single-line form). The multi-line `script: |` block was fixed **before** this run started (commit date 2026-09-19T01:04:20Z; run created 01:39:53Z).
- Job `release` (id 105817142401): steps 5 (Trusted product, privacy and cost gates) and 6 (Locate and hash APK) **succeeded**; step 7 "Android API 35 install, launch and golden path" **failed** — annotation: `The process '/usr/bin/sh' failed with exit code 1`, duration 01:56:21Z → 02:06:21Z (~10 min).
- **Conclusion: the multi-line script form was NOT the cause of run 35413347057's failure.** The Builder's regression-guard comment ("Regression guard for Actions run 35413347057: the multi-line `script: |` form … exits 2 before the APK install and golden path ever run") is factually incorrect for this run.

### Historical pattern (all actual finalizer runs)

| Run | Form | Release job | Emulator step |
|---|---|---|---|
| 35293489693 (11) | multi-line | executed | FAILED |
| 35302658175 (12) | multi-line | executed | FAILED |
| 35315519533 (14) | multi-line | executed | FAILED |
| 35333103534 (16) | multi-line | executed | FAILED |
| 35413347057 (22) | **single-line** | executed | **FAILED (exit 1)** |
| 35290093639, 35280202077, 35308123555, 35328441596, 35351344961, 35373075367, 35387142937, 35398964518, 35406890615 (9,10,13,15,17-21) | — | **skipped** | never ran |

The "successful" schedule runs never executed the release job (`pendingArtifact` not set), so they provide no evidence that the emulator step ever passed. **Every actual finalizer run has failed at the emulator step — 5 out of 5.**

I verified the multi-line mechanism from the action source (`reactivecircus/android-emulator-runner@v2`): `src/script-parser.ts` line-splits the script input and `src/main.ts` executes each line via `sh -c` — dash rejects `set -euo pipefail` (exit 2). That explains runs 11/12/14/16. Run 22 used the single-line form (one `sh -c "bash scripts/finalizer-e2e.sh"`), so its exit 1 has a **different root cause**: emulator boot timeout/flake, `adb install -r` failure, or an E2E golden-path failure in the release APK. The step logs require admin rights and were not accessible; the exact cause remains undetermined.

## 5. Does the candidate fix the concrete blocker?

**No.** The candidate changes nothing that affects the emulator step, `scripts/finalizer-e2e.sh`, `scripts/e2e-android.js`, `package.json` or the app. The regression guard only locks in the single-line form that was already in place when run 35413347057 failed. Re-running the trusted finalizer with this candidate would still fail at the emulator step (unless the failure is purely transient infrastructure, which cannot be assumed and was not demonstrated).

The mustFix in `docs/RELEASE_STATUS.json` ("Trusted V3 finalizer failed — GitHub Actions finalizer run 35413347057") therefore **remains open**.

## 6. Regression of previously accepted V3 invariants

All previously accepted criteria remain green in the trusted run; no new violations detected:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding (minor units), Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates).

Independent spot checks: no `Chrono`/chrono in `src/`/`app/`, no Premium/paywall/subscription/entitlement UX, no warm V2 design drift; demo fixture (`src/features/app/demoFixture.ts`) seeds the labels the E2E asserts (`Appartement`, `Vaisselle du soir`, `Sortir les poubelles`, `Alex`, `Sam`); `scripts/finalizer-e2e.sh` and `scripts/e2e-android.js` are real, honest single-shell wrapper + adb golden path.

## 7. MUST-FIX finding — NOT resolved

- **Path:** release — trusted finalizer emulator step (run 35413347057, step 7).
- **Problem:** the candidate misattributes the failure to the multi-line `script: |` form, which was already fixed before the run (head_sha 0f36b1f). The run failed with exit 1 after ~10 minutes; the concrete blocker is unfixed and the trusted finalizer cannot pass.
- **Evidence:** GitHub API run metadata (head_sha 0f36b1f), job 105817142401 step 7 failure + annotation "The process '/usr/bin/sh' failed with exit code 1", step duration 01:56:21Z→02:06:21Z, steps 5/6 success; all 5 actual finalizer runs failed at the same step; successful schedule runs had the release job skipped; candidate delta is only the regression guard test.
- **Required fix:** obtain the actual step-7 logs of run 35413347057 (or re-run the finalizer with diagnostics) and fix the concrete blocker: emulator boot timeout/flake, `adb install -r` failure, or E2E golden-path failure in the release APK. Preserve the regression guard but correct its root-cause comment. Do not modify protected control-plane files (the workflow fix is already in place).
- **Verification:** trusted finalizer must pass the Android API 35 emulator step (release APK install, launch, golden path) with exit 0, and `docs/RELEASE_STATUS.json` must contain `android-release-apk-sha256-<sha>` and `android-api35-install-launch-e2e-green` in V3-08 evidence.

## 8. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- Candidate delta is coherent and passes: the regression guard is a valid static contract check and is preserved as WIP baseline.
- The mustFix (trusted finalizer failure) is **NOT resolved**: the candidate's root-cause attribution is factually wrong for run 35413347057 and nothing in the delta affects the failing emulator step.
- `accept` is not possible: a mustFix remains.
- `reject` is not appropriate: the delta is coherent, harmless and worth preserving; the next cycle should repair the existing baseline rather than rebuild.
- Decision: **repair**.