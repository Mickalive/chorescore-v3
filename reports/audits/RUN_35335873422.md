# Audit RUN 35335873422 — V3-08 repair: finalizer failure NOT repaired, lockfile-only delta

## Decision: `repair`

The candidate is **coherent** (trusted verification exit 0, no regression) and is retained as WIP baseline, but the **trusted finalizer failure from Actions run 35333103534 is NOT repaired**. The candidate delta is only `package-lock.json` (transitive dependency bumps); no product, privacy, cost, APK, runtime, E2E or readiness code changed. The failure is at the finalizer's Android emulator step, whose root cause is a protected workflow file that the Builder cannot edit. A mustFix remains, so the decision cannot be `accept`.

## Context

- Active criterion: **V3-08 — Finition, accessibilité, coût et release mobile** (REPAIR).
- Objective: `REPAIR V3-08 trusted finalizer failure from Actions run 35333103534. Inspect the exact failed release logs, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria.`
- Trusted verification: **exit 0** — `reports/audits/TRUSTED_35335873422.md` (typecheck, 22 suites / 431 tests, privacy 71, cost 21 / 10 gates, expo install --check up-to-date, export, prebuild, Gradle BUILD SUCCESSFUL in 20m43s).
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine` (HEAD `ca20522`).
- Candidate delta vs pristine: **only `package-lock.json`** (6 transitive dependency bumps).

## Exact failed release logs (run 35333103534)

GitHub API `actions/runs/35333103534/jobs` (public repo, no admin log download needed for step conclusions):

| Job / step | Result |
|---|---|
| `prepare` | success |
| `release` step 1-4 (setup, checkout, node, java) | success |
| `release` step 5 — Trusted product, privacy and cost gates (npm ci, check, privacy:check, cost:check, expo install --check, expo export android + ios, prebuild, `gradlew :app:assembleRelease`) | **success** |
| `release` step 6 — Locate and hash APK | **success** |
| `release` step 7 — Android API 35 install, launch and golden path | **failure** |
| `reopen-on-failure` | success (reopened V3-08, dispatched factory) |
| `finalize-success` | skipped |

The failure is exactly the emulator step. Root cause (documented in `reports/director/RUN_35325991233.md` F2 and proven in cycle 35304642494): `reactivecircus/android-emulator-runner@v2` line-splits the multi-line `script: |` block and executes each line via its own `sh -c`; on Ubuntu `/bin/sh` is dash, which rejects `set -euo pipefail` (exit 2), and shell-local variables are lost between lines. The E2E golden path never runs. This is the same root cause as finalizer runs 35293489693, 35302658175 and 35315519533.

The workflow on `main` (`2fa7ba3`) and on `lab/chorescore-v3` still contains the multi-line `script: |` block at lines 90-94 — the trusted shell never applied the documented one-line fix (`script: bash scripts/finalizer-e2e.sh`).

## Candidate delta

- `package-lock.json` — 6 transitive dependency bumps from `npm install`:
  - `@types/node` 22.20.3 → 26.6.1 (major)
  - `undici-types` 6.21.0 → 8.9.0
  - `baseline-browser-mapping` 2.11.24 → 2.11.25
  - `electron-to-chromium` 1.5.430 → 1.5.431
  - `is-core-module` 2.16.2 → 2.17.0
  - `node-releases` 2.0.55 → 2.0.56
- `src/`, `scripts/`, `__tests__/`, `app/`, `package.json` — **byte-identical to pristine** (`diff -rq` confirmed).
- No change to `.github/workflows/` (protected control-plane; Builder cannot edit).
- WIP preserved: `scripts/finalizer-e2e.sh` (bash -n OK), `scripts/e2e-android.js` (node --check OK), `__tests__/domain/v3-08-finalizer-wrapper.test.ts` (passes).

## Why this is repair, not accept

Per audit instructions: `Accept iff the trusted verification passed AND no mustFix remains.`

- Trusted verification **passed** (exit 0) — mandatory condition satisfied.
- A mustFix **remains**: the open finding `Trusted V3 finalizer failed` (`docs/RELEASE_STATUS.json`, mustFix: true) is not resolved by this candidate. The failure is at the emulator step of the protected workflow; the candidate does not touch the workflow or the e2e scripts, and the Builder is forbidden from editing workflows (`AGENTS.md`, `candidate.sh` protected-path check).
- Unlike cycles 35310569665 (created `scripts/finalizer-e2e.sh`) and 35325991233 (fixed expo alignment), this cycle delivers **no Builder-scoped fix** — only lockfile churn unrelated to the failure.

The candidate is coherent and verified green, so per governance (`Un candidat repair cohérent est conservé comme baseline WIP`) it is kept as WIP baseline rather than rejected.

## Independent checks

| Check | Result |
|---|---|
| Trusted verification exit code | **0** — typecheck, 431 tests, privacy 71, cost 21, expo check up-to-date, export, prebuild, Gradle BUILD SUCCESSFUL |
| Failed finalizer run 35333103534 | steps 1-6 success; **step 7 (emulator) failure**; reopen-on-failure success |
| Workflow root cause | `script: \|` multi-line still present on main and lab — unchanged |
| Candidate delta vs pristine | only `package-lock.json`; src/scripts/tests/app/package.json identical |
| Lockfile consistency | `npm ci --dry-run` exit 0; trusted verification `npm ci` (862 packages) OK |
| WIP preservation | `finalizer-e2e.sh` bash -n OK; `e2e-android.js` node --check OK; wrapper test passes |
| Regression sweep V3-01..V3-07 | 431 tests PASS — zero-sum ledgers, integer minor units, immutable rate snapshot, Minutes/Points distinct, materialized balances, revision/tombstone, sync/auth/delta, privacy pipeline |
| Cost gates V3-08 | 21 tests / 10 gates PASS (50k bounded, tab 0, delta sync, 1-write, 10k cache, privacy gate, offline, golden path, WCAG AA) |
| Prohibited patterns | none introduced (lockfile-only delta) |
| Builder scope boundary | workflow fix is trusted-shell-owned; no Builder cycle can deliver it |

## Findings

### F1 (mustFix: true) — Trusted finalizer failure not repaired by this candidate

- **Path:** `.github/workflows/chorescore-v3-finalize.yml` (emulator step, lines 90-94) — root cause; candidate delta is `package-lock.json` only.
- **Problem:** Finalizer run 35333103534 failed at step 7 "Android API 35 install, launch and golden path". All product/privacy/cost gates, iOS export, release APK build and APK hashing succeeded (steps 1-6). The emulator step's multi-line `script: |` block is line-split by `android-emulator-runner@v2` into `sh -c` (dash) invocations that reject `set -euo pipefail` (exit 2) before the APK install/golden path runs. Same root cause as finalizer runs 35293489693, 35302658175, 35315519533. The workflow on main and lab still has the multi-line block; the candidate does not touch it.
- **Evidence:** GitHub API run 35333103534 jobs (step 7 failure, steps 1-6 success); `git show origin/main:.github/workflows/chorescore-v3-finalize.yml` still `script: |`; `candidate.patch` contains only package-lock.json; `diff -rq` shows src/scripts/tests/app/package.json identical to pristine; `scripts/finalizer-e2e.sh` exists but is not invoked.
- **Required fix:** Trusted shell (sole owner of `.github/workflows/` per AGENTS.md): on `main`, replace the emulator step's multi-line `script: |` block with `script: bash scripts/finalizer-e2e.sh`, then re-run the trusted finalizer. The Builder-scoped single-shell wrapper already exists and is preserved; no product code change is needed.
- **Verification:** Finalizer passes the Android API 35 emulator step (release APK install, launch without Metro, golden path Ajouter/Balances/A faire) with exit 0 and attests `android-release-apk-sha256-<sha>` + `android-api35-install-launch-e2e-green`.

### F2 (informational, not mustFix) — Candidate delta is lockfile-only churn

- **Path:** `package-lock.json`
- **Problem:** The only product-tree change is 6 transitive dependency bumps from `npm install` (including a major `@types/node` 22→26 bump). These do not address the finalizer failure and add unnecessary churn. Harmless and verified green; retained as WIP baseline per repair semantics.
- **Evidence:** `candidate.patch` shows only these bumps; `npm ci --dry-run` exit 0; trusted verification exit 0.
- **Required fix:** None for the candidate itself. The next cycle must focus on the actual blocker (trusted-shell workflow fix), not lockfile churn.
- **Verification:** Trusted verification exit 0 with the candidate lockfile; no test/typecheck regression.

## State

| Item | Status |
|---|---|
| V3-01 .. V3-07 | `complete` — no regression |
| V3-08 (product/config) | `repair` — candidate coherent and green, but finalizer failure not repaired; mustFix remains |
| Trusted verification | exit 0 |
| Candidate delta | `repair` — lockfile-only, retained as WIP baseline |
| Next step | Trusted shell applies the one-line workflow fix (`script: bash scripts/finalizer-e2e.sh`) on `main` and re-runs the trusted finalizer; no Builder rebuild can resolve the protected-workflow blocker |

No additional product, privacy, cost, or UI blocker exists in the candidate. The release blocker is exclusively the trusted-shell-owned workflow emulator step.