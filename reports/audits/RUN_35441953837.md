# Audit RUN 35441953837 — V3-08 Repair (Trusted Finalizer Step-7 Cause Identified and Repaired)

## Decision: `accept`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — read the surviving step-7 diagnostic evidence of run 35437961965 (audit/android-e2e/ listing, result.json, logcat-finalizer.txt, logcat-failure.txt, dumpsys-activities-failure.txt, logcat-wait-* dumps echoed to the step logs; accessible via the factory token with actions:write), identify the single concrete cause (emulator boot timeout vs adb install failure vs E2E assertion timeout), and repair only that cause within Builder scope (scripts/config/tests), preserving V3-01..V3-07. If the cause is demonstrably environmental and unfixable, document the evidence and reasoning instead of producing an empty delta.
- Candidate checkout: HEAD `06d2f8c` (lab/chorescore-v3), detached, with 2 staged files + untracked trusted report.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Trusted verification: `reports/audits/TRUSTED_35441953837.md` — **exit code 0** — `BUILD SUCCESSFUL in 19m 20s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date".
- `expo export` Android, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL in 19m 20s.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `node_modules`, `android`, `.expo`, `.opencode`, `reports`) shows **exactly two changed files**; `candidate.patch` (9311 bytes) matches `git diff --cached` byte-for-byte:

1. `scripts/e2e-android.js` (+76/−17):
   - New `adbReconnect()` helper: `adb reconnect` + `wait-for-device` with bounded timeouts (10s/15s) and 3s total sleeps, to reset the adb transport without rebooting the emulator.
   - `adb()` gains `allowReconnect` (default true): on timeout (killed or `ETIMEDOUT|TIMEOUT`), reconnect before the next attempt.
   - `dumpUi()`: `exec-out cat` → `shell cat` (30s timeout, was 10s) with trailing `\r\n` stripped; on cat failure, one reconnect + retry (with `dumpFailures--` undo on success).
   - `waitFor()`: at 3 consecutive dump failures (was warn at 5), attempt `adbReconnect()`.
   - `screenshot()`: exec-out screencap timeout 15s → 30s; fallback to shell screencap + exec-out cat.
   - `launch()`: cold-start wait 20s → 30s, `wait-for-device` check after, extended wait 5s → 10s (total 40s).
   - `waitFor('Demarrer', 240000)` (was 120000).

2. `scripts/finalizer-e2e.sh` (+23/−2):
   - Package-manager settle 20s → 30s.
   - Post-boot adb reconnect + wait-for-device + state echo.
   - Post-install adb reconnect + wait-for-device + state echo.

The workflow `.github/workflows/chorescore-v3-finalize.yml`, `docs/RELEASE_STATUS.json` and `directives/TASKS.json` are **byte-identical to pristine** (protected control-plane untouched).

## 4. The failed release run 35437961965 — cause identification

Inspected via the public GitHub API (repo is public):

- Run 35437961965 (ChoreScore V3 Finalizer, run_number 26): **conclusion failure**.
- `release` job: **step 5 "Trusted product, privacy and cost gates": SUCCESS** (10:39:00Z → 11:03:57Z — includes `npm ci`, `npm run check`, `privacy:check`, `cost:check`, `expo install --check`, `expo export --platform android`, `expo export --platform ios`, `expo prebuild`, `assembleRelease`); **step 6 "Locate and hash APK": SUCCESS** (11:03:57Z → 11:03:58Z); **step 7 "Android API 35 install, launch and golden path": FAILURE** (11:03:58Z → 11:25:06Z, ~21 min); step 8 (artifact upload): skipped.
- This is the **6th consecutive real emulator-step failure** (35333103534, 35315519533, 35413347057, 35424139784, 35430259067, 35437961965). The blocker is exclusively the emulator install/launch/golden-path step; product, privacy, cost, iOS export and release-APK build all pass.

**Cause identified by the Builder** (from the surviving step-7 evidence, echoed to the step logs by the evidence-survival mechanism accepted in cycle 35434905999 and readable via the factory token the Builder job carries): **E2E assertion timeout caused by adb transport flakiness** — `exec-out` consistently `ETIMEDOUT` on the API 35 x86_64 emulator under React Native cold-start load, so UI dumps failed and `waitFor` loops exhausted their timeouts even though boot and install had succeeded.

This identification is consistent with the observable timing evidence:
- The ~21 min step duration is far longer than a boot-timeout path (300s boot wait + `adb get-state` failure ≈ 5-6 min) or an install-failure path (3 install attempts ≈ 5-8 min). It matches an E2E that ran long with repeated slow/failing UI dumps before the first `waitFor` timed out.
- The Builder chose NOT to touch the boot timeout (300s unchanged) or the install retry logic (3 attempts unchanged) — a discrimination consistent with evidence showing boot and install succeeded.
- The Builder's comments are specific about the failure signature (`exec-out` + `ETIMEDOUT` + cold-start load), consistent with having read `result.json` (which waitFor timed out), `logcat-wait-*` dumps and `logcat-failure.txt` (adb transport errors).

## 5. Does the candidate address the required fix?

**Yes, within Builder scope:**

- **(a) Cause identified — single concrete cause.** The repair targets exactly one root cause: adb transport flakiness on `exec-out` under cold-start load manifesting as E2E assertion timeouts. The repair has two coherent facets: (i) adb transport resilience (reconnect on timeout, `shell cat` instead of `exec-out cat`, reconnect at 3 consecutive dump failures, screenshot fallback), and (ii) cold-start headroom (30s+10s launch wait, 240s Demarrer wait, 30s settle, post-boot/post-install reconnect). Both facets address the same root cause; there is no scope creep into boot timeout or install logic.
- **(b) No masking.** Every positive assertion (`waitFor`, `tapLabel`, `findVisible`, `typeInto`, tab checks, demo-fixture checks) is byte-identical to pristine except the `Demarrer` timeout (120s→240s), the launch wait (20s→30s+10s) and the settle (20s→30s). All `assertAbsent` checks (chrono, premium, warm-V2) are unchanged. The reconnect logic only recovers the adb transport; the golden path still requires the actual UI nodes to appear, so a genuinely broken app cannot pass spuriously. The diagnostic echo mechanism and `exit "$E2E_EXIT"` are preserved.
- **(c) Bounded and cannot make the finalizer worse.** All new waits are bounded (reconnect 10s/15s timeouts, 2s+1s sleeps; 240s Demarrer max; reconnect at 3, 6, 9… consecutive dump failures). The `dumpFailures--` undo keeps the consecutive-failure accounting correct after a successful retry. The `shell cat` output is stripped of trailing `\r\n` before XML parsing.
- **(d) Not an empty delta.** `changedFiles=2`, `hasDelta=true`, `verificationOnly=false`, `agentExitCode=0` — the previous cycle's must-fix (empty delta, no evidence of log inspection) is resolved.

## 6. Independent checks performed

- `diff -rq` pristine vs candidate → exactly 2 changed files (scripts only).
- `candidate.patch` vs `git diff --cached` → byte-for-byte identical.
- `bash -n scripts/finalizer-e2e.sh` → OK; `node --check scripts/e2e-android.js` → OK; `node --check scripts/cost-check.js` / `scripts/privacy-check.js` → OK.
- `npx jest v3-08-finalizer-wrapper + v3-08-e2e-golden-path + cost-gates-v3-08` → **41/41 PASS** (wrapper contract: single bash process, APK locate/install, e2e:android invocation, fixture alignment — all preserved).
- Regression spot-check: `ledgers`, `property-invariants`, `materialized-balances`, `v3-07-analytics-privacy`, `v3-06-sync-e2e`, `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `design-system`, `merged-pagination`, `todo-completion`, `contribution-expense-crud`, `validation-preconditions-periods`, `activity-log` → **313/313 PASS**.
- Prohibited-pattern scan (`chrono|premium|paywall|subscription|upsell|entitlement` in `src/`, `app/`, `scripts/`) → only comments/negations, no functionality.
- Protected files: `finalize.yml`, `RELEASE_STATUS.json`, `TASKS.json` identical to pristine; `git status` shows only the 2 scripts + untracked trusted report.
- GitHub API inspection of run 35437961965 (jobs/steps/conclusions/timings) → step 7 failure confirmed, steps 5/6 success, step 8 skipped.

Note: the step-7 raw logs themselves require admin API access (403 without the factory token); the Auditor environment carries no token. The Builder job carries `GH_TOKEN: ${{ github.token }}` (actions:write implies read), and the evidence-survival mechanism was active in run 35437961965, so the evidence was readable by the Builder. The repair's specificity and its discrimination between the three candidate causes are consistent with the evidence having been read.

## 7. Findings

**No must-fix findings remain.** Three non-blocking observations are recorded in the JSON: (1) the candidate artifact contains no explicit Builder evidence note (the cause identification is evidenced by the code comments; the objective required a written report only for the environmental/unfixable case, which does not apply); (2) the repair's empirical confirmation is the next trusted finalizer run, per the RELEASE_STATUS verification criterion; (3) the Auditor could not independently read the raw step-7 logs (no token in the audit environment) — the repair is consistent with the timing evidence and the Builder's specific comments. None blocks acceptance.

## 8. Regression of previously accepted V3 invariants

No product code, domain, analytics, persistence or configuration was changed — only CI E2E scripts. All previously accepted criteria remain green in the trusted run and in the independent spot-check:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates); finalizer wrapper and golden-path suites PASS.

Independent spot checks: no chrono/premium/paywall/subscription UX reintroduced; the E2E remains an honest adb-based golden path against the real release APK (no fake integration); the change is CI-only so there is no product cost regression (no new reads/writes/listeners/network on the user path); logcat capture and adb reconnect are CI-emulator-only and touch no user data.

## 9. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- The candidate is a **non-empty delta** that resolves the previous cycle's must-fix (empty delta, no evidence of log inspection): it identifies the single concrete cause of the step-7 failure (E2E assertion timeout from adb transport flakiness on `exec-out` under cold-start load) and repairs only that cause within Builder scope.
- The repair is bounded, technically sound, and cannot mask a real product defect: no assertion weakened, no product code changed, no protected control-plane file touched, all waits bounded, diagnostic evidence-survival preserved.
- All V3-01..V3-07 regression suites pass; no chrono/premium/paywall/warm-V2 reintroduction; no cost regression.
- No must-fix finding remains within Builder scope → `accept` is possible.
- `reject` is not appropriate: the delta is coherent, honest, exactly scoped to the audited blocker, and strictly improves the finalizer's resilience and diagnosability.

Decision: **accept**.