# Audit RUN_35146343615 — V3-02 REPAIR

**Cycle:** 35146343615  
**Criterion:** V3-02 — Structure application, langage visuel et socle local-first  
**Decision:** **accept**  
**Trusted verification:** exit 0 (TRUSTED_35146343615.md) — typecheck, 86 tests green, Expo export + Android prebuild ok  
**Role:** builder (per directive schema)

## Summary
Repair for the two must-fix findings from 35140323070 is complete and verified. Demo fixture is now internally consistent under canonical ids, gated on repository readiness, and SQLite-backed repository coverage exists with deterministic mocked driver.

## Objective verification

### 1) Demo fixture canonical consistency + readiness gating
- **Fix:** Extracted `src/features/app/demoFixture.ts` with `ensureDemoFixture()` seeding `users`, `households`, `memberships`, `members` via `seed()` (INSERT OR REPLACE) under `DEMO_HOUSEHOLD_ID='h-core'`, `DEMO_ALEX_MEMBER_ID='m-alex'`, `DEMO_SAM_MEMBER_ID='m-sam'`, `DEMO_SAM_USER_ID='demo-user-sam'` and `demoUser.userId='demo-user-alex'`. All subsequent records (tasks, contributions `Vaisselle du soir`/`Courses Migros`, todo `Sortir les poubelles`) reference the same `h-core`.
- **Repository readiness:** `AppContext.tsx` now initializes `reposRef` with `createInMemoryRepositories()` synchronously, stores `reposReadyPromiseRef`, exposes `ensureReposReady()` and awaits it in both `signIn()` and `loadHouseholds()` before any fixture or membership resolution. `isLoading` ties to `reposReady`, and `currentHouseholdId` is set to `DEMO_HOUSEHOLD_ID` after fixture. `loadHouseholdsForUser()` resolves via `getById(m.householdId)` which now finds `h-core`.
- **Evidence:** `src/features/app/demoFixture.ts:22-78` seed calls; `AppContext.tsx:79-93` ensureReposReady, `135-157` gated loadHouseholds/signIn; `src/infrastructure/repositories/index.ts` `SeedableRepository` interface; `SqliteRepositories.ts` INSERT OR REPLACE for users/households/memberships/members; `InMemoryRepositories.ts` seed maps.
- **Verification:** Tests `V3-02 demo fixture consistency` — `after demo sign-in the root groups list resolves the Appartement group` (expects length 1 id h-core), `a second sign-in does not duplicate` (users/memberships/members/contributions/todos lengths stable), `fixture converges through SQLite-backed repositories` (direct Sqlite* repos). Trusted suite 86 passed.

### 2) SQLite-backed repository tests + factory fallback
- **Fix:** `__tests__/infrastructure/repositories.test.ts` now mocks `expo-sqlite` with `FakeSQLiteDatabase` (execAsync no-op DDL, runAsync handles INSERT/INSERT OR REPLACE/UPDATE/DELETE, getFirstAsync/getAllAsync with WHERE/ORDER BY), exercises real `SqliteRepositories` code.
- **Tests added:**
  - `user CRUD round-trip and idempotent seed`, `household CRUD preserves unit/compensation config`, `membership CRUD unique lookup`, `member/contribution/persistent task/todo/expense/settlement round-trips`, `seed is an idempotent upsert`.
  - `RepositoryFactory selection` — `in-memory fallback is selected in test environment` asserts all 9 repos are InMemory instances; `business data written through factory-selected repos can be read back`.
- **Evidence:** File begins with `jest.mock('expo-sqlite')`, Fake class lines 60-155, beforeAll/beforeEach clearing tables, 14 new SQLite/factory/fixture tests. `RepositoryFactory.ts:61-72` isTestEnvironment checks `globalThis.__TEST__` and `NODE_ENV=test`.
- **Verification:** `npm test` green with new suite; trusted log confirms PASS repositories.test.ts.

## V3-02 acceptance checklist
- [x] app Expo démarre et exporte Android — trusted export 1302 modules, `metadata.json` 1.9KB, prebuild created `android/`, gradle configure ok
- [x] racine groupes sans restriction ni plan — `app/index.tsx` no badges/pricing, unlimited create, `InMemoryHouseholdRepository` test creates 5 groups
- [x] trois onglets exacts — `app/(tabs)/_layout.tsx` only `Ajouter`/`Balances`/`À faire`
- [x] design system V3 cohérent non générique — `theme.ts` graphite `#171719`, off-white `#F5F5F7`, metallic `#D1D1D6`, forest `#2D6A4F` / wine `#9B2226`
- [x] aucune UX Premium/paywall/chrono — grep shows only comments about removal, no `ChronoTimer`
- [x] données métier dans stockage local indexé — `SqliteStorage.ts` 8 tables + 12 indexes on groupId/occurredAt/unit/status, `SqliteRepositories` with `getByHousehold` indexed, `RepositoryFactory` SQLite with in-memory fallback
- [x] navigation courante rendue depuis local — `AppContext` reads `reposRef.current` (SQLite/in-memory), no network fetch on tab switch
- [x] ports/adapters preserve domain provider-agnostic — no `firebase` import, domain has no Firestore types

## V3-01 regression check
- [x] ledgers zero-sum, integer minor units, rate snapshot, Minutes/Points distinct — `__tests__/domain/*` all PASS (property-invariants, ledgers, validation)
- [x] no silent conversion, no cross-currency netting, no chrono reintroduction — unchanged domain

## Cost / frugality spot checks
- Indexes prevent full scan for household queries; factory test confirms no remote reads; Fake DB clears between tests (no history-proportional cost). Full cost regression gate is V3-08 scope, but V3-02 shows bounded writes (single INSERT per entity) and local reads.

## Findings
No mustFix remaining. No new premium/chrono/warm-design, provider-coupling, or destructive history introduced.

## Verdict
`accept` — trusted verification passed and both required repairs (canonical fixture + readiness gating, SQLite repository test coverage) are demonstrated with green tests and no V3-01 regression.
