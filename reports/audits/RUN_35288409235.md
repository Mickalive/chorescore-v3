# Audit RUN 35288409235 — V3-08 repair (waitFor numeric timeout)

**Decision: `accept`**

## Trusted verification

`reports/audits/TRUSTED_35288409235.md` ends with **`Trusted verification exit code: 0`**:

- `npm run check` — typecheck clean, **21 suites / 426 tests PASS**
- `npm run privacy:check` — PASS (static contract OK + 71 privacy tests)
- `npm run cost:check` — PASS (21 cost-gate tests, including V3-08 gates)
- `expo export --platform android` — OK (1318 modules bundled)
- `expo prebuild --platform android` — OK
- `gradlew :app:assembleDebug` — `BUILD SUCCESSFUL in 19m 47s` (395 tasks)

## Candidate delta

Exactly **one file, one line**: `scripts/e2e-android.js` line 287.

```diff
-  waitFor('Contribution', { exact: false, scroll: true, waitMs: 500 });
+  waitFor('Contribution', 15000);
```

`diff -rq` against the pristine accepted tree (`/home/runner/work/_temp/accepted-pristine`) shows **only** `scripts/e2e-android.js` differing. `src/`, `app/`, `__tests__/`, `docs/`, `governance/`, `directives/` are byte-identical → **no V3-01..V3-07 regression possible**.

## Active criterion — correctly repaired ✅

The audited must-fix F1 from cycle 35284783701 is fixed:

1. `waitFor(label, timeoutMs = 10000)` (line 175) accepts **only** a label and a numeric timeout. Its body computes `const until = Date.now() + timeoutMs` and polls `while (Date.now() < until)`.
2. The previous call passed an options object as `timeoutMs`: `Date.now() + { exact: false, scroll: true, waitMs: 500 }` string-concatenates to `<timestamp>[object Object]`, so `Date.now() < until` is **always false**, the polling loop never executes, and the function **throws `Timed out waiting for Contribution` immediately** regardless of app state — a guaranteed golden-path failure at step 6.
3. The candidate passes `15000` — a numeric timeout — so the loop polls for up to 15 s. All 12 `waitFor` calls in the script now pass numeric timeouts; no `waitFor('<label>', {` pattern remains anywhere in the repo.

### Independent simulation (mocked adb, package pre-installed, no `CHORESCORE_APK_PATH`)

| Scenario | Result |
|---|---|
| Candidate line 287 | Script reaches **`writeResult('pass')`**, all 8 checkpoints (`01-login` … `08-back-to-add`), exit 0 ✅ |
| Old options-object line restored | Fails at line 287 with `Timed out waiting for Contribution`, `writeResult('fail')` ❌ |

The repair is exactly what unblocks the golden path E2E — a V3-08 acceptance criterion and the trusted finalizer's last step.

## APK-path repair — intact ✅

The previous cycle's repair is fully preserved (the diff vs pristine contains no other change):

- `resolveApkPath()` (lines 40–55): `CHORESCORE_APK_PATH` priority when set and existing, then auto-locate under `android/app/build/outputs/apk/release/`, `null` when nothing found.
- Install-only-when-needed (lines 228–238): `pm list packages <packageName>` first; APK resolved/used only when the package is not installed.
- `writeResult()` records `apkPath`; docstring matches the implementation.

## Acceptance criteria (TASKS.json) — all met ✅

1. `scripts/e2e-android.js` passes numeric timeout to `waitFor('Contribution', ...)` at line 287 — **verified**.
2. Mocked-adb simulation no longer throws at the Contribution golden-path step — **verified** (`writeResult('pass')`).
3. `tsc --noEmit` exits 0 — **trusted verification**.
4. All 426 tests pass across 21 suites — **trusted verification**.
5. No regression on V3-01 through V3-07 — **verified** (no product code touched; full suite green).
6. Script reaches `writeResult('pass')` in golden-path simulation — **verified**.

## Independent checks

- `node --check scripts/e2e-android.js` — syntax OK.
- Node reproduction of the bug: object-as-timeout yields `until` of type string and zero loop iterations (immediate throw); numeric timeout polls for the full window.
- Grep for `waitFor('<label>', {` across the repo — no matches.
- No prohibited patterns in the delta: no chrono, premium, paywall, subscription, entitlement, warm V2 design, provider coupling, destructive history/reset, silent unit conversion, cross-currency netting, implicit cross-ledger compensation, fake integration, or accessibility/offline/error regression.

## Findings

**No must-fix findings remain.**

## Conclusion

Trusted verification passed (exit 0), the audited must-fix is correctly and minimally repaired, the APK-path repair is preserved, all acceptance criteria are met, and no regression is possible. **Decision: `accept`.**