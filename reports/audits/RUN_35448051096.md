# Audit RUN 35448051096 — V3-08 Repair (Trusted Finalizer Run 35445913329 Step-7 Blocker Repaired)

## Decision: `accept`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — inspect the exact failed release logs of Actions run 35445913329, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria (V3-01..V3-07).
- Candidate checkout: HEAD `bd2cb97` (lab/chorescore-v3), detached, with 2 staged files + untracked trusted report.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine` (HEAD `bd2cb97`).
- Trusted verification: `reports/audits/TRUSTED_35448051096.md` — **exit code 0** — `BUILD SUCCESSFUL in 19m 15s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date".
- `expo export` Android, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL in 19m 15s.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `node_modules`, `android`, `.expo`, `.opencode`, `reports`) shows **exactly two changed files**; `candidate.patch` (7682 bytes) matches `git diff --cached` byte-for-byte:

1. `scripts/e2e-android.js` (+62/−11):
   - `shell()` accepts a trailing `{ timeoutMs }` options object to override the default 30s per call.
   - `dumpUi()`: uiautomator dump timeout 30s → **60s** (the remaining dump-failure source after the previous cycle fixed the exec-out cat path); on dump failure, a diagnostic fallback captures `dumpsys activity top` (bounded 15s).
   - `waitFor()`: on timeout, in addition to logcat, captures `dumpsys activity top` (saved to `dumpsys-wait-*.txt`) and `pidof` process state (distinguishes "app crashed" from "dump too slow").
   - `screenshot()`: fallback pull switched from `adb exec-out cat` to `adb shell cat` (consistent with the accepted exec-out-ETIMEDOUT diagnosis).
   - `launch()`: cold-start wait 30s → **45s**, `wait-for-device` check, then a post-cold-start `adbReconnect()` to clear stale transport channels; extended-wait message updated to 55s.
2. `scripts/finalizer-e2e.sh` (+8/−2):
   - Package-manager settle 30s → **45s**.
   - Pre-E2E logcat capture (`audit/android-e2e/logcat-pre-e2e.txt`, bounded, non-fatal) for startup diagnosis.

The workflow `.github/workflows/chorescore-v3-finalize.yml`, `docs/RELEASE_STATUS.json` and `directives/TASKS.json` are **byte-identical to pristine** (protected control-plane untouched).

## 4. The failed release run 35445913329 — cause identification

Inspected via the public GitHub API (repo is public; raw step logs require a token — 403 without one):

- Run 35445913329 (ChoreScore V3 Finalizer, run_number 28): **conclusion failure**.
- `release` job: **step 5 "Trusted product, privacy and cost gates": SUCCESS** (13:29:46Z → 13:51:13Z — includes `npm ci`, `npm run check`, `privacy:check`, `cost:check`, `expo install --check`, `expo export --platform android`, `expo export --platform ios`, `expo prebuild`, `assembleRelease`); **step 6 "Locate and hash APK": SUCCESS** (13:51:13Z → 13:51:13Z); **step 7 "Android API 35 install, launch and golden path": FAILURE** (13:51:13Z → 14:12:47Z, ~21.5 min); step 8 (artifact upload): skipped; no artifacts uploaded.
- This is the **7th consecutive real emulator-step failure** (35333103534, 35315519533, 35413347057, 35424139784, 35430259067, 35437961965, 35445913329). The blocker is exclusively the emulator install/launch/golden-path step; product, privacy, cost, iOS export and release-APK build all pass.

**Cause identified by the Builder** (from the step-7 evidence echoed to the step logs by the evidence-survival mechanism, readable via the factory token the Builder job carries): **E2E assertion timeout caused by uiautomator dump timeouts on the loaded API 35 x86_64 emulator under React Native cold-start load.** The previous cycle fixed the `exec-out cat` ETIMEDOUT path; the remaining failure source is the `uiautomator dump` itself, which can take 30-60s on a loaded emulator — the 30s default `shell()` timeout killed it, each `dumpUi` failure costing ~96s (3×30s attempts + reconnects), so the 240s `Demarrer` window was exhausted after only ~2 iterations.

This identification is consistent with the observable timing evidence:
- The ~21.5 min step duration matches an E2E that ran long with repeated slow/failing UI dumps before `waitFor` timeouts (a boot-timeout path would be ~5-6 min; an install-failure path ~5-8 min).
- The Builder did NOT touch the boot timeout (300s unchanged) or the install retry logic (3 attempts unchanged) — consistent with evidence showing boot and install succeeded.
- The Builder's comments are specific about the failure signature (`uiautomator dump` 30-60s on a loaded emulator, cascading into `waitFor` timeouts), consistent with having read the surviving step-7 evidence.

## 5. Does the candidate address the required fix?

**Yes, within Builder scope:**

- **(a) Cause identified — single concrete cause.** The repair targets exactly one root cause: slow `uiautomator dump` (30-60s) killed by the 30s shell timeout, cascading into `waitFor` assertion timeouts. The repair has two coherent facets: (i) dump-path resilience (60s dump timeout via the new `shell()` options override, screenshot fallback switched from `exec-out cat` to `shell cat`), and (ii) cold-start headroom (45s launch wait + post-cold-start adb reconnect, 45s package-manager settle). Both facets address the same root cause; there is no scope creep into boot timeout or install logic.
- **(b) No masking.** Every positive assertion (`waitFor`, `tapLabel`, `findVisible`, `typeInto`, tab checks, demo-fixture checks) is byte-identical to pristine except the dump timeout (30s→60s), the launch wait (30s→45s) and the settle (30s→45s). All `assertAbsent` checks (chrono, premium, warm-V2) are unchanged. The golden path still requires the actual UI nodes to appear, so a genuinely broken app cannot pass spuriously. The diagnostic echo mechanism and `exit "$E2E_EXIT"` are preserved.
- **(c) Bounded and cannot make the finalizer worse.** All new waits are bounded (60s dump max, 45s launch, 45s settle, 15s dumpsys, 10s logcat, 10s/15s reconnect). The new diagnostics (dumpsys activity, pidof, pre-E2E logcat) are additive and only run on failure/timeout paths. The 60s dump timeout makes each fully-failed dump cycle more expensive (~186s vs ~96s), but the targeted failure mode is dumps that complete in 30-60s and were previously killed — for those, the fix is strictly better; for a genuinely hung dump, the enriched diagnostics will identify it.
- **(d) Not an empty delta.** `changedFiles=2`, `hasDelta=true`, `verificationOnly=false`, `agentExitCode=0` — the candidate is a concrete repair, not a verification-only cycle.

## 6. Independent checks performed

- `diff -rq` pristine vs candidate → exactly 2 changed files (scripts only).
- `candidate.patch` vs `git diff --cached` → byte-for-byte identical.
- `bash -n scripts/finalizer-e2e.sh` → OK; `node --check scripts/e2e-android.js` → OK; `node --check scripts/cost-check.js` / `scripts/privacy-check.js` → OK.
- `npx jest v3-08-finalizer-wrapper + v3-08-e2e-golden-path + cost-gates-v3-08` → **41/41 PASS** (wrapper contract: single bash process, APK locate/install, e2e:android invocation, fixture alignment — all preserved).
- Regression spot-check: `ledgers`, `property-invariants`, `materialized-balances`, `v3-07-analytics-privacy`, `v3-06-sync-e2e`, `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `design-system`, `merged-pagination`, `todo-completion`, `contribution-expense-crud`, `validation-preconditions-periods`, `activity-log`, `pagination`, `repositories`, `ports-adapters` → **357/357 PASS**.
- Prohibited-pattern scan (`chrono|premium|paywall|subscription|upsell|entitlement` in `src/`, `app/`, `scripts/`) → no functionality, only comments/negations.
- Protected files: `finalize.yml`, `RELEASE_STATUS.json`, `TASKS.json` identical to pristine; `git status` shows only the 2 scripts + untracked trusted report.
- GitHub API inspection of run 35445913329 (jobs/steps/conclusions/timings) → step 7 failure confirmed, steps 5/6 success, step 8 skipped, no artifacts.

Note: the step-7 raw logs themselves require admin API access (403 without the factory token); the Auditor environment carries no token. The Builder job carries `GH_TOKEN: ${{ github.token }}` (actions:write implies read), and the evidence-survival mechanism was active in run 35445913329, so the evidence was readable by the Builder. The repair's specificity and its discrimination between the candidate causes are consistent with the evidence having been read.

## 7. Findings

**No must-fix findings remain.** Three non-blocking observations are recorded in the JSON: (1) the repair's empirical confirmation is the next trusted finalizer run, per the RELEASE_STATUS verification criterion; (2) the Auditor could not independently read the raw step-7 logs (no token in the audit environment) — the repair is consistent with the timing evidence and the Builder's specific comments; (3) the 60s dump timeout makes each fully-failed dump cycle more expensive, an acceptable trade-off given the targeted failure mode and the enriched diagnostics. None blocks acceptance.

## 8. Regression of previously accepted V3 invariants

No product code, domain, analytics, persistence or configuration was changed — only CI E2E scripts. All previously accepted criteria remain green in the trusted run and in the independent spot-check:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates); finalizer wrapper and golden-path suites PASS.

Independent spot checks: no chrono/premium/paywall/subscription UX reintroduced; the E2E remains an honest adb-based golden path against the real release APK (no fake integration); the change is CI-only so there is no product cost regression (no new reads/writes/listeners/network on the user path); logcat/dumpsys capture and adb reconnect are CI-emulator-only and touch no user data.

## 9. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- The candidate is a **non-empty delta** that repairs the concrete blocker of run 35445913329: the trusted finalizer failed again at step 7 (emulator install/launch/golden path) with the same long-running E2E signature; the repair targets the remaining dump-failure source (uiautomator dump 30s→60s timeout) plus cold-start headroom and enriched diagnostics, all within Builder scope.
- The repair is bounded, technically sound, and cannot mask a real product defect: no assertion weakened, no product code changed, no protected control-plane file touched, all waits bounded, diagnostic evidence-survival preserved and extended.
- All V3-01..V3-07 regression suites pass; no chrono/premium/paywall/warm-V2 reintroduction; no cost regression.
- No must-fix finding remains within Builder scope → `accept` is possible.
- `reject` is not appropriate: the delta is coherent, honest, exactly scoped to the audited blocker, and strictly improves the finalizer's resilience and diagnosability.

Decision: **accept**.