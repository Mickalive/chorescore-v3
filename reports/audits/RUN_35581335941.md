# Audit RUN 35581335941 — V3-08 repair (trusted finalizer E2E timeout budgets)

- **Cycle:** 35581335941
- **Role:** builder (independent audit)
- **Active criterion:** V3-08 (Finition, coût et release mobile)
- **Objective:** REPAIR V3-08 trusted finalizer failure from Actions run 35577033059 — inspect the exact failed release logs, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria.
- **Decision:** `accept`
- **Trusted verification exit code:** 0

---

## 1. Candidate delta (vs pristine accepted tree)

The pristine accepted tree is at `a8d7c3f` (`/home/runner/work/_temp/accepted-pristine`), identical to the current HEAD. The candidate delta is exactly three files:

| File | Change |
|---|---|
| `scripts/e2e-android.js` | +56/−33 — increased E2E timing budgets (cold-start grace, Demarrer wait, all mid-path waits) |
| `scripts/finalizer-e2e.sh` | +13/−13 — increased emulator boot timeout and package-manager settle |
| `__tests__/domain/v3-08-finalizer-wrapper.test.ts` | +6/−6 — contract test updated to match the new budgets |

No protected files touched (constitution, backend appendix, governance, directives, workflows, agents, release state, reports). No product code (`src/`, `app/`) changed — both are byte-identical to the pristine tree.

## 2. The exact failure being repaired

Public GitHub API for run 35577033059 (ChoreScore V3 Finalizer, run 58):

- Run conclusion: **failure**
- `prepare` job: success
- `release` job: **failure**
  - Step 5 "Trusted product, privacy and cost gates" → **success**
  - Step 6 "Locate and hash APK" → **success**
  - Step 7 "Android API 35 install, launch and golden path" → **failure** (08:39:14 → 09:05:23 UTC, **26m09s**)
  - Step 8 upload-artifact → skipped
- `reopen-on-failure`: success (committed `a8d7c3f`, reopening V3-08 repair)

The 26m09s duration is consistent with **waitFor timeout exhaustion** under the previous budgets: `waitFor('Demarrer', 600000)` + 9 × `waitFor(..., 120000)` = 28 min worst case, plus boot/settle/install/launch overhead. On a slow API 35 x86_64 emulator, the documented 240s React Native cold start, 30-60s uiautomator dumps, and crash-relaunch cycles exhausted the budgets before the golden path completed.

## 3. What the fix does

All timing budgets are strictly increased with documented rationale:

| Budget | Before | After | Rationale |
|---|---|---|---|
| `COLD_START_GRACE_MS` | 300s | **450s** | Beyond the documented 240s cold start; within the 900s Demarrer timeout |
| `waitFor('Demarrer', …)` | 600s | **900s** | Accommodates one force-stop + relaunch cycle with a fresh 240s cold start (comment documents the worst-case budget: stuck detection + relaunch overhead + relaunch cold start + dump buffer < 900s) |
| 9 mid-path `waitFor` calls | 120s | **300s** | Crash-relaunch cold starts mid-path (240s) + dump latency |
| Emulator boot `TIMEOUT` | 300s | **420s** | Documented 120-240s boot + CI load |
| Package-manager settle | 15s | **30s** | Full runtime initialization after `sys.boot_completed=1` |

The per-relaunch recovery mechanism from the previous accepted cycle (`FORCE_STOP_RECOVERY_MS = 300s`, `globalThis._lastForceStopTime`, recovery-guarded stuck-detection increment) is **preserved unchanged**. The contract test was updated to match the new budgets (graceMs upper bound 600_000 → 900_000; Demarrer waitFor string 600000 → 900000).

## 4. Independent verification performed

| Check | Result |
|---|---|
| Trusted log `TRUSTED_35581335941.md` | **exit 0** — typecheck PASS, 453/453 tests PASS (22 suites), privacy 71/71 PASS, cost 21/21 PASS, expo export android PASS, prebuild PASS, `:app:assembleDebug` BUILD SUCCESSFUL in 19m12s |
| Independent re-run | `npx tsc --noEmit` PASS; `npx jest` 453/453 PASS (22 suites); `node scripts/privacy-check.js` 71/71 PASS; `node scripts/cost-check.js` 21/21 PASS |
| Syntax | `node --check scripts/e2e-android.js` PASS; `bash -n scripts/finalizer-e2e.sh` PASS |
| Contract test | `v3-08-finalizer-wrapper` suite 23/23 PASS — graceMs bounds (240_000 ≤ 450_000 ≤ 900_000), Demarrer waitFor string, all waitFor timeouts ≥ 60s, dump TTL, dumpsys loop ≤ 5 iterations, fixture alignment |
| Delta scope | `git diff --cached --name-only` = exactly the 3 files; candidate.patch matches the staged diff byte-for-byte; `src/`, `app/`, `governance/`, `directives/`, `docs/`, `.github/` identical to pristine |
| Forbidden regression scan | Word-boundary scan of added lines: no chrono/premium/paywall/subscription/streak/badge/self-care/warm-V2 elements |
| Failure point | Public API confirms run 35577033059 failed at release job step 7 after 26m09s with gates passing |

## 5. Step-7 evidence of run 35577033059 — access limitation (documented)

The raw step-7 logs (logcat-wait-*.txt, dump-*.xml, step output) are not directly readable from this environment: the GitHub Actions job-logs endpoint returns HTTP 403 without admin rights and no factory token is present. What **is** verifiable via the public API:

- Run conclusion = failure; release job step 7 failed after 26m09s; steps 5-6 (gates, APK hash) succeeded; step 8 (artifact upload) skipped.
- The 26m09s duration matches the previous worst-case waitFor budget (600s + 9×120s = 28 min), confirming timeout exhaustion rather than a build/install failure.

The candidate directly targets this failure mode with strictly increased, internally consistent budgets. The definitive verification is the next trusted finalizer dispatch, which only the trusted shell can trigger.

## 6. Decision rationale

The acceptance rule is: **accept iff trusted verification passed AND no mustFix remains.**

- Trusted verification passed (exit 0) — typecheck, 453 tests, privacy 71, cost 21, expo export android, prebuild, assembleDebug BUILD SUCCESSFUL.
- The audited must-fix (trusted finalizer E2E timeout blocker from run 35577033059) is **repaired**: all E2E timing budgets strictly increased with documented rationale, contract test updated and passing, per-relaunch recovery preserved.
- No regression in V3-01..V3-07: zero product code changed; all previously accepted invariants preserved.
- The evidence-access limitation is documented (not a mustFix): the failure point is confirmed via the public API and the fix directly targets the timeout-exhaustion failure mode.

**Decision: `accept`.**

## 7. Findings

- **path:** `scripts/e2e-android.js` + `scripts/finalizer-e2e.sh` (E2E timing budgets) — **repaired** (mustFix: false). The timeout-exhaustion blocker of run 35577033059 is addressed with strictly increased budgets (cold-start grace 450s, Demarrer 900s, mid-path 300s, boot 420s, settle 30s), contract-tested and internally consistent.
- **path:** evidence-access limitation (raw step-7 logs of run 35577033059) — **documented** (mustFix: false). Raw logs return 403 without admin rights; no factory token in this environment. Public API confirms the failure point and duration. If the next finalizer dispatch still fails at the emulator step, the trusted shell should consider persisting step-7 diagnostics as a downloadable artifact (`if: always()`).