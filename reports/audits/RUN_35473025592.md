# Audit RUN 35473025592 — V3-08 repair (trusted finalizer failure 35470723768)

**Decision: `accept`**

## 1. Summary

The candidate is a non-empty delta (`changedFiles=2`, `hasDelta=true`, `verificationOnly=false`, `agentExitCode=0`) repairing the trusted finalizer step-7 blocker of Actions run 35470723768 within Builder scope: `scripts/e2e-android.js` and `scripts/finalizer-e2e.sh` only.

Public run metadata confirms the failure is isolated to the emulator E2E step:

- Step 5 "Trusted product, privacy and cost gates" — **success** (includes `assembleRelease`, iOS export)
- Step 6 "Locate and hash APK" — **success**
- Step 7 "Android API 35 install, launch and golden path" — **failure** (the only blocker)

The candidate repairs exactly that step.

## 2. What the candidate changed

1. **`scripts/e2e-android.js` — `launch()`**: cold-start sleep 60s → 30s; **removed the proactive `adbReconnect()`** (kill-server + start-server) that ran after the cold-start sleep, immediately before the uiautomator warm-up dump.
2. **`scripts/finalizer-e2e.sh`**: package-manager settle 60s → 30s; **removed the post-install adb server restart** (kill-server + start-server + waits, ~13s).

**Rationale (documented in code comments):** a kill-server/start-server restart executed under heavy Hermes cold-start load disrupts the adb transport exactly when the uiautomator server needs to initialize, so the warm-up dump fails and the first real `dumpUi()` blocks for the full 45s timeout, burning the Demarrer `waitFor()` window. The pre-E2E restart at idle (before install) is retained; all reactive `adbReconnect()` recovery paths are retained.

## 3. Independent verification performed

| Check | Result |
|---|---|
| Candidate patch vs staged diff | byte-for-byte identical |
| `node --check scripts/e2e-android.js` | PASS |
| `bash -n scripts/finalizer-e2e.sh` | PASS |
| Pristine tree diff (accepted-pristine @ eb0f7a2) | only the 2 CI scripts differ; no product/test/governance/docs/workflow files changed |
| Trusted verification (TRUSTED_35473025592.md) | exit 0 — typecheck, 432 tests/22 suites, privacy 71, cost 21 (10 gates), expo export, prebuild, `assembleDebug` BUILD SUCCESSFUL |
| Failed run 35470723768 (public API) | steps 5-6 success, step 7 failure — blocker isolated to emulator E2E |
| E2E assertions | identical between baseline a46a20b and candidate (all `waitFor`/`assertAbsent`/`tapLabel` targets and timeouts) |
| Demarrer window | unchanged at 360s; reduced sleeps are non-deadline |
| Reactive recovery | `adbReconnect()` preserved in `adb()` retry, `dumpUi()` cat-failure, `waitFor()` consecutive-failure paths |
| Diagnostic evidence | logcat-wait-*, dumpsys-wait-*, pidof, result.json echo — all preserved |

## 4. Timing budget

- Candidate worst case before the Demarrer window: ~147s (30s sleep + 10s wait-for-device + 90s warm-up worst case + 5s fallback + 10s pidof) vs baseline ~218s.
- Window unchanged: 360s. App cold-start (60-120s typical) fits with ample margin.
- The removed restart no longer consumes window-adjacent time and the warm-up dump now runs on a stable transport — the first real dump does not block for 45s.

## 5. Regression of accepted criteria

No product code, domain, analytics, persistence or configuration changed — CI scripts only. All previously accepted criteria remain green:

- **V3-01** domain/ledgers: PASS (zero-sum, integer rounding, rate snapshot, no silent conversion).
- **V3-02** structure/design: PASS (no chrono/premium/warm-V2, factual copy, WCAG AA).
- **V3-03** Ajouter/history: PASS.
- **V3-04** Balances: PASS.
- **V3-05** À faire: PASS.
- **V3-06** groups/sync: PASS.
- **V3-07** data product: PASS (71 tests).
- **V3-08** cost gates: PASS (all 10 gates, 21 tests).

No chrono/premium/paywall/warm-V2 reintroduction. No cost regression.

## 6. Findings

Two informational findings (both `mustFix=false`):

1. **Exact step-7 logs not directly accessible** — GitHub log download requires admin rights (403 on both run-level and job-level log endpoints); the run page HTML does not embed logs. Diagnosis reconstructed from public run metadata, the audit history (8+ step-7 failures with the boot/install-succeed, UI-dump-slow/failing signature) and code-level analysis. The Builder's diagnosis is technically coherent.
2. **Empirical confirmation pending** — the repo alone cannot prove the next finalizer run will pass; the trusted finalizer is the empirical gate. The change is low-risk and directly targets the mechanism identified in cycle 35466799695 (uiautomator server session not initialized after adb server-process restart).

## 7. Conclusion

Trusted verification passed (exit 0) and no must-fix finding remains. **Decision: accept.** The next trusted finalizer run will empirically confirm the repair; if step 7 passes, V3-08 is complete and the release is ready for final promotion.