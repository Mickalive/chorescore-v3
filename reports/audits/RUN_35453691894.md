# Audit RUN 35453691894 — V3-08 Repair (Trusted Finalizer Run 35451816114 Step-7 Blocker Repaired)

## Decision: `accept`

## 1. Scope and inputs

- Active criterion: **V3-08** (finition, coût, release mobile).
- Objective: REPAIR V3-08 — inspect the exact failed release logs of Actions run 35451816114, fix only the concrete product/privacy/cost/APK/runtime/E2E/readiness blocker, and preserve all accepted criteria (V3-01..V3-07).
- Candidate checkout: HEAD `2851607` (lab/chorescore-v3), detached, with 2 staged files + untracked trusted report.
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine` (HEAD `2851607`).
- Trusted verification: `reports/audits/TRUSTED_35453691894.md` — **exit code 0** — `BUILD SUCCESSFUL in 15m 36s`.

## 2. Trusted verification (exit 0)

The trusted log shows all green:

- `npm run check` → typecheck + 22 suites / **432 tests PASS** (including `v3-08-e2e-golden-path`, `v3-08-finalizer-wrapper`, `cost-gates-v3-08`, all V3-01..V3-07 regression suites).
- `privacy:check` → static privacy release contract OK + 71 focused tests PASS.
- `cost:check` → V3-08 cost regression gate PASS (21 tests, 10 gates).
- `expo install --check` → "Dependencies are up to date".
- `expo export` Android, `expo prebuild`, Gradle `assembleDebug` → BUILD SUCCESSFUL in 15m 36s.
- Final line: `Trusted verification exit code: 0`.

No exit-code failure → not a mandatory mustFix on that ground.

## 3. Candidate delta vs pristine

`diff -rq` between pristine and candidate (excluding `.git`, `node_modules`, `android`, `.expo`, `.opencode`, `reports`, `audit`) shows **exactly two changed files**; `candidate.patch` (5989 bytes) matches `git diff --cached` byte-for-byte:

1. `scripts/e2e-android.js` (+27/−19):
   - `ADB_TIMEOUT_MS` 60s → **45s** (general adb calls; install timeout 180s unchanged).
   - `shell()` options object now accepts `{ timeoutMs, retries }` (was `{ timeoutMs }` only) and forwards `retries` to `adb()`.
   - `dumpUi()`: uiautomator dump **60s timeout × 3 attempts → 45s timeout × 1 attempt** (fail fast so `waitFor()` retries at a higher level within its own deadline; the previous 3-attempt approach wasted up to 180s per dump, blowing past the 240s Demarrer window).
   - `cat` of the dump file: 3 attempts → **1 attempt** (both the normal path and the reconnect-retry path).
   - `launch()`: cold-start wait 45s → **60s**.
   - `waitFor('Demarrer', 240000)` → **300000**.
2. `scripts/finalizer-e2e.sh` (+1/−1):
   - Package-manager settle 45s → **60s**.

The workflow `.github/workflows/chorescore-v3-finalize.yml`, `docs/RELEASE_STATUS.json`, `directives/TASKS.json`, `docs/ROADMAP.md` and `governance/RELEASE_DEFINITION.json` are **byte-identical to pristine** (protected control-plane untouched).

## 4. The failed release run 35451816114 — cause identification

Inspected via the public GitHub API (repo is public; raw step logs require admin rights — 403 without a token):

- Run 35451816114 (ChoreScore V3 Finalizer, run_number 30): **conclusion failure**.
- `release` job: **step 5 "Trusted product, privacy and cost gates": SUCCESS** (15:26:47Z → 15:44:18Z — includes `npm ci`, `npm run check`, `privacy:check`, `cost:check`, `expo install --check`, `expo export --platform android`, `expo export --platform ios`, `expo prebuild`, `assembleRelease`); **step 6 "Locate and hash APK": SUCCESS** (15:44:18Z → 15:44:18Z); **step 7 "Android API 35 install, launch and golden path": FAILURE** (15:44:18Z → 16:02:37Z, ~18.3 min); step 8 (artifact upload): skipped; no artifacts uploaded.
- This is the **8th consecutive real emulator-step failure** (35333103534, 35315519533, 35413347057, 35424139784, 35430259067, 35437961965, 35445913329, 35451816114). The blocker is exclusively the emulator install/launch/golden-path step; product, privacy, cost, iOS export and release-APK build all pass.

**Cause identified by the Builder** (from the step-7 evidence echoed to the step logs by the evidence-survival mechanism, readable via the factory token the Builder job carries): **E2E assertion timeout caused by the previous cycle's dump-timeout configuration.** Cycle 35448051096 raised the uiautomator dump timeout to 60s, but with the `adb()` default of 3 attempts, each failed `dumpUi` cycle cost up to 270s (3×60s dump + 3×30s cat), exceeding the 240s `Demarrer` window. A single failed dump after cold start therefore guaranteed a `waitFor` timeout.

This identification is consistent with the observable timing evidence and the independent timing math:

- The ~18.3 min step duration matches an E2E that ran long with repeated slow/failing UI dumps before `waitFor` timeouts (a boot-timeout path would be ~5-6 min; an install-failure path ~5-8 min).
- **Baseline math:** worst-case failed `dumpUi` cycle = 270s vs 240s Demarrer window → **0.89 cycles fit** → guaranteed timeout on the first failed dump.
- The Builder did NOT touch the boot timeout (300s unchanged) or the install retry logic (3 attempts unchanged) — consistent with evidence showing boot and install succeeded.
- The Builder's comments are specific about the failure signature ("Previous 3-retry approach wasted up to 180s per dump, blowing past the 240s Demarrer window"), consistent with having read the surviving step-7 evidence.

## 5. Does the candidate address the required fix?

**Yes, within Builder scope:**

- **(a) Cause identified — single concrete cause.** The repair targets exactly one root cause: the previous dump-timeout configuration (60s × 3 attempts = 270s per failed dumpUi cycle) exceeding the 240s Demarrer window. The repair has two coherent facets: (i) fail-fast dump strategy (45s timeout, 1 attempt) so `waitFor()` retries the whole `dumpUi` cycle at a higher level within its own deadline, and (ii) cold-start headroom (Demarrer window 240s→300s, launch wait 45s→60s, settle 45s→60s). Both facets address the same root cause; there is no scope creep into boot timeout or install logic.
- **(b) Timing verified.** Candidate worst-case failed `dumpUi` cycle = 75s (45s dump + 30s cat); **4.0 cycles fit** in the 300s window (3.75 with reconnect overhead) vs 0.89 before. The app now gets ~4 chances to render within the window instead of being guaranteed to fail on the first failed dump.
- **(c) No masking.** Every positive assertion (`waitFor`, `tapLabel`, `findVisible`, `typeInto`, tab checks, demo-fixture checks) is byte-identical to pristine except the Demarrer window (240s→300s). All `assertAbsent` checks (chrono, premium, warm-V2) are unchanged. The golden path still requires the actual UI nodes to appear, so a genuinely broken app cannot pass spuriously. The diagnostic echo mechanism and `exit "$E2E_EXIT"` are preserved.
- **(d) Bounded and cannot make the finalizer worse.** All new waits are bounded (45s dump, 30s cat, 60s launch, 300s window, 60s settle). The fail-fast strategy is strictly better in the hang case (more attempts per window, sooner reconnect at the waitFor level) and in the success case (dumps that complete in <45s work; the waitFor retry loop absorbs transient failures). The only theoretical regression — dumps that would complete in 45-60s being killed at 45s — is mitigated by the waitFor retry loop and the larger window, and is inconsistent with the observed failure signature (the previous 60s timeout would have succeeded on such dumps).
- **(e) Not an empty delta.** `changedFiles=2`, `hasDelta=true`, `verificationOnly=false`, `agentExitCode=0` — the candidate is a concrete repair, not a verification-only cycle.

## 6. Independent checks performed

- `diff -rq` pristine vs candidate → exactly 2 changed files (scripts only).
- `candidate.patch` vs `git diff --cached` → byte-for-byte identical.
- `bash -n scripts/finalizer-e2e.sh` → OK; `node --check scripts/e2e-android.js` → OK; `node --check scripts/cost-check.js` / `scripts/privacy-check.js` → OK.
- `npx jest v3-08-finalizer-wrapper + v3-08-e2e-golden-path + cost-gates-v3-08` → **41/41 PASS** (wrapper contract: single bash process, APK locate/install, e2e:android invocation, fixture alignment — all preserved).
- Regression spot-check: `ledgers`, `property-invariants`, `materialized-balances`, `v3-07-analytics-privacy`, `v3-06-sync-e2e`, `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `design-system`, `merged-pagination`, `todo-completion`, `contribution-expense-crud`, `validation-preconditions-periods`, `activity-log`, `pagination`, `repositories`, `ports-adapters` → **357/357 PASS**.
- Prohibited-pattern scan (`chrono|premium|paywall|subscription|upsell|entitlement` in `src/`, `app/`, `scripts/`) → no matches.
- Protected files: `finalize.yml`, `RELEASE_STATUS.json`, `TASKS.json`, `ROADMAP.md`, `RELEASE_DEFINITION.json` identical to pristine; `git status` shows only the 2 scripts + untracked trusted report.
- GitHub API inspection of run 35451816114 (jobs/steps/conclusions/timings) → step 7 failure confirmed, steps 5/6 success, step 8 skipped, no artifacts.
- Timing math verified independently: baseline 270s per failed dumpUi cycle vs 240s window (0.89 cycles fit); candidate 75s per cycle vs 300s window (4.0 cycles fit).

Note: the step-7 raw logs themselves require admin API access (403 without the factory token); the Auditor environment carries no token. The Builder job carries `GH_TOKEN: ${{ github.token }}` (actions:write implies read), and the evidence-survival mechanism was active in run 35451816114, so the evidence was readable by the Builder. The repair's specificity and its discrimination between the candidate causes are consistent with the evidence having been read, and the timing math independently confirms that the baseline configuration could not survive a single failed dumpUi cycle within the 240s window.

## 7. Findings

**No must-fix findings remain.** Three non-blocking observations are recorded in the JSON: (1) the repair's empirical confirmation is the next trusted finalizer run, per the RELEASE_STATUS verification criterion; (2) the Auditor could not independently read the raw step-7 logs (no token in the audit environment) — the repair is consistent with the timing evidence and the Builder's specific comments, and the timing math independently confirms the baseline's structural insufficiency; (3) the `retries` parameter counts attempts (so "1 retry" in the comment means 1 attempt) and dumps completing in 45-60s would now be killed at 45s — an acceptable trade-off given the waitFor retry loop, the larger window, and the observed failure signature. None blocks acceptance.

## 8. Regression of previously accepted V3 invariants

No product code, domain, analytics, persistence or configuration was changed — only CI E2E scripts. All previously accepted criteria remain green in the trusted run and in the independent spot-check:

- **V3-01** domain/ledgers: `ledgers`, `property-invariants`, `contribution-expense-crud`, `materialized-balances` PASS — zero-sum contribution, zero-sum money per currency with integer rounding, Minutes/Points distinct, settlement rate snapshot immutable, no silent conversion, no cross-currency netting.
- **V3-02** structure/design: `design-system` PASS — WCAG AA contrast, large-text scaling, no chrono/premium/warm-V2 elements, factual copy.
- **V3-03** Ajouter/history: `merged-pagination`, `pagination`, `cost-gates-v3-03` PASS.
- **V3-04** Balances: `validation-preconditions-periods`, `cost-gates-v3-04` PASS.
- **V3-05** À faire: `todo-completion`, `cost-gates-v3-05` PASS.
- **V3-06** groups/sync: `v3-06-groups-invitations-sync`, `v3-06-repair-sync-authorization`, `v3-06-sync-e2e`, `repositories`, `ports-adapters` PASS.
- **V3-07** data product: `v3-07-analytics-privacy` PASS (71 tests).
- **V3-08** cost gates: `cost-gates-v3-08` PASS (all 10 gates); finalizer wrapper and golden-path suites PASS.

Independent spot checks: no chrono/premium/paywall/subscription UX reintroduced; the E2E remains an honest adb-based golden path against the real release APK (no fake integration); the change is CI-only so there is no product cost regression (no new reads/writes/listeners/network on the user path); logcat/dumpsys capture and adb reconnect are CI-emulator-only and touch no user data.

## 9. Decision rationale

- Trusted verification passed (exit 0): **satisfied**.
- The candidate is a **non-empty delta** that repairs the concrete blocker of run 35451816114: the trusted finalizer failed again at step 7 (emulator install/launch/golden path) with the same long-running E2E signature; the repair targets the structural insufficiency of the previous dump-timeout configuration (270s per failed dumpUi cycle vs 240s window) with a fail-fast strategy plus cold-start headroom, all within Builder scope.
- The repair is bounded, technically sound, and cannot mask a real product defect: no assertion weakened, no product code changed, no protected control-plane file touched, all waits bounded, diagnostic evidence-survival preserved.
- All V3-01..V3-07 regression suites pass; no chrono/premium/paywall/warm-V2 reintroduction; no cost regression.
- No must-fix finding remains within Builder scope → `accept` is possible.
- `reject` is not appropriate: the delta is coherent, honest, exactly scoped to the audited blocker, and strictly improves the finalizer's resilience and diagnosability.

Decision: **accept**.