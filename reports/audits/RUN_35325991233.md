# Audit RUN 35325991233 — V3-08 repair: expo alignment fixed, trusted verification green

## Decision: `accept`

Candidate **accepted**. The Builder-fixed product/config blocker (expo dependency alignment) is resolved and trusted verification now exits 0 with all product, privacy, cost, export, prebuild and build gates passing. The remaining workflow emulator `script: |` issue is a protected control-plane file owned by the trusted shell per `AGENTS.md` / `TASKS.json` and does not block Builder acceptance for this item. No regression in V3-01..V3-07, no prohibited pattern, and the WIP wrapper test is preserved.

## Context

- Active criterion: **V3-08 — Finition, accessibilité, coût et release mobile** (REPAIR).
- Objective: `REPAIR V3-08: Update dependencies to satisfy expo install --check (align package.json to expected ~57.0.24 / ~57.0.19 / ~57.0.22 / ~57.0.21 / ~57.0.16 or run npx expo install --fix and commit package.json/package-lock.json), then re-run trusted verification so it reaches expo export --platform android/ios, prebuild and assembleRelease with exit 0. This is a Builder-fixable product/config change; no workflow edit needed for this item.; Trusted shell ... replace emulator step's multi-line script: | block with single-line script: bash scripts/finalizer-e2e.sh, then re-run trusted finalizer.`
- Trusted verification: **exit 0** — `reports/audits/TRUSTED_35325991233.md` (395 Gradle tasks, 19m26s).
- Pristine accepted tree: `/home/runner/work/_temp/accepted-pristine`.
- Prior state: V3-08 `in_progress` with two open findings in `docs/RELEASE_STATUS.json` — F1 (Builder: expo versions, exit 1) and F2 (trusted-shell: workflow multi-line).
- Candidate delta vs pristine: **only `package.json` + `package-lock.json`** (10 lines in package.json, 74 lines in lock).

## Trusted verification summary

`TRUSTED_35325991233.md` shows full green path:

- `npm run typecheck` — PASS (`tsc --noEmit`)
- `npm run test` — 22 suites / 431 tests PASS (including `v3-08-finalizer-wrapper.test.ts` and all V3-01..V3-08 suites)
- `privacy:check` — PASS (71 tests, taxonomy 3.0.0 deterministic, bucketing, cache, pipeline, privacy gate, consent, query budget, DP, buyer contracts, audit log)
- `cost:check` — PASS (21 tests, 10 gates: 50k ≤5 reads, tab-switch 0, 3-delta sync, bounded writes, 10k labels cache, privacy gate, budgets, offline, golden path, a11y)
- `npx expo install --check` — **PASS**: `Skipped checking dependencies: react-native-worklets and typescript... Dependencies are up to date` (was FAIL in 35324430975)
- `expo export` — PASS: `Android Bundled 18136ms ... (1318 modules)`, `Assets (27)`, `android bundles (1) hbc 3.2MB`, `Exported: /home/runner/work/_temp/v3-audit-export`
- `expo prebuild --platform android --no-install` — PASS: `Cleared android code`, `Created native directory`, `Updated package.json | no changes`, `Finished prebuild`
- Gradle — **BUILD SUCCESSFUL in 19m 26s, 395 actionable tasks: 395 executed** (`:app:assembleDebug` in this audit's verbose run; the trusted `release` job runs `:app:assembleRelease` in the finalizer — both prove native build chain works)

Independent `npx expo install --check` in this audit also exits 0.

## Candidate delta

- `package.json` — 5 version bumps exactly matching expected SDK 57 patches:
  ```diff
  - expo ~57.0.19          -> ~57.0.24
  - expo-constants ~57.0.16 -> ~57.0.19
  - expo-router ~57.0.18    -> ~57.0.22
  - expo-sharing ~57.0.17   -> ~57.0.21
  - @expo/metro-runtime ~57.0.14 -> ~57.0.16
  ```
- `package-lock.json` — 74 lines updated correspondingly (resolved versions, integrity).
- `src/`, `scripts/`, `__tests__/`, `docs/` — byte-identical to pristine (confirmed `diff -rq` shows only ephemeral `.expo`, `node_modules`, `.gradle`, `android/build` differences plus the two committed files). No domain, UI, or analytics code changed.
- `__tests__/domain/v3-08-finalizer-wrapper.test.ts` — preserved (127 lines, contract tests for single-shell wrapper, APK locate, fixture alignment, no chrono/premium).
- `scripts/finalizer-e2e.sh` — preserved (1155 bytes, `#!/usr/bin/env bash`, `set -euo pipefail`, `find android/app/build/outputs/apk/release`, empty guards, `adb install -r`, `npm run e2e:android`, `bash -n` OK).
- No change to `.github/workflows/chorescore-v3-finalize.yml` — still `script: |` (intentionally, as Builder cannot edit workflows).

This is the minimal correct repair: it does not discard accepted V3-01..V3-08 work and resolves the concrete failing check.

## Why this is accept

Per audit instructions: `Accept iff the trusted verification passed AND no mustFix remains.` 

- Trusted verification **passed** (exit 0) — mandatory condition satisfied.
- The previously mustFix F1 (expo versions) is now fixed and verified.
- The remaining F2 (workflow multi-line) is **not a Builder mustFix** for this cycle: `AGENTS.md` and `TASKS.json` explicitly assign it to the trusted shell, and `docs/NEXT_CYCLE.md` labels it F2 (trusted-shell only). Forcing the Builder to edit a protected workflow would violate `AGENTS.md`.
- All V3-01..V3-07 invariants and V3-08 cost/privacy/a11y gates remain green; no prohibited pattern introduced.

The next step (per `TASKS.json` and `docs/NEXT_CYCLE.md`) is for the trusted shell to apply the one-line workflow change on `main` and re-run the finalizer to obtain `android-release-apk-sha256` and `android-api35-install-launch-e2e-green`. Product-side readiness is complete.

## Independent checks

| Check | Result |
|---|---|
| Trusted verification exit code | **0** — full log shows typecheck, 431 tests, privacy 71, cost 21, expo check up-to-date, export, prebuild, Gradle BUILD SUCCESSFUL |
| `npx expo install --check` (independent) | exit 0 `Dependencies are up to date` |
| `npm run typecheck` (independent) | PASS |
| `npm run test` (independent) | 22 suites 431 tests PASS |
| Package versions vs expected | 5/5 match expected `~57.0.24` / `~57.0.19` / `~57.0.22` / `~57.0.21` / `~57.0.16` |
| Delta vs pristine | only package.json + lock differ; no src/scripts/tests drift |
| WIP wrapper preservation | `v3-08-finalizer-wrapper.test.ts` exists and passes; `finalizer-e2e.sh` bash -n OK; `e2e-android.js` node --check OK |
| Export / prebuild / build | Exported to temp dir, 1318 modules, 3.2MB hbc, prebuild cleared/created, 395 Gradle tasks SUCCESS |
| V3-01..V3-07 regression | ledgers zero-sum per currency, integer minor units, snapshot immutable, Minutes/Points distinct, materialized balances, auth/revision, privacy deterministic cache etc. — all PASS |
| Cost gates V3-08 | 10 gates PASS: 50k bounded, tab 0, delta sync, 1-write, 10k cache, privacy gate, offline, golden path, WCAG AA |
| Prohibited patterns | no chrono, no Premium/paywall, no cross-currency netting, no silent conversion, no destructive history, no Firebase in domain, no operational ID leakage (grep confirms) |
| Workflow state | `script: |` still present at lines 90-94 — correctly deferred to trusted shell per AGENTS.md; `finalizer-e2e.sh` ready for single-line invocation |

## Findings

### F1 (resolved, not mustFix) — expo dependency alignment

- **Path:** `package.json` / `package-lock.json`
- **Problem:** Previously blocked trusted verification (exit 1) with outdated deps.
- **Evidence:** Diff pristine→candidate shows 5 bumps to expected versions; `npx expo install --check` now PASS; TRUSTED log confirms `Dependencies are up to date` and reaches export/prebuild/BUILD SUCCESSFUL.
- **Required fix:** None — already fixed via `npx expo install --fix` and committed.
- **Verification:** Trusted verification exit 0 with all gates; independent `expo install --check` exit 0.

### F2 (informational, not Builder mustFix) — workflow emulator multi-line still present, trusted-shell owned

- **Path:** `.github/workflows/chorescore-v3-finalize.yml` (emulator step, lines 90-94)
- **Problem:** Still `script: |` with `set -euo pipefail` etc. Will fail in finalizer's `android-emulator-runner@v2` (dash). Same root cause as 35293489693 etc.
- **Evidence:** Current workflow cat shows `script: |` block; diff vs pristine none; `scripts/finalizer-e2e.sh` exists but not invoked. TASKS explicitly says Builder must not edit workflows.
- **Required fix:** Trusted shell on `main`: replace with `script: bash scripts/finalizer-e2e.sh` then re-run finalizer. No product code change needed.
- **Verification:** Finalizer must pass Android API 35 install/launch/golden path and attest `android-release-apk-sha256` + `android-api35-install-launch-e2e-green`.

## State

| Item | Status |
|---|---|
| V3-01 .. V3-07 | `complete` — no regression |
| V3-08 (product/config) | `accept` — expo alignment fixed, all trusted product checks green, delta minimal |
| Trusted verification | exit 0 |
| Candidate delta | `accept` — preserved WIP, only version bumps |
| Next step | Trusted shell workflow one-liner + finalizer re-run for APK/E2E attestation (no Builder rebuild needed) |

No mustFix remains in Builder scope. No additional product, privacy, cost, or UI blocker.

