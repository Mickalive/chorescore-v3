{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-07",
    "objective": "REPAIR V3-07: Fix the test file types: cast intentionally-malformed records with 'as unknown as' or 'as any' where the test deliberately injects forbidden fields (line 434, 870); use full valid event shapes or casts for the clean-product records (lines 501-505); replace 'commercial-use' with a valid DataProcessingPurpose (e.g. 'synthetic-data-generation') or cast the argument (line 730). Then rerun the full trusted gate.; Allow label as pipeline INPUT (it is consumed for classification and never emitted): remove 'label'/'title'/'notes' from the input-forbidden list (or restrict input validation to operational IDs only) and rely on the output event types + PrivacyReleaseGate to guarantee no free text in outputs. Add a test proving a labeled contribution fact transforms successfully AND the emitted AnonymousContributionEvent contains no label/title/notes field.; Make the test data satisfy the gate's own anti-reconstruction rules: use >= 10 records with repeated (category, month, beneficiaryCount) combinations so combinations.size <= facts.length * 0.8 and facts.length >= minCohortSize*2, and use non-empty data for the audit-log test (or assert the gate's actual conservative behavior instead of approval). Do not weaken the gate's re-identification protections.; Align test and implementation: either assert 2 (queries remaining after this query) in the test, or change the service to report remaining-including-current (limit - queriesThisMinute - 1 is the 'after this query' count; if the intended semantics is 'remaining including this one', return limit - queriesThisMinute). Pick one documented semantic and make the test match it.; Make the test deterministic: use a negative retentionDays (e.g. -1) so cutoff is strictly in the future of any just-created record, or change getExpiredRecords to '<=' so a zero-retention record created in the same millisecond is expired, or inject a clock. Keep the retention semantics documented and deterministic.; Resolve the concrete failing trusted check without discarding already-correct candidate work, then rerun the full gate.",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [
      "trusted verification exit 0 — no TS errors",
      "all 70+ tests pass (no new failures)",
      "F1 repaired — test file type errors resolved",
      "F2 repaired — contribution/entry transforms are not dead code, labeled facts transform successfully, output events contain no free text",
      "F3 repaired — gate tests use ≥10 records and pass with approved=true",
      "F4 repaired — query budget remaining count matches documented semantic",
      "F5 repaired — consent expiry test is deterministic across runs",
      "no regression V3-01 through V3-06",
      "Operational Store et Research Analytics Plane séparés",
      "analytics désactivable sans casser le produit",
      "taxonomie/classification versionnée",
      "aucun texte libre/ID opérationnel dans sorties",
      "privacy release gate et anti-reconstruction",
      "événements V3 couvrent contributions dépenses settlements usage",
      "classification cache évite appels identiques répétés",
      "aucun LLM synchrone dans le parcours utilisateur",
      "sans nouvel événement la pipeline ne rescane/reclassifie pas",
      "rétention et reprocessing contrôlés"
    ],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress",
      "V3-04: accepted and must not regress",
      "V3-05: accepted and must not regress",
      "V3-06: accepted and must not regress"
    ]
  }
}
