{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-02",
    "objective": "REPAIR V3-02: Keep the demo fixture internally consistent: either seed/create the demo household (and demo user) under the canonical ids (DEMO_HOUSEHOLD_ID 'h-core', 'demo-user-alex') or use the id returned by create() consistently for memberships, members, tasks, entries, todos and currentHouseholdId. Also gate sign-in/loadHouseholds on repository readiness (consume reposReady) or retry the fixture once repos are initialized, so an early sign-in cannot permanently skip the fixture.; Add tests covering the SQLite-backed repositories (e.g. CRUD round-trips against a mocked/in-memory expo-sqlite driver or the factory's fallback selection), verifying that business data written through the repositories can be read back and that the in-memory fallback is selected in the test environment.",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [
      "app Expo démarre et exporte Android",
      "racine groupes sans restriction ni plan",
      "trois onglets exacts",
      "design system V3 cohérent et non générique",
      "aucune UX Premium/paywall/chrono",
      "données métier nombreuses dans un stockage local indexé plutôt que gros tableaux JSON",
      "navigation courante rendue depuis le local sans fetch cloud obligatoire",
      "ports/adapters préservent le domaine provider-agnostic"
    ],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress"
    ]
  }
}
