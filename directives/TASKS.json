{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-04",
    "objective": "REPAIR V3-04: Implement the Compenser action on the Balances screen (inline or modal): select contribution creditor and amount, quote the money amount via the group rate (quoteCrossLedgerMoneyAmount), display Avant/Apres for both ledgers, run validateSettlementPreconditions, persist an immutable CrossLedgerSettlement with rateSnapshot via repos.settlements.create, then refresh both ledger views. Keep the action disabled when crossLedgerCompensationEnabled=false or no rate is configured.; Add a filtered history section at the bottom of Balances (Tout / Contributions / Depenses / Compensations), reusing paginateActivityLog with settlements included, compact rows per §10, paginated and cached per V3_BACKEND_FRUGAL.md §6.; Wire the materialized balance service into the screen: maintain a materialized snapshot, apply deltas on entry add/remove/settlement, and refresh on focus or on a data-change signal from AppContext so Balances reflects new entries without full replay and without staleness.; Filter settlements by period (filterByPeriod on occurredAt) before computing period balances, in both the screen and the materialized service, and add tests asserting a settlement outside the period does not affect the period view while remaining in all-time.; Replace with a test that exercises the real refresh contract: render the Balances screen against a fake repo, add an entry, and assert the view updates via delta without a full re-read; or assert the screen subscribes to a data-change signal. Keep a bounded-read assertion based on repository call counts rather than wall-clock time.",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [
      "deux blocs de ledger distincts",
      "périodes sont des vues et non resets",
      "suggestions de règlement cohérentes",
      "compensation off par défaut",
      "taux groupe explicite et snapshot historique",
      "aucune conversion implicite ou cross-currency",
      "balances matérialisées == reconstruction ledger dans tests",
      "ouvrir Balances ne nécessite pas de full scan historique distant"
    ],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress"
    ]
  }
}
