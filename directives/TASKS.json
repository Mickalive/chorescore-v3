{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-08",
    "objective": "REPAIR V3-08: Escape the apostrophe (doesn\\'t) or reword the title (e.g. 'does not reload') so the string literal is valid.; Change the import on line 14 to: import { typography, colors } from './design-system/theme';; Make the test scan for actual design-token usage rather than any occurrence of the word (e.g. assert no 'terracotta:' color key / no warm hex values in the colors object, or read the parsed theme instead of the raw file), or reword the theme.ts comment so the assertion is meaningful. The design system itself is V3-clean (graphite/off-white/metallic, forest green/wine red only) and must stay so.; Add a real compensation step to the golden path test: create a CrossLedgerSettlement via repos.settlements.create with a rateSnapshot (contributionValue/contributionUnit/moneyAmountMinor/currency), then assert both ledgers remain zero-sum and the settlement's effect on contribution and money balances is applied (per materializedBalances deltaUpdateContributionFromSettlement / deltaUpdateMoneyFromSettlement semantics).; Resolve the concrete failing trusted check without discarding already-correct candidate work, then rerun the full gate.",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [
      "tests et privacy gate verts",
      "cost regression gate vert",
      "audit visuel V3",
      "accessibilité et grands textes",
      "offline et erreurs persistence",
      "50k entrées historiques n'impliquent pas 50k reads à l'ouverture",
      "tab switching ne recharge pas les mêmes objets",
      "sync de trois deltas ne recharge pas l'historique",
      "writes contribution/dépense bornés et documentés",
      "10k labels identiques ne causent pas 10k appels IA",
      "Android export/prebuild/release APK",
      "installation lancement sans Metro",
      "golden path E2E automatisé",
      "iOS readiness documentée"
    ],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress",
      "V3-04: accepted and must not regress",
      "V3-05: accepted and must not regress",
      "V3-06: accepted and must not regress",
      "V3-07: accepted and must not regress"
    ]
  }
}
