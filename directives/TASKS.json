{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-08",
    "objective": "REPAIR V3-08: Change the workflow's emulator step to invoke the golden path as a SINGLE line so the action cannot split it, e.g. `script: bash scripts/finalizer-e2e.sh` (or a single-line `bash -euo pipefail -c '...'`). The workflow file is trusted-shell owned; the Builder provides scripts/finalizer-e2e.sh containing: set -euo pipefail; apk=$(find android/app/build/outputs/apk/release -type f -name '*.apk' | head -1); adb install -r \"$apk\"; npm run e2e:android. The E2E script already auto-locates the APK and skips install when the package is present, so the script file only needs the same logic in one shell.; In the next cycle, add the workflow-compatible invocation path: create scripts/finalizer-e2e.sh (Builder scope) with the install + golden-path logic in a single shell, and have the trusted shell change the workflow's emulator step to `script: bash scripts/finalizer-e2e.sh`. Keep the E2E hardening — it is necessary once the script actually runs, and it is coherent (no regressions, trusted verification exit 0).",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress",
      "V3-04: accepted and must not regress",
      "V3-05: accepted and must not regress",
      "V3-06: accepted and must not regress",
      "V3-07: accepted and must not regress",
      "V3-08: auditor accepted, pending trusted finalizer completion"
    ]
  }
}
