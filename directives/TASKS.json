{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-08",
    "objective": "REPAIR V3-08: Make the APK path optional when the package is already installed: check 'pm list packages <packageName>' first and only require/use apkPath when an install is actually needed, or auto-locate the APK under android/app/build/outputs/apk/release/ when CHORESCORE_APK_PATH is unset. The implementation must match the documented contract ('optional if already installed').",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [
      "scripts/e2e-android.js runs without CHORESCORE_APK_PATH when package is pre-installed",
      "tsc --noEmit exits 0",
      "All 426 tests pass across 21 suites",
      "No regression on V3-01 through V3-07",
      "Script reaches golden-path steps instead of throwing at first guard"
    ],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress",
      "V3-04: accepted and must not regress",
      "V3-05: accepted and must not regress",
      "V3-06: accepted and must not regress",
      "V3-07: accepted and must not regress"
    ]
  }
}
