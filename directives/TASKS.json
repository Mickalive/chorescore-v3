{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-08",
    "objective": "REPAIR V3-08: Keep the pristine navigation (router.push('/add') + setCurrentHouseholdId(id)) and keep the safe wrapper improvements, then repair the actual blocker: the app must reach the 'Demarrer' screen within the E2E script's fixed 900s window on the API 35 emulator. Investigate the app cold-start / demo sign-in / initial render path (Hermes init, splash screen, SQLite demo seeding, sign-in flow) and/or add mid-wait diagnostics to scripts/e2e-android.js (periodic logcat/dumpsys during the Demarrer wait, not only at timeout) so the next cycle can capture the exact failure point if it persists. Preserve V3-01..V3-07 invariants.",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress",
      "V3-04: accepted and must not regress",
      "V3-05: accepted and must not regress",
      "V3-06: accepted and must not regress",
      "V3-07: accepted and must not regress",
      "V3-08: audit accepted, pending finalizer handoff — E2E blocker must be repaired first"
    ]
  }
}
