{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-04",
    "objective": "REPAIR V3-04: Wire a delta-based refresh contract: maintain the materialized snapshot and apply deltas for entries added/removed in other tabs (via a data-change signal from AppContext, or a delta-aware focus refresh that fetches only new/changed entries), so Balances reflects new entries without a full re-read/full replay on every focus. Add the required test: render the Balances screen against a fake repo, add an entry, and assert the view updates via delta without a full re-read, using repository call counts (not wall-clock time); or assert the screen subscribes to a data-change signal. Remove the unproven claim in the test header.; Apply the settlement's money delta to the materialized money snapshot (add a deltaUpdateMoneyFromSettlement or equivalent: creditor +moneyAmountMinor, counterparty -moneyAmountMinor) so both ledger views refresh immediately after repos.settlements.create, and use the settlement entity returned by repos.settlements.create (real id) for state and delta so the in-session state matches the persisted ledger.",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [
      "deux blocs de ledger distincts",
      "périodes sont des vues et non resets",
      "suggestions de règlement cohérentes",
      "compensation off par défaut",
      "taux groupe explicite et snapshot historique",
      "aucune conversion implicite ou cross-currency",
      "balances matérialisées == reconstruction ledger dans tests",
      "ouvrir Balances ne nécessite pas de full scan historique distant",
      "delta-based refresh: screen updates without full re-read on focus",
      "money delta applied immediately after settlement creation"
    ],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress"
    ]
  }
}
