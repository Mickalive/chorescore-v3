{
  "schemaVersion": 1,
  "milestone": "v3-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V3-08",
    "objective": "REPAIR V3-08: Increment consecutiveEmptyMatchCount only when a REAL dump ran (cache miss) and returned no matching node. Concretely: expose a fromCache flag from dumpUi() (or compare _dumpCacheTime before/after the findNodes call) and guard the increment on the dump actually having executed; cache hits must neither increment nor reset the counter. Keep the force-stop + relaunch recovery and the counter resets on crash/dump-failure/stuck recovery.; Strengthen the contract test to assert the cache-miss guard: e.g. assert dumpUi() returns a fromCache flag (or equivalent) and that the consecutiveEmptyMatchCount increment is conditioned on a real dump having run (cache miss). Reverting the guard must fail the test.",
    "scope": "Repair only the audited must-fix findings on the preserved V3 baseline; do not rebuild accepted work.",
    "acceptance": [],
    "previousCriteriaPreserved": [
      "V3-01: accepted and must not regress",
      "V3-02: accepted and must not regress",
      "V3-03: accepted and must not regress",
      "V3-04: accepted and must not regress",
      "V3-05: accepted and must not regress",
      "V3-06: accepted and must not regress",
      "V3-07: accepted and must not regress",
      "V3-08: audit repair, crash-detection mustFix pending fix"
    ]
  }
}
